// カタログ129: 5問化 (lv741〜790)
// 方針: 値・可否で止まっている既出を「実体・過程・逆変換」に上げる。要素分解原則を適用。
const TASKS = [];
function t(o) { TASKS.push(o); }

t({ lv: 741, concept: 'bit演算: 目標XORを作る部分集合の構成', title: '目標XORになる最小の組み合わせ', tags: ['bit演算'], io: '1行目にn、2行目にn個の整数a(1..n)、3行目に目標値xが与えられる。', ask: 'a1からanの中から順序を保ったまま部分集合を選び、選んだ要素すべてのXORがちょうどxになるようにする。まず選ぶ要素数が最小のものを探し、要素数が同じ候補が複数あるときは選んだindex(1始まり)の並びが辞書順で最小になるものを選ぶ。1行目に要素数を出力し、要素数が1以上なら2行目に選んだindexを昇順で空白区切りで出力する。空集合(要素数0)で条件を満たす場合は1行目に0だけを出力して終える。どのように選んでもxを作れない場合はimpossibleとだけ出力する。', h1: 'それまでの要素で作れるXOR値の集合を覚えておき、要素を1つ見るたびに新しく作れる値を追加していく、部分和問題のXOR版だと考える。', h2: '同じXOR値に複数の作り方があるときは、要素数が少ない方を残し、要素数が同じなら選んだindex列が辞書順で小さい方を残す。', ref: 'n = int(input())\na = list(map(int, input().split()))\nx = int(input())\nreachable = {0: ()}\nfor idx in range(n):\n    val = a[idx]\n    pos = idx + 1\n    nxt = dict(reachable)\n    for xr, subset in reachable.items():\n        nxr = xr ^ val\n        cand = subset + (pos,)\n        cur = nxt.get(nxr)\n        if cur is None or (len(cand), cand) < (len(cur), cur):\n            nxt[nxr] = cand\n    reachable = nxt\nif x in reachable:\n    subset = reachable[x]\n    print(len(subset))\n    if subset:\n        print(" ".join(map(str, subset)))\nelse:\n    print("impossible")', tip: 'XORには繰り上がりがないため、和がxになる部分集合を探す普通の部分和DPと同じ考え方でそのままXOR版に置き換えられる。同じ値を複数の方法で作れるときにどれを残すかという比較ルールさえ決めれば、値だけでなく実際の作り方まで復元できる。', ins: ['1\n5\n5', '1\n5\n0', '3\n1 2 3\n3', '4\n1 2 4 8\n5', '3\n1 1 1\n1', '2\n2 4\n1'] })
t({ lv: 742, concept: 'bit演算: 最大XORペアの実際の組', title: '最大XORを作る2つのindex', tags: ['bit演算'], io: '1行目にn、2行目にn個の整数a(1..n)が与えられる。', ask: 'a1からanの中から異なる2つを選んだときのXORが最大になる組を1組求める。最大値を与える組が複数あるときは小さい方のindexが最小のものを、それでも複数あれば大きい方のindexが最小のものを選ぶ。選んだ2つのindex(1始まり)を小さい順に空白区切りで1行に出力する。', h1: 'nが小さいうちは全ペアのXORを計算して比較すれば十分間に合う。', h2: 'indexを小さい方から順に二重ループで走査し、それまでの最大値より真に大きい値が出たときだけ答えを更新すれば、自然と最小のindex組が残る。', ref: 'n = int(input())\na = list(map(int, input().split()))\nbest = -1\nbi = bj = -1\nfor i in range(n):\n    for j in range(i + 1, n):\n        v = a[i] ^ a[j]\n        if v > best:\n            best = v\n            bi, bj = i + 1, j + 1\nprint(bi, bj)', tip: 'XORの最大ペアは各数を2進数の桁でたどるトライ木を使うとO(n log(最大値))で求められる。値の実体(どの組か)まで求めたいときは、トライ木の各節にindexも持たせておくと復元がしやすくなる。', ins: ['2\n1 2', '3\n5 1 2', '4\n4 8 15 16', '4\n10 3 7 12', '5\n1 2 3 4 5', '3\n1 4 6'] })
t({ lv: 743, concept: 'DP: 石を踏む経路の復元', title: '辞書順最小の飛び石ルート', tags: ['DP'], io: '1行目にn(石の数)とk(1回のジャンプで進める最大歩数)、2行目にn個の0か1(1が安全な石、0が水没した石で、石は1からnの番号)が与えられる。', ask: 'カエルは石1から出発し、1回のジャンプで1歩以上k歩以下先の石へ進める。ただし安全な石(値が1)にしか着地できない。石nに到達するために必要な最小ジャンプ回数を求め、その回数を達成できる経路の中で訪れる石番号の列が辞書順で最小になるものを1つ選ぶ。1行目に最小ジャンプ回数、2行目に石1から石nまで訪れた石番号を順番に空白区切りで出力する。石nに到達できない場合はimpossibleとだけ出力する。', h1: '石nから逆向きに、各石からゴールまでの最小ジャンプ回数をあらかじめ求めておく。', h2: '出発地点から、まだ通っていない石の中で最小ジャンプ回数を1つ減らせる石のうち、番号が最も小さいものを貪欲に選んでいけば辞書順最小の経路になる。', ref: 'n, k = map(int, input().split())\ns = list(map(int, input().split()))\nINF = float("inf")\ndist = [INF] * (n + 1)\ndist[n] = 0\nfor i in range(n - 1, 0, -1):\n    if s[i - 1] == 0:\n        continue\n    best = INF\n    for j in range(i + 1, min(i + k, n) + 1):\n        if s[j - 1] == 1 and dist[j] + 1 < best:\n            best = dist[j] + 1\n    dist[i] = best\nif dist[1] == INF:\n    print("impossible")\nelse:\n    path = [1]\n    cur = 1\n    remaining = dist[1]\n    while cur != n:\n        for nxt in range(cur + 1, min(cur + k, n) + 1):\n            if s[nxt - 1] == 1 and dist[nxt] == remaining - 1:\n                path.append(nxt)\n                cur = nxt\n                remaining -= 1\n                break\n    print(len(path) - 1)\n    print(" ".join(map(str, path)))', tip: '最短経路の値だけでなく経路そのものを復元したいときは、まずゴールまでの最短距離を全地点で求めておき、その距離が1つ減る隣を貪欲にたどると、最短性を保ったまま辞書順最小の経路が得られる。', ins: ['2 1\n1 1', '1 1\n1', '3 1\n1 0 1', '5 2\n1 1 0 1 1', '6 3\n1 0 0 1 0 1', '5 3\n1 1 1 1 1'] })
t({ lv: 744, concept: 'ゲームDP: 両端取りゲームの実況', title: '端から取り合う勝負の実況中継', tags: ['DP'], io: '1行目にn、2行目にn個の整数(カードの得点)が与えられる。', ask: '2人のプレイヤーが交互にカードの列の左端か右端から1枚ずつ取っていき、n枚すべてがなくなるまで続ける。先手から始め、双方とも自分の合計得点を最大化するように最適に行動する。自分の得点が左端を取っても右端を取っても同じになるときは必ず左端を取る。実際に取られていくカードの値を取られた順に1行目へ空白区切りで出力し、2行目に先手の合計得点を出力する。', h1: '区間[i,j]にカードが残っているとき、今その区間を取る側が最終的に自分の取り分としていくら得られるかをdp[i][j]とおいて考える。', h2: 'dp[i][j]はa[i]+(区間の合計-dp[i+1][j])と a[j]+(区間の合計-dp[i][j-1])の大きい方になる。値が同じときは左端を選んだ扱いにする。', ref: 'def main():\n    n = int(input())\n    a = list(map(int, input().split()))\n    prefix = [0] * (n + 1)\n    for i in range(n):\n        prefix[i + 1] = prefix[i] + a[i]\n    memo = {}\n\n    def dp(i, j):\n        if i > j:\n            return 0\n        if i == j:\n            return a[i]\n        key = (i, j)\n        if key in memo:\n            return memo[key]\n        left = a[i] + ((prefix[j + 1] - prefix[i + 1]) - dp(i + 1, j))\n        right = a[j] + ((prefix[j] - prefix[i]) - dp(i, j - 1))\n        res = left if left >= right else right\n        memo[key] = res\n        return res\n\n    i, j = 0, n - 1\n    order = []\n    while i <= j:\n        if i == j:\n            order.append(a[i])\n            i += 1\n            continue\n        left = a[i] + ((prefix[j + 1] - prefix[i + 1]) - dp(i + 1, j))\n        right = a[j] + ((prefix[j] - prefix[i]) - dp(i, j - 1))\n        if left >= right:\n            order.append(a[i])\n            i += 1\n        else:\n            order.append(a[j])\n            j -= 1\n    p1 = sum(order[t] for t in range(0, len(order), 2))\n    print(" ".join(map(str, order)))\n    print(p1)\n\n\nmain()', tip: 'このdpは「最終スコア」ではなく「今取る側が最終的に手にする取り分」を表しているのがポイントで、相手も最適に行動する前提を(区間の合計-相手の取り分)という形で組み込んでいる。同じ値の実況記録は将棋やオセロの棋譜のように、最適な意思決定の過程をそのまま見せてくれる。', ins: ['1\n7', '2\n3 3', '3\n1 5 2', '4\n8 15 3 7', '5\n4 1 6 2 9', '6\n2 2 2 2 2 2'] })
t({ lv: 745, concept: 'バックトラック: 重複なし連結の実際の文字列', title: '文字が重ならない最長のつなぎ方', tags: ['バックトラック'], io: '1行目にn、2行目にn個の単語(すべて小文字アルファベット、空白区切り)が与えられる。', ask: '与えられた単語の中から、元の並び順を保ったまま好きなだけ選んで連結し、できた文字列にどの文字も2回以上現れないようにする(1つも選ばずにできる空文字列も候補に含める)。そのような連結でできる文字列のうち長さが最大のものを求め、同じ長さの候補が複数あるときは文字列として辞書順で最小のものを選ぶ。1行目に選んだ文字列(空文字列なら何も文字を出力しない空行)、2行目にその長さを出力する。', h1: 'nが小さいので、どの単語を選ぶかを0か1かのビット集合(bitmask)で全通り試すことができる。', h2: '選んだ単語を元の順番のまま連結してから、文字の種類数(setに入れたときの大きさ)と文字列の長さが一致するかどうかで重複がないか判定できる。', ref: 'n = int(input())\nwords = input().split()\nbest = None\nbest_str = ""\nfor mask in range(1 << n):\n    idxs = [i for i in range(n) if mask & (1 << i)]\n    s = "".join(words[i] for i in idxs)\n    if len(set(s)) != len(s):\n        continue\n    key = (-len(s), s)\n    if best is None or key < best:\n        best = key\n        best_str = s\nprint(best_str)\nprint(-best[0])', tip: '「作れるかどうか」だけでなく実際の文字列まで求める問題では、比較のキーを(-長さ, 文字列)のようなタプルにしておくと、そのままの大小比較が「長い方が勝ち、同じ長さなら辞書順で小さい方が勝ち」という判定になってくれる。', ins: ['1\nabc', '2\nab cd', '2\naa bb', '3\na b a', '3\nun iq ue', '4\nx y xz w'] })
t({ lv: 746, concept: '貪欲: ラベル分割の実際の断片を全部', title: '同じ文字をまとめた欠片を全部出す', tags: ['貪欲'], io: '1行に小文字アルファベットからなる文字列sが与えられる。', ask: 'sを左から順にできるだけ多くの区間に分割する。ただし、どの文字も分割後に2つ以上の区間にまたがって現れてはいけない(同じ文字が現れる範囲は必ず1つの区間に収まる)。区間の個数が最大になるように分割し、できた各区間の文字列を出現順に空白区切りで1行に出力する。', h1: '各文字が文字列の中で最後に現れるindexを先に調べておく。', h2: '区間の左端から右へ見ていき、これまでに見た文字の最後に現れるindexの最大値まで区間を伸ばし、現在位置がそこに追いついたら区間を区切る。', ref: 's = input()\nlast = {}\nfor i, c in enumerate(s):\n    last[c] = i\nparts = []\nstart = 0\nend = 0\nfor i, c in enumerate(s):\n    end = max(end, last[c])\n    if i == end:\n        parts.append(s[start:i + 1])\n        start = i + 1\nprint(" ".join(parts))', tip: 'この貪欲法は「各文字が最後に現れるindex」という前計算だけで、区間を伸ばすか区切るかを機械的に決められる典型例。区切りの個数だけでなく実際の断片を出すと、なぜそこで切れるのかがひと目でわかる。', ins: ['a', 'ab', 'aa', 'abab', 'abac', 'abcdefg'] })
t({ lv: 747, concept: '数え上げ: 単調な3つ組を全部列挙', title: '増加または減少する3つ組を洗い出す', tags: ['数え上げ'], io: '1行目にn、2行目にn個の整数が与えられる。', ask: 'index i<j<kのすべての組み合わせのうち、a[i]<a[j]<a[k](狭義の増加)またはa[i]>a[j]>a[k](狭義の減少)になっているものをすべて求める。見つかった組を(i,j,k)が辞書順で小さい順に並べ、1行に1組ずつ空白区切りのindex(1始まり)で出力する。条件を満たす組が1つもない場合はnoneとだけ出力する。', h1: '3重ループで全部の(i,j,k)を試しても、nが小さいうちは十分間に合う。', h2: '間の値が等しい場合(a[i]とa[j]が同じなど)は増加でも減少でもないので数えないよう、不等号は必ず狭義(<または>)で判定する。', ref: 'n = int(input())\na = list(map(int, input().split()))\nres = []\nfor i in range(n):\n    for j in range(i + 1, n):\n        for k in range(j + 1, n):\n            if (a[i] < a[j] < a[k]) or (a[i] > a[j] > a[k]):\n                res.append((i + 1, j + 1, k + 1))\nif not res:\n    print("none")\nelse:\n    for r in res:\n        print(r[0], r[1], r[2])', tip: 'このような「全部数える」問題を先に解いていれば、「全部出す」への変更はループの中で個数を1増やしていた場所を、その組をresultへ追加する処理に置き換えるだけで済むことが多い。', ins: ['3\n1 2 3', '3\n3 2 1', '3\n2 2 2', '1\n5', '4\n1 3 2 4', '5\n5 3 4 2 1'] })
t({ lv: 748, concept: '貪欲: 交互01を作る具体的なスワップ列', title: '交互になるための交換を実況', tags: ['貪欲'], io: '1行に0と1だけからなる文字列sが与えられる。', ask: 'sの中から2つの位置を選んで値を交換する操作を繰り返し、隣り合う文字が必ず異なる(0と1が交互に並ぶ)文字列にしたい。0と1それぞれの個数を変えずに交換だけでその形にできる場合、必要な交換回数が最小になるようにし、その最小回数を1行目に出力する。回数が1以上のときは、実際に行う交換を「入れ替える2つの位置(1始まり)」として1回につき1行、行った順に出力する。すでに0回の交換で交互に並んでいる場合は0だけを出力する。0を先頭にする交互列と1を先頭にする交互列のどちらも同じ最小回数で作れるときは0を先頭にする方を選ぶ。どちらの交互列も交換だけでは作れない場合はimpossibleとだけ出力する。', h1: '0を先頭にする交互列と1を先頭にする交互列をそれぞれ目標にして、sに含まれる0と1の個数がその目標に必要な個数と一致するかをまず確認する。', h2: '目標とずれている位置のうち「0であるべきなのに1になっている位置」と「1であるべきなのに0になっている位置」を集め、小さい方から順に1つずつペアにして交換すれば最小回数になる。', ref: 'def target_pattern(n, start):\n    other = "1" if start == "0" else "0"\n    return "".join(start if i % 2 == 0 else other for i in range(n))\n\n\ndef mismatches(s, t):\n    typeA = []\n    typeB = []\n    for i in range(len(s)):\n        sc = s[i]\n        tc = t[i]\n        if sc == tc:\n            continue\n        if tc == "1":\n            typeA.append(i + 1)\n        else:\n            typeB.append(i + 1)\n    return typeA, typeB\n\n\ns = input().strip()\nn = len(s)\ncount0 = s.count("0")\ncount1 = n - count0\ncandidates = []\nfor start in ["0", "1"]:\n    t = target_pattern(n, start)\n    need0 = t.count("0")\n    need1 = n - need0\n    if need0 == count0 and need1 == count1:\n        typeA, typeB = mismatches(s, t)\n        if len(typeA) == len(typeB):\n            candidates.append((len(typeA), start, typeA, typeB))\nif not candidates:\n    print("impossible")\nelse:\n    candidates.sort(key=lambda c: (c[0], c[1]))\n    swaps, start, typeA, typeB = candidates[0]\n    if swaps == 0:\n        print(0)\n    else:\n        print(swaps)\n        for x, y in zip(typeA, typeB):\n            print(x, y)', tip: 'スワップは値の個数を変えず場所を移すだけなので、達成できる目標は0と1の個数がぴったり一致するものに限られる。個数さえ合えば「ずれている場所」を2種類に分けて先頭からペアにするだけで最小交換列が作れる。', ins: ['10', '00', '0011', '1', '000111', '0110'] })
t({ lv: 749, concept: 'DP: 最小コストルートの本数', title: '一番安い上り方は何通り', tags: ['DP'], io: '1行目にn、2行目にn個の整数(石1からnそれぞれに乗るときのコスト)が与えられる。', ask: 'カエルは石1から出発し、1回のジャンプで1つ隣か2つ隣の石へ進める。石nに到達するまでの合計コスト(通ったすべての石のコストの合計、出発点の石1のコストも含む)が最小になるルートが何通りあるかを求めて出力する。', h1: '石iまでの最小コストdp[i]を、dp[i]=cost[i]+min(dp[i-1],dp[i-2])として求める(dp[1]=cost[1], dp[2]=cost[1]+cost[2]とする)。', h2: 'dp[i-1]とdp[i-2]のどちらが小さいかで石iに来る行き方の本数cnt[i]が決まる。同点のときは両方の行き方が使えるのでcnt[i-1]+cnt[i-2]になる。', ref: 'n = int(input())\ncost = list(map(int, input().split()))\nif n == 1:\n    print(1)\nelse:\n    dp = [0] * (n + 1)\n    cnt = [0] * (n + 1)\n    dp[1] = cost[0]\n    cnt[1] = 1\n    dp[2] = cost[0] + cost[1]\n    cnt[2] = 1\n    for i in range(3, n + 1):\n        c = cost[i - 1]\n        if dp[i - 1] < dp[i - 2]:\n            dp[i] = dp[i - 1] + c\n            cnt[i] = cnt[i - 1]\n        elif dp[i - 2] < dp[i - 1]:\n            dp[i] = dp[i - 2] + c\n            cnt[i] = cnt[i - 2]\n        else:\n            dp[i] = dp[i - 1] + c\n            cnt[i] = cnt[i - 1] + cnt[i - 2]\n    print(cnt[n])', tip: 'コストがすべて同じだと最小コストのルートが同点続きになり、その本数はフィボナッチ数列と同じ増え方をする。最小値そのものを求めるDPに、その最小値を達成する経路の本数を横に持たせるだけで数え上げに拡張できる典型パターン。', ins: ['1\n5', '2\n3 4', '3\n1 1 1', '4\n0 0 0 0', '5\n0 0 0 0 0', '6\n2 2 2 2 2 2'] })
t({ lv: 751, concept: '累積和: 累積値から元の増分列を復元', title: '毎日の増分を累積から逆算', tags: ['累積和'], io: '1行目にn、2行目にn個の整数P(1からn番目、i日目までの累積値)が与えられる。', ask: '各日の増分a[i]を求める。a[1]はP[1]そのもの、i>=2のときa[i]はP[i]からP[i-1]を引いた値になる。a[1]からa[n]までを空白区切りで1行に出力する。', h1: '累積値の列は、元の増分列を先頭から順に足し合わせていったものだと考える。', h2: '隣り合うP[i]とP[i-1]の差を取れば、その日単独の増分が復元できる(P[0]は0とみなす)。', ref: 'n = int(input())\np = list(map(int, input().split()))\na = [p[0]] + [p[i] - p[i - 1] for i in range(1, n)]\nprint(" ".join(map(str, a)))', tip: '累積和は元の列から作る変換だけでなく、差分を取って元に戻す逆変換もセットで使えるようにしておくと、センサーの積算値から瞬間値を復元するような場面でそのまま応用できる。', ins: ['1\n5', '3\n1 3 6', '3\n5 5 5', '4\n0 -2 -2 3', '2\n-1 -1', '5\n10 8 8 20 19'] })
t({ lv: 752, concept: 'いもす法: 最大重なり時点の対象区間', title: '一番混み合う瞬間にいる面々', tags: ['いもす法'], io: '1行目にn、続くn行に区間の両端l rが与えられる(区間は両端を含む[l,r]で、上から順に1からnまでの番号とする)。', ask: 'すべての区間の中で、同時に重なっている区間の数が最大になる時刻tを求める。最大値を達成する時刻が複数あるときは最小のtを選ぶ。1行目にtとそのときの重なり数を空白区切りで出力し、2行目にその時刻を含む区間のindex(1始まり)を昇順で空白区切りで出力する。', h1: '各区間の開始で+1、終了の次の時刻で-1が起きるとみなして時刻順に処理すると、各瞬間の重なり数の変化がわかる(いもす法)。', h2: '重なり数が更新されるたびに、それまでの最大値を真に上回ったときだけ記録を更新すれば、最初に最大値に達する最小の時刻が残る。', ref: 'n = int(input())\nintervals = []\nfor _ in range(n):\n    l, r = map(int, input().split())\n    intervals.append((l, r))\nevents = []\nfor l, r in intervals:\n    events.append((l, 1))\n    events.append((r + 1, -1))\nevents.sort()\ncur = 0\nbest_count = -1\nbest_t = None\ni = 0\nm = len(events)\nwhile i < m:\n    pos = events[i][0]\n    while i < m and events[i][0] == pos:\n        cur += events[i][1]\n        i += 1\n    if cur > best_count:\n        best_count = cur\n        best_t = pos\ncovering = [idx + 1 for idx, (l, r) in enumerate(intervals) if l <= best_t <= r]\nprint(best_t, best_count)\nprint(" ".join(map(str, covering)))', tip: 'いもす法は最終的な重なり数の最大値を求めるだけでなく、その瞬間にどの区間が参加しているかまで芋づる式に取り出せる。区間スケジューリングで一番混み合う瞬間に誰がいるかを知りたいときにそのまま使える考え方。', ins: ['1\n5 5', '2\n1 5\n3 7', '3\n1 2\n3 4\n5 6', '3\n2 4\n2 4\n2 4', '4\n1 4\n2 6\n5 8\n7 9', '2\n2 4\n4 6'] })
t({ lv: 753, concept: '2次元累積和: 最大和になる部分長方形の実際の範囲', title: '一番得する長方形はどこにあるか', tags: ['2次元累積和'], io: '1行目にR C、続くR行にそれぞれC個の整数が並ぶ行列が与えられる。', ask: 'この行列の中から、行と列でそれぞれ連続する範囲を選んでできる部分長方形のうち、合計が最大になるものを1つ求める。合計が同じ候補が複数あるときは上端の行番号r1が最小、それでも同じなら左端の列番号c1が最小、それでも同じなら下端の行番号r2が最小、それでも同じなら右端の列番号c2が最小のものを選ぶ。1行目に選んだ範囲をr1 c1 r2 c2(すべて1始まり)の順で出力し、2行目にその合計を出力する。', h1: '2次元の累積和テーブルを先に作っておけば、どんな部分長方形の合計もすぐに計算できる。', h2: 'R,Cが小さいうちは、上端・下端・左端・右端の位置を全通り試しても十分間に合う。', ref: 'R, C = map(int, input().split())\nmat = [list(map(int, input().split())) for _ in range(R)]\npre = [[0] * (C + 1) for _ in range(R + 1)]\nfor i in range(1, R + 1):\n    for j in range(1, C + 1):\n        pre[i][j] = mat[i - 1][j - 1] + pre[i - 1][j] + pre[i][j - 1] - pre[i - 1][j - 1]\n\n\ndef rect_sum(r1, c1, r2, c2):\n    return pre[r2][c2] - pre[r1 - 1][c2] - pre[r2][c1 - 1] + pre[r1 - 1][c1 - 1]\n\n\nbest = None\nfor r1 in range(1, R + 1):\n    for r2 in range(r1, R + 1):\n        for c1 in range(1, C + 1):\n            for c2 in range(c1, C + 1):\n                s = rect_sum(r1, c1, r2, c2)\n                key = (-s, r1, c1, r2, c2)\n                if best is None or key < best:\n                    best = key\nprint(best[1], best[2], best[3], best[4])\nprint(-best[0])', tip: '1次元の最大部分区間和を2次元に広げたもので、2次元累積和さえ用意すれば発想はまったく同じ。行列が負の値ばかりでも範囲は必ず1マス以上選ぶ必要があるので、一番マシな1マスが答えになる。', ins: ['1 1\n5', '1 1\n-3', '2 2\n1 -1\n-1 1', '3 3\n1 -2 1\n1 3 1\n1 -2 1', '2 3\n1 2 3\n-1 -2 -3', '1 4\n-1 2 3 -1'] })
t({ lv: 754, concept: 'しゃくとり: 積がk未満の最長区間の実体', title: '掛けてk未満になる一番長い区間はどこ', tags: ['しゃくとり'], io: '1行目にn k、2行目にn個の正の整数が与えられる。', ask: '連続する区間の中で、含まれる数をすべて掛け合わせた積がk未満になるもののうち、長さが最大のものを1つ求める。長さが同じ候補が複数あるときは開始位置が最も早いものを選ぶ。見つかった場合は1行目に開始と終了の位置(1始まり、両端を含む)、2行目にその長さを出力する。そのような区間が1つも存在しない場合はnoneとだけ出力する。', h1: '右端を1つずつ伸ばしながら積を更新し、積がk以上になったら左端を動かして積を減らす、二点法(しゃくとり法)が使える。', h2: '要素はすべて正の整数なので、積は要素を追加すると必ず増え取り除くと必ず減る。この単調性がしゃくとり法の前提になっている。', ref: 'n, k = map(int, input().split())\na = list(map(int, input().split()))\nbest_len = 0\nbest_l = best_r = -1\nprod = 1\nleft = 0\nfor right in range(n):\n    prod *= a[right]\n    while prod >= k and left <= right:\n        prod //= a[left]\n        left += 1\n    if left <= right:\n        length = right - left + 1\n        if length > best_len:\n            best_len = length\n            best_l, best_r = left, right\nif best_len == 0:\n    print("none")\nelse:\n    print(best_l + 1, best_r + 1)\n    print(best_len)', tip: '積がk未満の区間の個数を数えるしゃくとり法と処理の骨組みはほとんど同じで、カウントを足す代わりに区間の長さを記録するだけで実際に一番長い区間を求める問題に変えられる。', ins: ['1 5\n3', '1 1\n3', '4 100\n10 5 2 6', '5 10\n1 2 3 4 5', '4 2\n1 1 1 1', '6 50\n2 3 1 4 2 1'] })
t({ lv: 755, concept: 'しゃくとり: ちょうどk種類の部分配列の個数', title: '異なる値がちょうどk種類の区間の数', tags: ['しゃくとり'], io: '1行目にn k、2行目にn個の整数が与えられる。', ask: '連続する区間(部分配列)のうち、含まれる値の種類数がちょうどkになるものが何個あるかを求めて出力する。', h1: '種類数がk以下の区間の個数を求める関数を先に作る。', h2: 'ちょうどk種類の区間の個数は、k以下の区間の個数からk-1以下の区間の個数を引くことで求められる。', ref: 'def at_most(a, k):\n    if k < 0:\n        return 0\n    count = {}\n    left = 0\n    total = 0\n    for right in range(len(a)):\n        count[a[right]] = count.get(a[right], 0) + 1\n        while len(count) > k:\n            count[a[left]] -= 1\n            if count[a[left]] == 0:\n                del count[a[left]]\n            left += 1\n        total += right - left + 1\n    return total\n\n\nn, k = map(int, input().split())\na = list(map(int, input().split()))\nprint(at_most(a, k) - at_most(a, k - 1))', tip: 'ちょうどkを直接扱うしゃくとり法は書きにくいが、k以下は区間を広げるほど崩れない単調な条件なので扱いやすい。k以下の個数からk-1以下の個数を引くという引き算のテクニックは、種類数に限らずいろいろな条件で使える定番の変形。', ins: ['1 1\n5', '1 2\n5', '3 2\n1 2 3', '5 1\n1 1 1 1 1', '4 2\n1 2 1 3', '6 3\n1 2 3 1 2 3'] })
t({ lv: 756, concept: 'ハッシュ: 重複なし最長区間の実際の範囲', title: '被りなし最長区間はどこからどこか', tags: ['ハッシュ'], io: '1行目にn、2行目にn個の整数が与えられる。', ask: '連続する区間の中で、同じ値が2回以上現れない(すべて異なる値だけからなる)もののうち、長さが最大になるものを1つ求める。長さが同じ候補が複数あるときは開始位置が最も早いものを選ぶ。開始と終了の位置(1始まり、両端を含む)を1行目に、その長さを2行目に出力する。', h1: '各値が最後に現れたindexを覚えておきながら右端を伸ばしていく、しゃくとり法が使える。', h2: '今見ている値が現在の区間の中に既にあれば、区間の左端をその値の直後まで一気に動かす。', ref: 'n = int(input())\na = list(map(int, input().split()))\nlast = {}\nleft = 0\nbest_len = 0\nbest_l = best_r = -1\nfor right in range(n):\n    if a[right] in last and last[a[right]] >= left:\n        left = last[a[right]] + 1\n    last[a[right]] = right\n    length = right - left + 1\n    if length > best_len:\n        best_len = length\n        best_l, best_r = left, right\nprint(best_l + 1, best_r + 1)\nprint(best_len)', tip: '重複が出た瞬間に左端を1つずつ動かすのではなく、直前に同じ値が出たindexの直後まで一気に飛ばすことで全体を高速に処理できる。長さだけでなく実際の区間まで覚えておけば、最長区間がどのデータの塊なのか具体的に確認できる。', ins: ['1\n7', '3\n1 1 1', '5\n1 2 3 1 2', '6\n1 2 1 3 4 5', '4\n1 2 3 4', '5\n5 4 3 2 1'] })
t({ lv: 757, concept: 'ハッシュ: アナグラム班分けの中身を全部出す', title: 'アナグラム班の名簿を全部見せる', tags: ['ハッシュ'], io: '1行目にn、2行目にn個の単語(すべて小文字アルファベット、空白区切り)が与えられる。', ask: '同じ文字を同じ個数だけ使っている単語同士(アナグラムの関係にある単語同士)を同じ班にまとめる。班を、その班に含まれる最小のindexが小さい順に並べ、各班のメンバーのindex(1始まり)を昇順にした空白区切りの行として、1班につき1行出力する。', h1: '各単語の文字を並べ替えて得られる文字列(ソートした文字列)をキーにすれば、同じキーを持つ単語同士が同じ班になる。', h2: '班分けが終わったら、各班の中で一番小さいindexを比較キーにして班同士を並べ替える。', ref: 'n = int(input())\nwords = input().split()\ngroups = {}\nfor i, w in enumerate(words):\n    key = "".join(sorted(w))\n    groups.setdefault(key, []).append(i + 1)\norder = sorted(groups.values(), key=lambda g: g[0])\nfor g in order:\n    print(" ".join(map(str, g)))', tip: 'アナグラム判定で使う「文字をソートした文字列をキーにする」という発想は、班の個数を数えるだけでなく、実際に誰と誰が同じ班かという名簿を作るところまでそのまま延長できる。', ins: ['1\na', '2\nab ba', '3\nab cd ba', '4\neat tea tan ate', '5\nab cd ef dc ba', '3\nabc def ghi'] })
t({ lv: 758, concept: 'ハッシュ: 出現回数がちょうどkの要素', title: 'ぴったりk回だけ出てきた値', tags: ['ハッシュ'], io: '1行目にn k、2行目にn個の整数が与えられる。', ask: '配列の中に出現回数がちょうどk回である値をすべて求め、値の小さい順に空白区切りで1行に出力する。そのような値が1つもない場合はnoneとだけ出力する。', h1: '各値の出現回数をハッシュ(辞書)で数える。', h2: '出現回数を数え終えたら、回数がちょうどkのものだけを取り出して値でソートする。', ref: 'from collections import Counter\n\nn, k = map(int, input().split())\na = list(map(int, input().split()))\nc = Counter(a)\nres = sorted(v for v, cnt in c.items() if cnt == k)\nprint(" ".join(map(str, res)) if res else "none")', tip: '出現回数を数えるだけでなく「ちょうどその回数だった値は何か」まで踏み込むと、最頻値以外の情報(例えば1回しか出ていない外れ値)も同じ仕組みで取り出せることがわかる。', ins: ['1 1\n5', '1 2\n5', '5 2\n1 1 2 3 3', '6 1\n4 4 5 6 6 7', '4 4\n9 9 9 9', '5 3\n2 2 2 2 2'] })
t({ lv: 759, concept: 'ソート: 上位k件を全部並べる', title: '成績上位k人をそのまま発表', tags: ['ソート'], io: '1行目にn k、2行目にn個の整数が与えられる。', ask: '値が大きい方からk個を選ぶ。値が同じもの同士は、元の並びで先に出てきた方を先に選ぶ。選んだk個の値を、大きい順(同じ値同士は選んだ順)に空白区切りで1行に出力する。', h1: '各要素について(値の符号を反転したもの, 元のindex)のペアを作ってソートすると、値が大きい順で同点なら元の順番が早い方が先に並ぶ。', h2: 'ソート後の先頭からk個を取り出せばよい。', ref: 'n, k = map(int, input().split())\na = list(map(int, input().split()))\nidx = sorted(range(n), key=lambda i: (-a[i], i))\ntop = idx[:k]\nprint(" ".join(str(a[i]) for i in top))', tip: '「k番目に大きい値」だけを求める問題を少し発展させて上位k件を全部出す場合、同点の扱いを元の順番が早い方を優先すると決めておけば、出力が一意に定まり採点しやすくなる。', ins: ['1 1\n5', '3 3\n1 2 3', '5 2\n4 4 4 1 1', '4 1\n3 3 3 3', '6 4\n5 1 5 3 2 5', '2 2\n-1 -5'] })
t({ lv: 760, concept: 'ヒープ: k番目に小さい和を作るペア', title: 'k番目に小さい和はどの組み合わせか', tags: ['ヒープ'], io: '1行目にn m k、2行目に配列A(n個)、3行目に配列B(m個)が与えられる。', ask: 'Aの要素1つとBの要素1つを選んでできる組み合わせはn*m通りある。それぞれの和を計算し、和が小さい順に並べる。和が同じもの同士はAのindexが小さい方を先に、それも同じならBのindexが小さい方を先にする。この並びでk番目(1始まり)にある組み合わせのAとBのindex(1始まり)を1行目に空白区切りで、その和を2行目に出力する。', h1: 'n,mが小さいので、すべての組み合わせとその和をリストにしてしまってから並べ替えれば十分間に合う。', h2: '並べ替えるときのキーを(和, Aのindex, Bのindex)の組にすれば、同点の扱いも含めて一度にソートできる。', ref: 'n, m, k = map(int, input().split())\nA = list(map(int, input().split()))\nB = list(map(int, input().split()))\npairs = []\nfor i in range(n):\n    for j in range(m):\n        pairs.append((A[i] + B[j], i + 1, j + 1))\npairs.sort()\ns, i, j = pairs[k - 1]\nprint(i, j)\nprint(s)', tip: '「k番目に小さい和はいくつか」という値だけの問題も、キーに(和, i, j)を使ってソートしておけば、そのままインデックスを読み取るだけで、どの組み合わせかという実体まで手に入る。', ins: ['1 1 1\n3\n4', '2 2 1\n1 5\n2 3', '2 2 4\n1 5\n2 3', '2 2 2\n1 4\n2 3', '2 2 3\n1 2\n2 1', '3 1 2\n5 1 3\n0'] })
t({ lv: 761, concept: 'ヒープ: 窓口ごとの完了時刻を全員分', title: 'k窓口シミュレーションの逐次完了時刻', tags: ['ヒープ'], io: '1行目にn k、続くn行に各客の到着時刻と対応にかかる時間が到着順に与えられる。', ask: 'k個の窓口があり、それぞれ1人ずつしか対応できない。各客は到着したら、その時点で一番早く空く窓口(すでに空いていればすぐ、対応中ならそれが終わる時刻)に案内される。同じ早さで空く窓口が複数あるときは窓口番号が小さい方を使う。対応は到着時刻と窓口が空く時刻のうち遅い方から始まり、対応にかかる時間だけ進んで終わる。すべての客について、対応を終える時刻を到着順に空白区切りで1行に出力する。', h1: '各窓口が次に空く時刻を配列で持っておき、客が来るたびに一番早く空く窓口を探す。', h2: '一番早く空く窓口を探すときは、空く時刻が同じなら窓口番号が小さい方を選ぶ。', ref: 'n, k = map(int, input().split())\ncustomers = [tuple(map(int, input().split())) for _ in range(n)]\nfree = [0] * k\ncompletions = []\nfor arrival, dur in customers:\n    best_idx = min(range(k), key=lambda idx: (free[idx], idx))\n    start = max(arrival, free[best_idx])\n    finish = start + dur\n    free[best_idx] = finish\n    completions.append(finish)\nprint(" ".join(map(str, completions)))', tip: 'ここではk窓口分の次に空く時刻を線形探索しているが、kが大きい場合はこれを優先度付きキュー(ヒープ)にしておくと、毎回一番早く空く窓口をすぐに取り出せるようになる。', ins: ['1 1\n0 5', '2 1\n0 3\n1 2', '3 2\n0 5\n0 3\n1 1', '1 3\n2 4', '4 2\n0 3\n0 3\n1 2\n5 1', '3 1\n0 2\n0 2\n0 2'] })
t({ lv: 762, concept: '集合: 最長連続区間の具体的な範囲', title: '一番長く連続している区間はどこからどこ', tags: ['集合'], io: '1行目にn、2行目にn個の整数(重複や順序は自由)が与えられる。', ask: '与えられた整数の集合(重複は無視する)の中から、1ずつ増える連続した整数の並びを作れる範囲を探し、その長さが最大になるものを1つ求める。長さが同じ範囲が複数あるときは、開始する値が最小のものを選ぶ。開始の値と終了の値を1行目に空白区切りで出力し、2行目にその長さを出力する。', h1: '整数をいったん集合(set)に入れてしまえば、ある値がその集合に含まれているかをすぐ調べられる。', h2: 'ある値vについて、v-1が集合に含まれていなければvはどれかの連続区間の始まりだとわかる。そこからv+1, v+2, …が含まれる間だけ区間を伸ばしていく。', ref: 'n = int(input())\na = list(map(int, input().split()))\ns = set(a)\nbest_len = 0\nbest_start = None\nfor v in s:\n    if v - 1 not in s:\n        length = 1\n        cur = v\n        while cur + 1 in s:\n            cur += 1\n            length += 1\n        if length > best_len:\n            best_len = length\n            best_start = v\n        elif length == best_len and v < best_start:\n            best_start = v\nprint(best_start, best_start + best_len - 1)\nprint(best_len)', tip: 'この方法は集合の各値をだいたい定数回しか調べないため、値の範囲がとても広くても要素数に比例した時間で最長連続区間を求められる。値を全部ソートしてから隣同士を比べる方法よりも速くなることが多い。', ins: ['1\n5', '5\n1 2 3 5 6', '6\n100 4 200 1 3 2', '5\n7 7 7 7 7', '4\n-3 -2 -1 0', '4\n1 2 10 11'] })
t({ lv: 763, concept: 'ソート: 差の合計が最小になる実際の組分け', title: '差の合計最小のペア分けを全部出す', tags: ['ソート'], io: '1行目に偶数n、2行目にn個の整数が与えられる。', ask: 'n個の数をちょうど2個ずつのペアに分け、各ペアの絶対差の合計が最小になるように組み合わせる。最小の合計を達成する組分けとして、数を小さい順に並べてから隣同士を2個ずつ組にしたものを採用し、できたペアを小さい方の値の順に1行に1ペアずつ空白区切り(小さい方、大きい方の順)で出力する。', h1: '全体をまず昇順にソートしてみる。', h2: 'ソート後に隣り合う2個ずつ(1,2番目、3,4番目、…)を組にすると、差の合計が最小になることが知られている。', ref: 'n = int(input())\na = list(map(int, input().split()))\nb = sorted(a)\nfor i in range(0, n, 2):\n    print(b[i], b[i + 1])', tip: 'ソート済みの数直線上で隣同士を組にするのが差の合計を最小にする理由は、離れた数同士を組ませるとその分だけ余分な差が生まれてしまうから。似た値をペアにするのが常に一番損をしない組分けになる。', ins: ['2\n5 1', '4\n4 1 3 2', '4\n1 1 1 1', '6\n10 1 2 9 3 8', '2\n-5 5', '6\n5 5 5 1 1 1'] })
t({ lv: 764, concept: '数え上げ: 転倒しているペアを全部列挙', title: '転倒ペアをすべて書き出す', tags: ['数え上げ'], io: '1行目にn、2行目にn個の整数が与えられる。', ask: 'index i<jのうちa[i]>a[j]となっている組(転倒しているペア)をすべて求め、(i,j)が辞書順で小さい順に1行に1組ずつ空白区切りのindex(1始まり)で出力する。転倒しているペアが1つもない場合はnoneとだけ出力する。', h1: '2重ループですべての(i,j)の組を調べれば、nが小さいうちは十分間に合う。', h2: '転倒数を数えるときに個数を1増やしていた場所を、そのままresultへの追加に変えるだけでよい。', ref: 'n = int(input())\na = list(map(int, input().split()))\nres = []\nfor i in range(n):\n    for j in range(i + 1, n):\n        if a[i] > a[j]:\n            res.append((i + 1, j + 1))\nif not res:\n    print("none")\nelse:\n    for i, j in res:\n        print(i, j)', tip: '転倒数そのものはマージソートを利用するとO(n log n)で数えられるが、実際にどのペアが転倒しているかまで全部欲しい場合はその個数自体が最大でn(n-1)/2個になり得るため、素直な2重ループで列挙するほうがわかりやすい場合も多い。', ins: ['1\n5', '2\n2 1', '3\n3 2 1', '4\n2 4 1 3', '5\n1 2 3 5 4', '5\n5 4 3 2 1'] })
t({ lv: 765, concept: '数え上げ: 和がkになるペアを全部書き出す', title: '和がkになる組み合わせを総ざらい', tags: ['数え上げ'], io: '1行目にn k、2行目にn個の整数が与えられる。', ask: 'index i<jのうちa[i]+a[j]がちょうどkになる組をすべて求め、(i,j)が辞書順で小さい順に1行に1組ずつ空白区切りのindex(1始まり)で出力する。そのような組が1つもない場合はnoneとだけ出力する。', h1: '2重ループですべての(i,j)の組を試し、和がkになるかどうかを直接確かめれば、nが小さいうちは十分間に合う。', h2: '和がkになる組の個数を数えるだけならハッシュ(辞書)でk-a[i]がすでに出てきたかを調べる方法が速いが、組そのものを全部欲しい場合は見つけるたびにその場でindexの組を記録しておく。', ref: 'n, k = map(int, input().split())\na = list(map(int, input().split()))\nres = []\nfor i in range(n):\n    for j in range(i + 1, n):\n        if a[i] + a[j] == k:\n            res.append((i + 1, j + 1))\nif not res:\n    print("none")\nelse:\n    for i, j in res:\n        print(i, j)', tip: '和がkになるペアの個数を数える定番の解法はハッシュを使ってO(n)で済ませられるが、そのままでは組そのものを復元しにくい。個数ではなく組そのものを全部出したいときは、素直な2重ループの方が実装のミスが少なく安全なことが多い。', ins: ['1 5\n5', '2 5\n2 3', '4 5\n1 4 2 3', '3 100\n1 2 3', '5 7\n1 5 2 4 3', '5 -2\n-1 1 -2 2 0'] })
t({ lv: 766, concept: '文字列: 単語頻度表を作る', title: '単語頻度表を作る', tags: ['ハッシュ'], io: '1行目に文字列(空白区切りの単語列)を与える。', ask: '各単語の出現回数を数え、出現回数の多い順に単語を並べて出力せよ。回数が同じ場合は、その単語が文字列中で最初に現れた順で並べる。出力は「単語:回数」の形式で1行に1語ずつ、複数行で示せ。', h1: '各単語の出現回数をハッシュ(辞書)で数える。', h2: '同じ回数の単語は、最初に出てきた順を保つように安定ソートする。', ref: 's = input()\nwords = s.split()\ncount = {}\norder = []\nfor w in words:\n    if w not in count:\n        count[w] = 0\n        order.append(w)\n    count[w] += 1\norder_index = {w: i for i, w in enumerate(order)}\nresult = sorted(order, key=lambda w: (-count[w], order_index[w]))\nfor w in result:\n    print(f"{w}:{count[w]}")', tip: 'カウントとソートを組み合わせる集計は自然言語処理の基本操作です。Pythonのsortは安定ソートなので、同着の順序をキーに元の出現順を加えるだけで簡単に保てます。ハッシュ(辞書)による頻度集計はログ解析などでも頻出のテクニックです。', ins: ['a a b b c', 'apple', 'the quick brown fox the fox the', 'x y x y x y', 'z z y y x x w', 'cat dog cat dog cat bird'] })
t({ lv: 767, concept: '数論: 互いに素が続く最長区間の中身', title: '仲良し隣人の実際の並び', tags: ['数論'], io: '1行目に整数n、2行目にn個の整数(空白区切り)を与える。', ask: '隣り合う要素の最大公約数が1であるような連続部分列のうち、最長のものを求め、その要素をそのまま空白区切りで出力せよ。同じ長さの区間が複数あるときは、最も左から始まるものを採用する。', h1: '隣接ペアのgcdを1と比べながら、現在の区間の開始位置を管理する。', h2: '区間長が更新されたときだけ開始位置と長さを記録すればよい。', ref: 'import math\nn = int(input())\na = list(map(int, input().split()))\nbest_len = 1\nbest_start = 0\ncur_start = 0\nfor i in range(1, n):\n    if math.gcd(a[i - 1], a[i]) == 1:\n        pass\n    else:\n        cur_start = i\n    if i - cur_start + 1 > best_len:\n        best_len = i - cur_start + 1\n        best_start = cur_start\nprint(" ".join(map(str, a[best_start:best_start + best_len])))', tip: 'math.gcdを使えば最大公約数はすぐ求まります。隣接ペアが互いに素かどうかだけを見ればよいので、区間全体で互いに素かを確認する必要はありません。この考え方は最長連続区間を求める問題全般に応用できます。', ins: ['1\n7', '5\n2 3 4 9 8', '6\n2 4 6 3 5 7', '4\n1 1 1 1', '3\n6 10 15', '5\n1 2 1 3 1'] })
t({ lv: 768, concept: '累積和: 和が0の区間を列挙', title: '和が0の区間を全部並べる', tags: ['累積和'], io: '1行目に整数n、2行目にn個の整数(空白区切り)を与える。', ask: '連続部分列であって合計が0になるものを、開始位置と終了位置(1-indexed、両端を含む)の組としてすべて求めよ。開始位置が小さい順に、開始位置が同じ場合は終了位置が小さい順に並べ、1行に1組「開始 終了」の形式で出力せよ。該当する区間がなければ NONE と出力せよ。', h1: '累積和(先頭に0を含む)を計算し、同じ値を持つ2つの位置の間が和0の区間になる。', h2: '累積和の値ごとに出現位置のリストを持ち、後で全ペアを開始位置順に並べ直す。', ref: 'n = int(input())\na = list(map(int, input().split()))\nprefix = [0] * (n + 1)\nfor i in range(n):\n    prefix[i + 1] = prefix[i] + a[i]\nfrom collections import defaultdict\npos = defaultdict(list)\nfor i in range(n + 1):\n    pos[prefix[i]].append(i)\nresult = []\nfor value, idxs in pos.items():\n    for x in range(len(idxs)):\n        for y in range(x + 1, len(idxs)):\n            result.append((idxs[x] + 1, idxs[y]))\nresult.sort()\nif result:\n    for s, e in result:\n        print(s, e)\nelse:\n    print("NONE")', tip: '和が0になる区間は「同じ累積和の値が2回出てくる区間」に対応します。辞書で累積和の出現位置をまとめておけば、該当する区間を全て効率よく列挙できます。個数を数えるだけでなく実際の区間を書き出す練習として有用です。', ins: ['1\n0', '1\n5', '5\n1 -1 1 -1 1', '4\n2 -2 3 -3', '3\n1 2 3', '6\n3 -1 -2 4 -4 0'] })
t({ lv: 769, concept: '文字列: 一意文字を出現順に列挙', title: '一度きりの文字を全部並べる', tags: ['文字列'], io: '1行目に文字列を与える。', ask: '文字列中にちょうど1回だけ出現する文字を、その文字列に現れる順番のまま全て取り出して連結し出力せよ。該当する文字がなければ NONE と出力せよ。', h1: '各文字の出現回数をまず数える。', h2: '元の文字列を先頭から見て、回数が1の文字だけを拾えば順序も自然に保たれる。', ref: 's = input()\nfrom collections import Counter\ncnt = Counter(s)\nresult = "".join(ch for ch in s if cnt[ch] == 1)\nprint(result if result else "NONE")', tip: 'Counterで頻度を数えたあと、元の文字列をもう一度なぞるだけで出現順を保ったまま条件を満たす文字を取り出せます。順序を保持したまま抽出したいときによく使う定石です。', ins: ['aabbc', 'abcabc', 'x', 'abcdefg', 'aabbcde', 'mississippi'] })
t({ lv: 770, concept: '数え上げ: 各要素の右の小さい要素数を列挙', title: '各要素の右に何個小さいか(全部出力)', tags: ['数え上げ'], io: '1行目に整数n、2行目にn個の整数(空白区切り)を与える。', ask: '各要素について、自分より右側にあり、かつ自分より小さい値を持つ要素の個数を求め、先頭の要素から順に空白区切りで出力せよ。', h1: '各位置について右側を単純に走査して数えても答えは出る。', h2: 'nが小さいので二重ループで十分間に合う。', ref: 'n = int(input())\na = list(map(int, input().split()))\nresult = []\nfor i in range(n):\n    c = 0\n    for j in range(i + 1, n):\n        if a[j] < a[i]:\n            c += 1\n    result.append(c)\nprint(" ".join(map(str, result)))', tip: 'この総和を求める問題は既出ですが、各要素ごとの値を全部出す設定にすると出力の作り方まで問われます。BIT(フェニック木)や分割統治を使えばO(n log n)にも高速化できますが、まずは二重ループで正しさを確認するのが近道です。', ins: ['1\n5', '5\n5 2 6 1 3', '4\n1 2 3 4', '4\n4 3 2 1', '3\n2 2 2', '6\n3 4 9 6 1 2'] })
t({ lv: 771, concept: '二分探索: 値の最初と最後の位置', title: '過半数の値の出現範囲を求める', tags: ['二分探索'], io: '1行目に整数n、2行目にソート済みのn個の整数(空白区切り)、3行目に整数xを与える。', ask: 'ソート済み配列の中でxが最初に現れる位置と最後に現れる位置を1-indexedで求め、空白区切りで出力せよ。xが配列に存在しなければ -1 -1 と出力せよ。', h1: '二分探索で「x以上が最初に現れる位置」と「xより大きい値が最初に現れる位置」をそれぞれ求める。', h2: '存在確認は、最初の位置を求めたあとにその位置の値がxと一致するかで判定できる。', ref: 'import bisect\nn = int(input())\na = list(map(int, input().split()))\nx = int(input())\nlo = bisect.bisect_left(a, x)\nhi = bisect.bisect_right(a, x)\nif lo == hi:\n    print(-1, -1)\nelse:\n    print(lo + 1, hi)', tip: '二分探索でソート済み配列の境界を求めるテクニックは、Pythonのbisectモジュールにそのまま用意されています。bisect_leftとbisect_rightを組み合わせると、値の存在確認だけでなく出現範囲や個数もすぐに求まります。', ins: ['1\n5\n5', '1\n5\n3', '7\n1 2 2 2 2 2 3\n2', '5\n1 1 1 1 1\n1', '6\n1 2 3 4 5 6\n7', '5\n2 4 4 4 6\n4'] })
t({ lv: 772, concept: '貪欲: 頻度を相異なるにした後に残る文字を列挙', title: '頻度を相異なるにした後の中身', tags: ['貪欲'], io: '1行目に小文字英字からなる文字列を与える。', ask: '各文字の出現回数が互いに異なるようになるまで、出現回数が重複している文字の回数を1ずつ減らす操作を繰り返す(削除する文字は毎回、現在最大の回数を持つものから優先して選ぶ)。操作が終わった後に残る各文字の出現回数を、文字の辞書順に「文字:回数」の形式で1行ずつ出力せよ。回数が0になった文字は出力しない。', h1: '文字ごとの出現回数を数え、回数が大きい方から処理すると重複を避けやすい。', h2: '既に使われている回数の集合を持ちながら、重複するたびに1ずつ減らして空いている回数を探す。', ref: 's = input()\nfrom collections import Counter\ncnt = Counter(s)\nitems = sorted(cnt.items(), key=lambda kv: -kv[1])\nused = set()\nfinal = {}\nfor ch, f in items:\n    while f > 0 and f in used:\n        f -= 1\n    if f > 0:\n        used.add(f)\n    final[ch] = f\nfor ch in sorted(final.keys()):\n    if final[ch] > 0:\n        print(f"{ch}:{final[ch]}")', tip: '頻度を相異なるにする問題は「値」だけでなく「どの文字が最終的に何回残るか」まで見ると、貪欲法の動きがよく分かります。大きい頻度から処理し、既に使われた回数を避けて1つずつ減らしていくのが典型的な貪欲の型です。', ins: ['aab', 'aabbbcc', 'a', 'aaaa', 'xxyyzz', 'abcabcabc'] })
t({ lv: 773, concept: '区間: 統合後の区間リストを出力', title: '重なる区間を統合してすべて出す', tags: ['区間'], io: '1行目に整数n、続くn行に各区間の開始と終了(空白区切りの整数2つ、開始≤終了)を与える。', ask: '重なる区間(端点が接する場合も含む)をすべて統合し、統合後の区間を開始位置の昇順に、1行ごとに「開始 終了」の形式で出力せよ。', h1: '区間を開始位置でソートしてから順に見ていく。', h2: '直前に確定した区間の終了と次の区間の開始を比較し、続いているかどうかを判定する。', ref: 'n = int(input())\nintervals = []\nfor _ in range(n):\n    l, r = map(int, input().split())\n    intervals.append((l, r))\nintervals.sort()\nmerged = []\nfor l, r in intervals:\n    if merged and l <= merged[-1][1]:\n        if r > merged[-1][1]:\n            merged[-1] = (merged[-1][0], r)\n    else:\n        merged.append((l, r))\nfor l, r in merged:\n    print(l, r)', tip: 'ソートしてから隣接する区間だけを見比べるのは、区間統合問題に共通する型です。統合後の個数だけでなく実際の区間そのものを出力する練習は、後で差分更新や幾何のアルゴリズムを学ぶときの土台にもなります。', ins: ['1\n3 5', '3\n1 3\n2 6\n8 10', '4\n1 2\n3 4\n5 6\n7 8', '3\n1 4\n2 3\n3 5', '2\n1 2\n2 3', '5\n5 7\n1 3\n2 4\n6 8\n10 12'] })
t({ lv: 774, concept: '数え上げ: 最大値がkの区間を列挙', title: '最大がちょうどkの区間を全部書き出す', tags: ['数え上げ'], io: '1行目に整数nと整数k(空白区切り)、2行目にn個の整数(空白区切り)を与える。', ask: '連続部分列であって、その中の最大値がちょうどkであるものを、開始位置と終了位置(1-indexed、両端含む)の組としてすべて求めよ。開始位置が小さい順に、開始位置が同じ場合は終了位置が小さい順に並べ、1行に1組「開始 終了」の形式で出力せよ。該当する区間がなければ NONE と出力せよ。', h1: '全ての開始位置と終了位置の組を全探索し、その区間の最大値を求めても、nが小さければ間に合う。', h2: '最大値がkを超えた時点で、その開始位置からの延長は打ち切ってよい。', ref: 'n, k = map(int, input().split())\na = list(map(int, input().split()))\nresult = []\nfor i in range(n):\n    cur_max = a[i]\n    for j in range(i, n):\n        cur_max = max(cur_max, a[j])\n        if cur_max > k:\n            break\n        if cur_max == k:\n            result.append((i + 1, j + 1))\nif result:\n    for l, r in result:\n        print(l, r)\nelse:\n    print("NONE")', tip: '最大値がちょうどkの区間を全部列挙する問題は全探索でも書けますが、最大値がkを超えた時点で打ち切る枝刈りをすると無駄な探索を減らせます。個数を数えるだけでなく実際の区間を書き出す練習は、しゃくとり法や単調デックを学ぶときの土台になります。', ins: ['1 5\n5', '1 5\n3', '5 4\n1 4 2 5 3', '3 10\n1 2 3', '3 3\n3 3 3', '4 5\n5 1 5 1'] })
t({ lv: 775, concept: '文字列: ランレングス符号化を実際に作る', title: 'ランレングス圧縮の中身を書く', tags: ['文字列'], io: '1行目に英字からなる文字列を与える。', ask: '同じ文字が連続する部分を「文字+連続回数」の形にまとめて並べたランレングス符号化の結果を1行で出力せよ(回数が1の場合も数字1を省略せず書く)。', h1: '先頭から同じ文字が続く区間の長さを数えていく。', h2: '文字が変わったタイミングで、それまでの文字と回数をまとめて出力用の文字列に足す。', ref: 's = input()\nresult = ""\ni = 0\nn = len(s)\nwhile i < n:\n    j = i\n    while j < n and s[j] == s[i]:\n        j += 1\n    result += s[i] + str(j - i)\n    i = j\nprint(result)', tip: 'ランレングス符号化は画像や文字列の圧縮でよく使われる基本的な手法です。同じ文字が続く区間の長さを数えるだけの単純な処理ですが、可逆な符号化(後で元に戻せる)であることが重要な性質です。', ins: ['a', 'aaa', 'aabbba', 'abcabc', 'zzzzzzzzzz', 'aabbcc'] })
t({ lv: 776, concept: 'ハッシュ: 2回だけ出た値を列挙', title: 'ちょうど2回出る値を全部挙げる', tags: ['ハッシュ'], io: '1行目に整数n、2行目にn個の整数(空白区切り)を与える。', ask: '配列中にちょうど2回だけ出現する値を、小さい順に空白区切りで出力せよ。該当する値がなければ NONE と出力せよ。', h1: '各値の出現回数を辞書で数える。', h2: '回数がちょうど2の値だけを取り出し、ソートして出力する。', ref: 'n = int(input())\na = list(map(int, input().split()))\nfrom collections import Counter\ncnt = Counter(a)\nresult = sorted(v for v in cnt if cnt[v] == 2)\nif result:\n    print(" ".join(map(str, result)))\nelse:\n    print("NONE")', tip: 'Counterで頻度を数えるのは頻出パターンです。個数だけでなく「どの値が条件を満たすか」を全部列挙する形にすると、辞書の値を条件でフィルタしてソートする練習になります。', ins: ['1\n5', '4\n1 1 2 2', '6\n3 3 3 4 4 5', '5\n1 2 3 4 5', '8\n2 2 3 3 4 4 5 5', '5\n-1 -1 2 3 3'] })
t({ lv: 777, concept: '文字列: 語彙数が釣り合う切れ目を列挙', title: '釣り合う切れ目を全部挙げる', tags: ['文字列'], io: '1行目に文字列sを与える。', ask: '文字列を空でない前半と後半に分けるとき、前半に含まれる文字の種類数と後半に含まれる文字の種類数が等しくなるような分割位置(前半の文字数、1以上s長-1以下)を、小さい順に空白区切りで出力せよ。該当する位置がなければ NONE と出力せよ。', h1: '前半の文字種類数は左から累積、後半の文字種類数は右から累積して求められる。', h2: '各分割位置での両方の種類数を比較していけばよい。', ref: 's = input()\nn = len(s)\nleft_count = [0] * (n + 1)\nseen = set()\nfor i in range(n):\n    seen.add(s[i])\n    left_count[i + 1] = len(seen)\nright_count = [0] * (n + 1)\nseen = set()\nfor i in range(n - 1, -1, -1):\n    seen.add(s[i])\n    right_count[i] = len(seen)\nresult = []\nfor cut in range(1, n):\n    if left_count[cut] == right_count[cut]:\n        result.append(cut)\nif result:\n    print(" ".join(map(str, result)))\nelse:\n    print("NONE")', tip: '「左から見た累積」と「右から見た累積」を両方前計算しておくテクニックは、この手の分割位置探索でよく使われます。両側から別々に集計することで、全分割位置をO(n)でまとめて調べられます。', ins: ['ab', 'aaab', 'abab', 'abc', 'xxyyzz', 'abcabcabc'] })
t({ lv: 778, concept: '貪欲: 全冊を二人に割り振る実際の組分け', title: 'ふたりのために本を仕分ける', tags: ['貪欲'], io: '1行目に整数n、2行目にn冊の本を読むのにかかる時間(空白区切りの整数、1-indexedの本番号順)を与える。', ask: 'n冊の本を2人で分担して読む。所要時間が大きい本から順に、次の規則で1冊ずつ2人のどちらかに割り当てる貪欲法を実装せよ。その時点での合計読書時間が小さい方に割り当てる(同じ場合は1人目に割り当てる)。割り当てが終わったあと、1人目が担当することになった本の番号(1-indexed、入力での順番)を昇順に空白区切りで出力せよ。', h1: '所要時間が大きい本から先に割り当てると、後から調整する余地が少なくなり公平に近づく。', h2: 'その時点での2人の合計時間を比べ、小さい方に割り当てていく。', ref: 'n = int(input())\nt = list(map(int, input().split()))\norder = sorted(range(n), key=lambda i: -t[i])\ntotal = [0, 0]\nassign = [[], []]\nfor i in order:\n    p = 0 if total[0] <= total[1] else 1\n    assign[p].append(i)\n    total[p] += t[i]\nresult = sorted(x + 1 for x in assign[0])\nprint(" ".join(map(str, result)))', tip: 'この貪欲法(LPT法)は必ずしも真の最適解を保証しませんが、多くの場合で良い近似解を素早く作れます。スケジューリング問題で実務的によく使われる考え方です。', ins: ['1\n5', '2\n3 7', '4\n4 3 2 1', '3\n1 1 1', '5\n1 1 1 1 1', '6\n1 2 3 4 5 6'] })
t({ lv: 779, concept: 'ハッシュ: 和0ペアの実際の組み合わせ', title: '打ち消し合うペアを実際に組む', tags: ['ハッシュ'], io: '1行目に整数n、2行目にn個の整数(空白区切り、0を含む可能性あり)を与える。', ask: '配列の要素から2つずつ取り出し、和が0になるペアをできるだけ多く作りたい(同じ位置の要素は1回しか使えない)。値vと-vが両方あればあるだけ組にし、0は2個で1組のペアにする。作られたペアを「a b」(a<=bとなるよう小さい方を先に書く)の形式で、aの昇順(同じaならbの昇順)に1行ずつ出力せよ。ペアが1つも作れなければ NONE と出力せよ。', h1: '正の値vと負の値-vが同じ回数だけ存在する範囲で、ペアを作ることができる。', h2: '0は2個で1組のペアになる。', ref: 'n = int(input())\na = list(map(int, input().split()))\nfrom collections import Counter\ncnt = Counter(a)\npairs = []\nvalues = sorted(set(v for v in cnt if v > 0))\nfor v in values:\n    m = min(cnt[v], cnt[-v])\n    for _ in range(m):\n        pairs.append((-v, v))\nzero_pairs = cnt[0] // 2\nfor _ in range(zero_pairs):\n    pairs.append((0, 0))\npairs.sort()\nif pairs:\n    for x, y in pairs:\n        print(x, y)\nelse:\n    print("NONE")', tip: '和が0になるペアを実際に構成する問題は、値ごとの出現回数を数えるだけで解けます。符号違いの値をハッシュで対応付ける考え方は、two-sum系の問題に共通する土台です。', ins: ['1\n0', '2\n0 0', '4\n1 -1 2 -2', '5\n1 1 -1 2 3', '3\n5 5 5', '8\n0 0 0 -3 3 -3 3 2'] })
t({ lv: 780, concept: '数え上げ: XORがkの区間を列挙', title: 'XORがkになる区間を全部出す', tags: ['bit演算'], io: '1行目に整数nと整数k(空白区切り)、2行目にn個の整数(空白区切り)を与える。', ask: '連続部分列であって、その中の全要素のXORがちょうどkになるものを、開始位置と終了位置(1-indexed、両端含む)の組としてすべて求めよ。開始位置が小さい順に、開始位置が同じ場合は終了位置が小さい順に並べ、1行に1組「開始 終了」の形式で出力せよ。該当する区間がなければ NONE と出力せよ。', h1: '累積XOR(先頭に0を含む)を計算すると、区間のXORは2つの累積XORの排他的論理和で求まる。', h2: '累積XORの値ごとに出現位置を辞書に持ち、目的の値との組を全て数え上げる。', ref: 'n, k = map(int, input().split())\na = list(map(int, input().split()))\nprefix = [0] * (n + 1)\nfor i in range(n):\n    prefix[i + 1] = prefix[i] ^ a[i]\nfrom collections import defaultdict\npos = defaultdict(list)\nfor i in range(n + 1):\n    pos[prefix[i]].append(i)\nresult = []\nfor i in range(n + 1):\n    target = prefix[i] ^ k\n    if target in pos:\n        for j in pos[target]:\n            if j > i:\n                result.append((i + 1, j))\nresult.sort()\nif result:\n    for l, r in result:\n        print(l, r)\nelse:\n    print("NONE")', tip: '区間XORが「2つの累積XORの排他的論理和」で求まるのは、区間和が「2つの累積和の差」で求まるのと同じ考え方です。XORは自分自身とXORすると0になる性質を利用しています。', ins: ['1 5\n5', '1 5\n3', '3 0\n1 2 3', '4 3\n1 2 3 0', '5 7\n1 2 3 4 5', '4 1\n1 1 1 1'] })
t({ lv: 781, concept: '文字列: 先頭に足して回文にする最小追加', title: '回文にする最短の追加文字列', tags: ['文字列'], io: '1行目に文字列sを与える。', ask: '文字列sの先頭に何文字か追加することで、文字列全体を回文にしたい(末尾には追加しない)。追加する文字数が最小になるように、追加すべき文字列を求めて出力せよ。すでに回文で追加が不要な場合は NONE と出力せよ。', h1: '文字列の後ろに区切り文字を挟んで反転した文字列をつなげ、KMPの失敗関数(部分一致テーブル)を計算する。', h2: 'その失敗関数の最後の値が、先頭から続く最長の回文部分の長さになる。', ref: 'def kmp_fail(t):\n    fail = [0] * len(t)\n    k = 0\n    for i in range(1, len(t)):\n        while k > 0 and t[i] != t[k]:\n            k = fail[k - 1]\n        if t[i] == t[k]:\n            k += 1\n        fail[i] = k\n    return fail\n\ns = input()\nrev = s[::-1]\nt = s + "#" + rev\nfail = kmp_fail(t)\nL = fail[-1]\naddition = rev[:len(s) - L]\nprint(addition if addition else "NONE")', tip: 'この手法はKMP法の失敗関数を応用したもので、文字列+区切り文字+反転文字列という形を作ると、先頭から続く最長の回文接頭辞の長さが1回の走査で求まります。線形時間で解ける古典的なテクニックです。', ins: ['a', 'ab', 'aacecaaa', 'abcd', 'abba', 'aabb'] })
t({ lv: 782, concept: '文字列: 最長回文を実際に構成する', title: '作れる最長回文を実際に組み立てる', tags: ['文字列'], io: '1行目に小文字英字の文字列を与える。', ask: '文字列に含まれる文字を自由に並べ替えて作れる最長の回文を1つ構成して出力せよ。左半分は使う文字を辞書順に並べ、右半分はその左半分をちょうど逆順にしたものとする。奇数回出現する文字が1つ以上あるときは、その中で最も小さい文字を中央に1つだけ置く。', h1: '各文字の出現回数を数え、偶数個は左右対称に、奇数個の余り1文字だけを中央候補にする。', h2: '中央に置ける文字は高々1つなので、複数の文字が奇数回あっても中央に選ばれるのは辞書順最小の1つだけになる。', ref: 'from collections import Counter\ns = input()\ncnt = Counter(s)\nhalf = []\nmid = ""\nfor ch in sorted(cnt.keys()):\n    c = cnt[ch]\n    half.append(ch * (c // 2))\n    if c % 2 == 1:\n        if mid == "":\n            mid = ch\nleft = "".join(half)\nresult = left + mid + left[::-1]\nprint(result)', tip: '文字の並べ替えで作れる最長回文は、奇数回出現する文字が2つ以上あっても、そのうち1つだけを中央に置き、残りの奇数分は諦めることで長さを最大化できます。数え上げだけでなく実際に組み立てる練習として有用です。', ins: ['a', 'aabb', 'aaabbbccc', 'abcabc', 'aabbccdde', 'xyz'] })
t({ lv: 783, concept: 'ハッシュ: 差がkの値ペアを列挙', title: '差がkになる値の組を全部書く', tags: ['ハッシュ'], io: '1行目に整数nと整数k(空白区切り、kは1以上)、2行目にn個の整数(空白区切り)を与える。', ask: '配列に含まれる値(重複を除く)の中から2つ選び、大きい方から小さい方を引いた差がちょうどkになる組をすべて求め、「小 大」の形式で、小さい値の昇順に1行ずつ出力せよ。該当する組がなければ NONE と出力せよ。', h1: '配列の値を集合(set)に入れておくと、ある値+kが存在するかをすぐ確認できる。', h2: '各値についてv+kが集合にあるかを調べ、あれば組として記録する。', ref: 'n, k = map(int, input().split())\na = list(map(int, input().split()))\nvalues = set(a)\nresult = []\nfor v in values:\n    if v + k in values:\n        result.append((v, v + k))\nresult.sort()\nif result:\n    for x, y in result:\n        print(x, y)\nelse:\n    print("NONE")', tip: '差がkのペアを探す問題は、集合に値を入れておけば「v+kがあるか」を毎回O(1)で調べられます。二数の差や和を扱う問題では、まず集合やハッシュに値を入れておくのが定石です。', ins: ['1 3\n5', '5 2\n1 3 5 7 9', '6 5\n1 2 3 4 5 6', '3 100\n1 2 3', '5 1\n1 1 2 2 3', '4 4\n-2 2 6 10'] })
t({ lv: 784, concept: '文字列: 最小窓の実際の文字列を求める', title: '全文字を含む最小窓の中身', tags: ['文字列'], io: '1行目に文字列s、2行目に文字列tを与える。', ask: 'sの連続部分文字列のうち、tに含まれる全ての文字を(必要な個数だけ)含む最短のものを求め、その文字列そのものを出力せよ。該当する部分文字列がなければ NONE と出力せよ。同じ長さのものが複数あれば、最も左から始まるものを採用する。', h1: '右端を伸ばしながら必要な文字が揃うまで進め、揃ったら左端を縮めて最小の窓を探す。', h2: '窓の中で不足している文字の種類数を管理すると、揃ったかどうかをO(1)で判定できる。', ref: 's = input()\nt = input()\nfrom collections import Counter\nneed = Counter(t)\nmissing = len(t)\nleft = 0\nbest_len = float("inf")\nbest_start = 0\nfor right, ch in enumerate(s):\n    if need[ch] > 0:\n        missing -= 1\n    need[ch] -= 1\n    while missing == 0:\n        if right - left + 1 < best_len:\n            best_len = right - left + 1\n            best_start = left\n        need[s[left]] += 1\n        if need[s[left]] > 0:\n            missing += 1\n        left += 1\nif best_len == float("inf"):\n    print("NONE")\nelse:\n    print(s[best_start:best_start + best_len])', tip: '最小窓を求める問題は多くの場合「長さ」だけを聞かれますが、実際の文字列を取り出す形にすると、開始位置と終了位置の両方を正しく管理する練習になります。二点法(尺取り法)の代表的な応用例です。', ins: ['a\na', 'a\nb', 'ADOBECODEBANC\nABC', 'aa\naa', 'abcdef\nz', 'bba\nab'] })
t({ lv: 785, concept: '数え上げ: 最長の連続増加区間の中身', title: '右肩上がりの最長区間を実際に出す', tags: ['数え上げ'], io: '1行目に整数n、2行目にn個の整数(空白区切り)を与える。', ask: '隣り合う要素が単調に増加している(直前より大きい)連続部分列のうち、最長のものを求め、その要素をそのまま空白区切りで出力せよ。同じ長さのものが複数あれば、最も左から始まるものを採用する。', h1: '隣り合う要素を比較し、増加が途切れた位置を区間の新しい開始点とする。', h2: '区間長が更新されたときだけ開始位置と長さを記録すればよい。', ref: 'n = int(input())\na = list(map(int, input().split()))\nbest_len = 1\nbest_start = 0\ncur_start = 0\nfor i in range(1, n):\n    if a[i] > a[i - 1]:\n        pass\n    else:\n        cur_start = i\n    if i - cur_start + 1 > best_len:\n        best_len = i - cur_start + 1\n        best_start = cur_start\nprint(" ".join(map(str, a[best_start:best_start + best_len])))', tip: 'この最長増加区間を求める問題は既出ですが、長さだけでなく実際の中身を出力する設定にすると、開始位置をどこで更新するかまで正確に管理する必要があります。同じ骨組みは山型区間や回文区間の探索にも応用できます。', ins: ['1\n9', '5\n1 2 3 4 5', '5\n5 4 3 2 1', '6\n1 3 2 4 5 0', '4\n2 2 2 2', '7\n1 2 1 2 3 4 1'] })
t({ lv: 786, concept: '貪欲: 上位k個の要素を列挙', title: '上位k個を実際に選び出す', tags: ['貪欲'], io: '1行目に整数nと整数k(空白区切り)、2行目にn個の整数(空白区切り)を与える。', ask: '配列の中から値が大きい順にk個選びたい。値が同じ場合は元の配列でのインデックスが小さい方(先に現れた方)を優先して選ぶ。選ばれたk個の要素を、元の配列での出現順に空白区切りで出力せよ。', h1: '値の大きい順に並べ替え、同じ値なら元のインデックスが小さい方を優先する。', h2: '上位k個を選んだら、出力時には元の並び順(インデックス順)に戻す。', ref: 'n, k = map(int, input().split())\na = list(map(int, input().split()))\norder = sorted(range(n), key=lambda i: (-a[i], i))\nchosen = sorted(order[:k])\nprint(" ".join(str(a[i]) for i in chosen))', tip: '上位k個を求める問題は合計値だけを聞かれることが多いですが、実際にどの要素が選ばれるかまで求めると、同点の処理(タイブレーク)を明確に決める練習になります。ソートのキーに複数の条件を並べるテクニックはこの手の問題で頻出です。', ins: ['1 1\n5', '5 3\n5 1 4 1 5', '4 2\n3 3 3 3', '6 4\n10 20 20 5 20 1', '3 3\n1 2 3', '5 1\n2 8 3 8 1'] })
t({ lv: 787, concept: '文字列: 一致に必要な最小回転数', title: '何回まわせば一致するか', tags: ['文字列'], io: '1行目に文字列a、2行目に文字列b(aと同じ長さ)を与える。', ask: 'aの先頭から1文字ずつ末尾に移す操作(左回転)を何回か行うとbに一致させられるか調べたい。一致させるのに必要な最小の左回転回数を出力せよ。何回行っても一致しない場合は -1 と出力せよ。', h1: '左回転k回の結果はa[k:]+a[:k]で作れる。', h2: 'kを0から順に試し、最初に一致したところが答えになる。', ref: 'a = input()\nb = input()\nn = len(a)\nresult = -1\nfor k in range(n):\n    if a[k:] + a[:k] == b:\n        result = k\n        break\nprint(result)', tip: '文字列の回転はスライスで簡単に表現できます。より大きな文字列では「a+aの中にbが部分文字列として含まれるか」を調べる方が高速ですが、小さな入力では単純な全探索でも十分です。', ins: ['a\na', 'ab\nba', 'abcde\ncdeab', 'abc\nacb', 'aaab\nabaa', 'abcd\ndabc'] })
t({ lv: 788, concept: '数え上げ: 積がkの倍数のペアを列挙', title: '掛けるとkの倍数になる組を全部出す', tags: ['数え上げ'], io: '1行目に整数nと整数k(空白区切り、kは1以上)、2行目にn個の正の整数(空白区切り)を与える。', ask: '配列から異なる2つの位置(i<j、1-indexed)を選び、その2つの値の積がkの倍数になる組をすべて求め、iの昇順、同じiならjの昇順に「i j」の形式で1行ずつ出力せよ。該当する組がなければ NONE と出力せよ。', h1: '2つの値を掛けてkで割った余りが0かどうかを直接確認すればよい。', h2: 'nが小さければ全てのペアを二重ループで調べても間に合う。', ref: 'n, k = map(int, input().split())\na = list(map(int, input().split()))\nresult = []\nfor i in range(n):\n    for j in range(i + 1, n):\n        if (a[i] * a[j]) % k == 0:\n            result.append((i + 1, j + 1))\nif result:\n    for i, j in result:\n        print(i, j)\nelse:\n    print("NONE")', tip: '積の割り切れる条件を調べる問題は、実際に掛け算してから余りを見るのが最も分かりやすい方法です。nが大きくなると素因数分解を使った高速化が必要になりますが、まずは愚直な確認から始めるのが定石です。', ins: ['2 5\n5 1', '2 5\n2 3', '4 6\n2 3 4 5', '3 7\n1 1 1', '5 4\n2 2 2 2 2', '4 9\n3 3 3 2'] })
t({ lv: 789, concept: '文字列: 母音差最大の窓の中身', title: '母音優勢の最強区間を実際に出す', tags: ['文字列'], io: '1行目に英小文字の文字列を与える。', ask: '空でない連続部分文字列のうち、(母音の個数)-(子音の個数)が最大になるものを求め、その文字列そのものを出力せよ(母音はa,i,u,e,o)。値が同じ区間が複数あるときは、最も短いものを、それでも複数あるときは最も左から始まるものを採用する。', h1: '母音を+1、子音を-1とみなすと、最大区間を求める問題は最大部分列和の問題と同じ形になる。', h2: '同じ値になる区間が複数あるときは、短い方、それでも同じなら左にあるものを優先する。', ref: 's = input()\nn = len(s)\nvowels = set("aeiou")\nbest = None\nfor i in range(n):\n    diff = 0\n    for j in range(i, n):\n        diff += 1 if s[j] in vowels else -1\n        length = j - i + 1\n        cand = (-diff, length, i)\n        if best is None or cand < best[0]:\n            best = (cand, i, j)\nif best is not None:\n    _, i, j = best\n    print(s[i:j + 1])', tip: '母音と子音の差を+1/-1に置き換えると、この問題は最大部分列和(Kadaneのアルゴリズム)と同じ構造になります。値だけでなく実際の区間を求めるときは、同点をどう選ぶかのルールを決めておくことが大切です。', ins: ['a', 'b', 'aeiou', 'bcdfg', 'aabbaa', 'abcdeaiou'] })
t({ lv: 790, concept: '累積和: 和がkの区間を列挙', title: '和がちょうどkの区間を全部書き出す', tags: ['累積和'], io: '1行目に整数nと整数k(空白区切り、kは0以上)、2行目にn個の0以上の整数(空白区切り)を与える。', ask: '連続部分列であって、その合計がちょうどkになるものを、開始位置と終了位置(1-indexed、両端含む)の組としてすべて求めよ。開始位置が小さい順に、開始位置が同じ場合は終了位置が小さい順に並べ、1行に1組「開始 終了」の形式で出力せよ。該当する区間がなければ NONE と出力せよ。', h1: '累積和を使うと、区間の合計は2点の累積和の差として求まる。', h2: '全ての開始位置と終了位置の組を調べ、差がkになるものを集める。', ref: 'n, k = map(int, input().split())\na = list(map(int, input().split()))\nprefix = [0] * (n + 1)\nfor i in range(n):\n    prefix[i + 1] = prefix[i] + a[i]\nresult = []\nfor i in range(n):\n    for j in range(i, n):\n        if prefix[j + 1] - prefix[i] == k:\n            result.append((i + 1, j + 1))\nif result:\n    for l, r in result:\n        print(l, r)\nelse:\n    print("NONE")', tip: '和がちょうどkになる区間を列挙する問題は、累積和の差を全探索するだけで解けます。配列の値が0以上のときは、0を挟んで同じ和が続けて現れることがある点に注意すると良い練習になります。', ins: ['1 0\n0', '1 5\n5', '5 5\n1 2 3 4 5', '4 0\n0 0 1 0', '3 100\n1 2 3', '6 6\n1 5 0 0 6 0'] })

export const CATALOG = TASKS;

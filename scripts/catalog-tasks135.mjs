// カタログ135: 5問化 (lv2〜60)
// 方針: 値・可否で止まっている既出を「実体・過程・逆変換」に上げる。要素分解原則を適用。
const TASKS = [];
function t(o) { TASKS.push(o); }

t({ lv: 2, concept: '変数と代入: 金額を紙幣枚数に分解', title: '金額を千円・百円・十円に分ける', tags: ['変数と代入'], io: '1行に金額(10円単位の0以上の整数)が与えられる', ask: 'その金額を千円札の枚数、百円玉の枚数、十円玉の枚数の順に空白区切りで1行に出力する(千円札→百円玉→十円玉の順に大きい方から数える)', h1: 'まず金額を1000で割った商が千円札の枚数、余りが残りの金額になる', h2: '残りの金額を100で割り、その余りをさらに10で割ればよい', ref: 'n = int(input())\nsen = n // 1000\nn = n % 1000\nhyaku = n // 100\nn = n % 100\njuu = n // 10\nprint(sen, hyaku, juu)', tip: '商と余りを繰り返し使って値を分解するのは、後で出てくる「割り算だけで桁をばらす」の部品と同じ考え方。お金の計算はプログラミングの練習によく使われる。', ins: ['0', '10', '100', '1230', '9990', '12340'] })
t({ lv: 4, concept: '文字列の操作: 前半と後半の入れ替え', title: '前半と後半を入れ替える', tags: ['文字列の操作'], io: '1行に文字列(長さが偶数)が与えられる', ask: '文字列を前半分と後半分に分け、後半分+前半分の順に連結した文字列を出力する(前半分は先頭からlen//2文字とする)', h1: 's[:len(s)//2] と s[len(s)//2:] でちょうど半分に分けられる', h2: '後半分を先に、前半分を後に連結して出力する', ref: 's = input()\nh = len(s) // 2\nfront = s[:h]\nback = s[h:]\nprint(back + front)', tip: 'スライスで文字列を2つに割ってから順番を入れ替えて組み立てるのは、この後の文字列パズルでよく使う操作。前半と後半が同じ文字だけなら、入れ替えても見た目は変わらない。', ins: ['zz', 'ab', 'abcd', 'aabb', '1234', 'abcdef'] })
t({ lv: 5, concept: '場合分け(if/elif): 予想が正解より大きいか小さいか', title: '予想と正解の大小判定', tags: ['条件分岐'], io: '1行目に予想した数、2行目に正解の数が整数で与えられる', ask: '予想が正解と一致するなら correct、予想の方が大きいなら high、予想の方が小さいなら low を出力する', h1: 'まず等しいかどうかをifで判定するとシンプルになる', h2: '等しくなければelifで大小を比較する', ref: 'guess = int(input())\nanswer = int(input())\nif guess == answer:\n    print("correct")\nelif guess > answer:\n    print("high")\nelse:\n    print("low")', tip: '数当てゲームで「もっと大きい/もっと小さい」とヒントを出す仕組みそのもの。等しい場合を先に判定しておくと、あとの分岐がシンプルになる。', ins: ['5\n5', '10\n3', '3\n10', '0\n0', '-5\n-10', '-10\n-5'] })
t({ lv: 8, concept: 'while: 倍々ゲームで超えるまでの回数', title: '2倍を繰り返して超えるまで', tags: ['while ループ'], io: '1行目に開始値a(1以上の整数)、2行目に上限値bが与えられる', ask: 'aを2倍することを繰り返し、b以上になるまでに2倍した回数を出力する(最初からaがb以上ならば0を出力する)', h1: 'while a < b: の間だけ2倍してカウントを増やす', h2: '条件を満たさなくなったらループを抜けてカウントを出力する', ref: 'a = int(input())\nb = int(input())\ncount = 0\nwhile a < b:\n    a *= 2\n    count += 1\nprint(count)', tip: 'aが1以上である限り2倍を繰り返せば必ずb以上に到達するので、このループは必ず止まる。倍々に増えるものが目標を超えるまでの回数を数える場面はよくある。', ins: ['1\n1', '1\n2', '1\n100', '5\n40', '3\n3', '2\n1000000'] })
t({ lv: 11, concept: 'タプル: 2つのタプルを丸ごと交換', title: '中身をそっくり入れ替える', tags: ['タプルとアンパック'], io: '1行目にタプルaとなる2つの整数、2行目にタプルbとなる2つの整数が空白区切りで与えられる', ask: 'aとbをまとめてタプルにしてから中身をまるごと交換し、交換後のaの2つの値、交換後のbの2つの値をこの順に空白区切りで1行に出力する', h1: 'a = (a1, a2) のように2つの値を1つのタプルにまとめる', h2: 'a, b = b, a のように書けばタプルごと交換できる。出力は print(*a, *b) のように展開する', ref: 'a1, a2 = map(int, input().split())\nb1, b2 = map(int, input().split())\na = (a1, a2)\nb = (b1, b2)\na, b = b, a\nprint(*a, *b)', tip: 'タプルはひとかたまりの値として扱えるので、複数の値を一度に交換したいときに便利。中身が同じ値のタプル同士を交換しても結果は変わらない。', ins: ['1 2\n3 4', '0 0\n5 5', '-1 -2\n-3 -4', '7 7\n7 7', '100 200\n300 400', '9 -9\n-9 9'] })
t({ lv: 12, concept: '辞書: 値からキーを探す(逆引き)', title: '値からキーを逆引きする', tags: ['辞書'], io: '1行目に辞書の項目数n、続くn行に「キー 値」の形で整数のペア(値は重複しない)、最後の行に探したい値が与えられる', ask: '指定した値を持つキーを出力する。見つからない場合は none と出力する', h1: '読み込んだペアを辞書に入れておく', h2: 'd.items()で回して、値が一致した瞬間のキーを覚えればよい', ref: 'n = int(input())\nd = {}\nfor _ in range(n):\n    k, v = map(int, input().split())\n    d[k] = v\ntarget = int(input())\nfound = "none"\nfor k, v in d.items():\n    if v == target:\n        found = str(k)\n        break\nprint(found)', tip: '「キーから値を引く」の逆方向を探すには、辞書を回して値の側を比較する。値がユニークだと分かっているときによく使う考え方。', ins: ['1\n10 100\n100', '3\n1 5\n2 6\n3 7\n6', '3\n1 5\n2 6\n3 7\n999', '1\n0 0\n0', '4\n-1 -5\n-2 -6\n-3 -7\n-4 -8\n-8', '2\n5 1\n6 2\n1'] })
t({ lv: 14, concept: '幾何: 正方形の対角線', title: '対角線の長さ', tags: ['幾何'], io: '1行に正方形の1辺の長さ(整数)が与えられる', ask: '対角線の長さを小数第4位で四捨五入して出力する(対角線の長さは1辺×√2)', h1: 'math.sqrt(2)を使えば√2の値が手に入る', h2: 'round(値, 4)で小数第4位に丸める', ref: 'import math\na = int(input())\nd = a * math.sqrt(2)\nprint(round(d, 4))', tip: '正方形の対角線は1辺の√2倍というのは幾何でよく使う関係。対角線は正方形を2つの直角二等辺三角形に分けたときの斜辺にあたる。', ins: ['1', '2', '5', '10', '0', '100'] })
t({ lv: 15, concept: 'zip: 2つの列から辞書を作る', title: 'キーと値のペアにする', tags: ['enumerate と zip'], io: '1行目にキーの列(整数、重複なし)、2行目に同じ個数の値の列が空白区切りで与えられる', ask: 'zipで対応する要素をキーと値として辞書を作り、キーの昇順に「キー:値」の形で並べて空白区切りで1行に出力する', h1: 'dict(zip(keys, values))と書けば1行で辞書ができる', h2: 'sorted(d.items())でキーの昇順に並び替えてから文字列を組み立てる', ref: 'keys = list(map(int, input().split()))\nvalues = list(map(int, input().split()))\nd = dict(zip(keys, values))\nprint(\' \'.join(f"{k}:{v}" for k, v in sorted(d.items())))', tip: 'dict(zip(...))は2つの列を対応づけて辞書にする定番の書き方。キーの順番が入力と違っていても、辞書にしてから並べ替えれば整理できる。', ins: ['1 2 3\n10 20 30', '5 2 8\n50 20 80', '0\n99', '-1 -2\n100 200', '7\n7', '10 20 30 40\n1 2 3 4'] })
t({ lv: 16, concept: '幾何: 弧の長さから中心角を求める', title: '弧の長さから中心角へ', tags: ['幾何'], io: '1行目に半径r、2行目に弧の長さLが小数で与えられる(どちらも正の数)', ask: '中心角を度で求め、小数第2位で四捨五入して出力する(弧の長さ=2πr×角度/360 という関係を角度について解く)', h1: '円周は2πrで求まる。弧の長さは円周のうち角度分の割合になる', h2: '角度 = L×360/(2πr) という式に当てはめる', ref: 'import math\nr = float(input())\nl = float(input())\nangle = l * 360 / (2 * math.pi * r)\nprint(round(angle, 2))', tip: '扇形の弧の長さの公式(既出)を角度について逆に解いただけ。弧の長さから中心角を求める場面は、時計の針の動きなどにも応用できる。', ins: ['1\n3.14159265', '10\n10', '5\n7.85398163', '2\n1', '100\n1', '1\n0'] })
t({ lv: 17, concept: '幾何: 二等辺三角形の面積(底辺と等辺から)', title: '二等辺三角形の面積', tags: ['幾何'], io: '1行目に底辺の長さb、2行目に等しい2辺の長さaが与えられる', ask: '面積を小数第2位で四捨五入して出力する(底辺の真ん中で2つの直角三角形に分け、高さ=√(a^2-(b/2)^2)、面積=底辺×高さ/2)', h1: '二等辺三角形は頂点から底辺に垂線を下ろすと2つの合同な直角三角形に分かれる', h2: 'ピタゴラスの定理で高さを求めてから、底辺×高さ/2で面積を出す', ref: 'import math\nb = float(input())\na = float(input())\nh = math.sqrt(a * a - (b / 2) ** 2)\narea = b * h / 2\nprint(round(area, 2))', tip: '3辺から面積を求めるヘロンの公式(既出)と同じ答えになるが、こちらは高さを直接ピタゴラスの定理で求める考え方。底辺の半分と等辺が等しいと高さは0になり面積もつぶれる。', ins: ['6\n5', '2\n1', '10\n13', '1\n1', '8\n17', '100\n51'] })
t({ lv: 18, concept: '幾何: 台形の高さを逆算', title: '面積から台形の高さを求める', tags: ['幾何'], io: '1行目に上底a、2行目に下底b、3行目に台形の面積Sが与えられる', ask: '台形の高さhを小数第2位で四捨五入して出力する(台形の面積は(a+b)×h/2で求まる式をhについて解く)', h1: '台形の面積の公式(既出)を思い出し、hだけを左辺に残すように式を変形する', h2: 'h = 2×S/(a+b) という式に当てはめる', ref: 'a = float(input())\nb = float(input())\ns = float(input())\nh = 2 * s / (a + b)\nprint(round(h, 2))', tip: '台形の面積(既出)を求める公式を、今度は高さについて逆に解く練習。上底が0だと三角形になるが、式はそのまま使える。', ins: ['3\n5\n16', '1\n1\n1', '10\n20\n150', '0\n4\n8', '7\n3\n5', '2.5\n2.5\n12.5'] })
t({ lv: 19, concept: '幾何: 三角柱の表面積', title: '三角柱の紙の面積', tags: ['幾何'], io: '1行目に底面の三角形の3辺の長さ、2行目に三角柱の高さ(柱の長さ)が与えられる', ask: '表面積を小数第2位で四捨五入して出力する(底面積はヘロンの公式で求め、表面積=底面積×2+(3辺の和)×柱の高さ)', h1: '3辺から面積を求めるヘロンの公式(既出)の部品をそのまま使う', h2: '底面2枚分の面積に、側面3枚分(長方形)の面積を足す', ref: 'import math\na, b, c = map(float, input().split())\nh = float(input())\ns = (a + b + c) / 2\nbase_area = math.sqrt(s * (s - a) * (s - b) * (s - c))\nsurface = base_area * 2 + (a + b + c) * h\nprint(round(surface, 2))', tip: 'ヘロンの公式で底面積を求める部分(既出)の応用問題。柱の高さが0だと三角形が2枚重なっただけの面積になる。', ins: ['3 4 5\n10', '6 6 6\n1', '5 5 6\n2', '1 1 1\n1', '10 10 10\n0', '3 4 5\n0.5'] })
t({ lv: 20, concept: '関数定義: 関数の中で別の関数を呼ぶ(合成)', title: '関数を組み合わせて使う', tags: ['関数定義'], io: '1行に整数nが与えられる', ask: 'nを2倍する関数と、受け取った値に3を足す関数を別々に定義し、nを2倍してから3を足した結果を出力する', h1: 'def double(x): return x * 2 のように、それぞれの処理を別の関数に分ける', h2: '片方の関数の戻り値を、そのままもう片方の関数の引数として渡す', ref: 'def double(x):\n    return x * 2\n\ndef add_three(x):\n    return x + 3\n\nn = int(input())\nprint(add_three(double(n)))', tip: '小さな関数を組み合わせて大きな処理を作るのは関数の基本的な使い方。add_three(double(n))のように、関数の結果を別の関数にそのまま渡せる。', ins: ['0', '1', '5', '-4', '100', '-1'] })
t({ lv: 21, concept: '高階関数: reduce で累積計算', title: 'reduceで全部まとめて計算', tags: ['lambda と高階関数'], io: '1行に空白区切りの整数の列(1個以上)が与えられる', ask: 'functools.reduceを使って、すべての要素の積を求めて出力する', h1: 'from functools import reduce でreduceを使えるようにする', h2: 'reduce(lambda x, y: x * y, リスト) のように書くと、左から順に掛け合わせてくれる', ref: 'from functools import reduce\nnums = list(map(int, input().split()))\nprint(reduce(lambda x, y: x * y, nums))', tip: 'reduceはリストの要素を順番に2つずつ処理してひとつの値にまとめる高階関数。要素に0が含まれると結果は必ず0になる。', ins: ['1 2 3 4', '5', '0 5 10', '-1 2 -3', '1 1 1 1 1', '10 10 10'] })
t({ lv: 22, concept: '幾何: 三角柱の体積', title: '三角柱の体積', tags: ['幾何'], io: '1行目に底面の三角形の3辺の長さ、2行目に柱の高さが与えられる', ask: '体積を小数第2位で四捨五入して出力する(底面積はヘロンの公式で求め、体積=底面積×柱の高さ)', h1: '三角柱の表面積(既出)と同じく、まずヘロンの公式で底面積を求める', h2: '底面積に柱の高さを掛けるだけで体積になる', ref: 'import math\na, b, c = map(float, input().split())\nh = float(input())\ns = (a + b + c) / 2\nbase_area = math.sqrt(s * (s - a) * (s - b) * (s - c))\nvolume = base_area * h\nprint(round(volume, 2))', tip: '三角柱の表面積(既出)で使った底面積の求め方をそのまま使い、今度は高さを掛けて体積にする。柱の高さが0なら体積も0になる。', ins: ['3 4 5\n10', '6 6 6\n1', '5 5 6\n2', '1 1 1\n1', '10 10 10\n0', '3 4 5\n0.5'] })
t({ lv: 23, concept: '文章題: 速さの単位変換', title: 'km/h を m/s に直す', tags: ['文章題'], io: '1行に時速(km/h)を表す数値が与えられる', ask: '秒速(m/s)に変換し、小数第3位で四捨五入して出力する(1km/h=1000/3600 m/s)', h1: 'kmをmに直すと1000倍、時間を秒に直すと3600倍になる', h2: '時速×1000/3600という式で秒速が求まる', ref: 'kmh = float(input())\nmps = kmh * 1000 / 3600\nprint(round(mps, 3))', tip: '速さの単位変換は距離をm、時間を秒にそろえてから割るだけ。時速36kmがちょうど秒速10mになることは覚えておくと便利。', ins: ['0', '36', '100', '3.6', '1', '72'] })
t({ lv: 24, concept: '文章題: 複数区間の平均速度', title: '区間ごとの距離と時間から平均を出す', tags: ['文章題'], io: '1行目に区間の数n、続くn行に「距離 時間」が空白区切りで与えられる', ask: 'すべての区間の距離の合計を、すべての区間の時間の合計で割った平均速度を小数第2位で四捨五入して出力する', h1: '距離の合計と時間の合計をそれぞれ別の変数に集計していく', h2: '区間ごとの速さを平均するのではなく、合計同士を割ることに注意する', ref: 'n = int(input())\ntotal_d = 0.0\ntotal_t = 0.0\nfor _ in range(n):\n    d, t = map(float, input().split())\n    total_d += d\n    total_t += t\nprint(round(total_d / total_t, 2))', tip: '区間ごとの速さの単純平均と、距離の合計÷時間の合計は一般に一致しない。旅行全体の平均速度を求めるときは後者が正しい。', ins: ['1\n60 1', '2\n100 2\n50 1', '3\n10 1\n20 1\n30 1', '1\n1 2', '2\n0 5\n0 5', '4\n100 1\n100 1\n100 1\n100 1'] })
t({ lv: 26, concept: '文章題: 誤差の百分率', title: '予想と実際のずれは何パーセント', tags: ['文章題'], io: '1行目に予想値、2行目に実際の値(0でない)が与えられる', ask: '誤差率を |予想-実際|/実際×100 で計算し、小数第2位で四捨五入して%記号なしで出力する', h1: 'abs()を使えば予想と実際の差の絶対値が求まる', h2: '実際の値で割ってから100を掛けるとパーセントになる', ref: 'predicted = float(input())\nactual = float(input())\nerror = abs(predicted - actual) / actual * 100\nprint(round(error, 2))', tip: '誤差率は実際の値を基準にして測るのが普通。予想が実際より大きくても小さくても、絶対値をとるので同じ式で計算できる。', ins: ['110\n100', '80\n100', '100\n100', '-5\n5', '50\n3', '1\n1000000'] })
t({ lv: 27, concept: '文章題: 購入額に応じた割引率', title: '買う額が多いほど安くなる', tags: ['文章題'], io: '1行に合計購入金額(整数)が与えられる', ask: '5000円以上なら20%引き、3000円以上5000円未満なら10%引き、それ未満なら割引なしとして、支払う金額を整数で出力する(小数点以下切り捨て)', h1: 'if/elifを使って金額の範囲を大きい方から順に判定する', h2: '割引後の金額をint()で整数に切り捨ててから出力する', ref: 'price = int(input())\nif price >= 5000:\n    pay = price * 0.8\nelif price >= 3000:\n    pay = price * 0.9\nelse:\n    pay = price\nprint(int(pay))', tip: '「〜円以上で割引」のような段階的な割引はセールなどでよく見かける。境界のちょうど3000円や5000円がどちらの区分に入るか、条件式の書き方に注意する。', ins: ['0', '2999', '3000', '4999', '5000', '10001'] })
t({ lv: 28, concept: '文章題: 二種類の税率の合計', title: '品目ごとに税率が違うときの合計', tags: ['文章題'], io: '1行目に軽減税率8%対象の金額の合計、2行目に標準税率10%対象の金額の合計が与えられる', ask: 'それぞれに税率を掛けた税込み金額を合計し、整数で出力する(小数点以下切り捨て)', h1: '8%対象の金額と10%対象の金額は別々に税込み額を計算する', h2: '2つの税込み額を足してからint()で切り捨てる', ref: 'a = float(input())\nb = float(input())\ntotal = a * 1.08 + b * 1.10\nprint(int(total))', tip: '軽減税率のように税率が複数あるレシートでは、税率ごとにグループ分けしてから税込み額を計算する。片方が0円のときはもう片方の税率だけがかかる。', ins: ['1000\n1000', '0\n1000', '1000\n0', '0\n0', '500\n2500', '12345\n6789'] })
t({ lv: 29, concept: '文章題: 基本料金と従量料金', title: '基本料金+使えば使うほど', tags: ['文章題'], io: '1行目に基本料金、2行目に1単位あたりの従量単価、3行目に使用量が整数で与えられる', ask: '支払う合計金額を「基本料金+従量単価×使用量」で計算して出力する', h1: '3つの入力をそれぞれ受け取ってから、掛け算と足し算を組み立てる', h2: '使用量が0でも基本料金だけは必ずかかることに注意する', ref: 'base = int(input())\nrate = int(input())\nusage = int(input())\nprint(base + rate * usage)', tip: '電気代や水道代のように「基本料金+使った分だけ加算」という料金体系は日常のいたるところにある。使用量が0のときは基本料金だけが残る。', ins: ['500\n10\n0', '500\n10\n100', '0\n5\n20', '1000\n0\n50', '300\n2\n1', '2000\n15\n300'] })
t({ lv: 31, concept: '文章題: 単利の残高推移', title: '年ごとの残高を並べる', tags: ['文章題'], io: '元金P、年利率r(%)、年数nが空白区切りで1行', ask: '各年末の残高（単利、円未満切り捨て）を1年目からn年目まで1行ずつ出力する', h1: '単利では毎年の利息はいつも元金P×r/100の一定額だけ増える', h2: '浮動小数点の丸め誤差を避けるため、切り捨て前に小さな値を足しておく', ref: 'import math\n\ndef solve():\n    P, r, n = input().split()\n    P = int(P); r = float(r); n = int(n)\n    for y in range(1, n+1):\n        bal = P + P*r/100*y\n        print(math.floor(bal + 1e-9))\n\nsolve()', tip: '単利の利息(既出)を1年ごとに繰り返すと、残高は等差数列(既出)になる。複利との違いは、利息が元金にしか付かない点。', ins: ['1000 5 3', '500 10 4', '1 100 1', '2000 2.5 2', '100000 1 5', '10 0 2'] })
t({ lv: 32, concept: '文字列: ある文字の出現位置をすべて列挙', title: '指定した文字が何文字目にあるか全部', tags: ['文字列'], io: '1行目に文字列s、2行目に探す文字c(1文字)', ask: 'sの中でcが現れる位置(0始まりの添字)をすべて、小さい順にスペース区切りで1行に出力する。1つも無ければ空行を出力する', h1: 'enumerate(s)で位置と文字を同時に取り出せる', h2: '見つからない場合は空リストのままjoinすればよい(空行になる)', ref: 'def solve():\n    s = input()\n    c = input()\n    idx = [str(i) for i, ch in enumerate(s) if ch == c]\n    print(\' \'.join(idx))\n\nsolve()', tip: '文字の出現回数(既出)を数えるだけでなく、位置まで記録すると文字列探索の基本になる。indexで1つずつ探すより enumerate の方が安全。', ins: ['banana\na', 'hello\nz', 'a\na', 'aaaaa\na', 'Mississippi\ns', 'banana\nn'] })
t({ lv: 33, concept: '時計: 経過秒から時刻を復元', title: '秒数を時分秒に戻す', tags: ['時計'], io: '深夜0時からの経過秒数sが1つ(0以上86399以下)', ask: 'sを24時間表記のHH:MM:SS形式(各2桁ゼロ埋め)に変換して出力する', h1: '時は s//3600、残りは s%3600 のうち、分は残り//60、秒は残り%60 で求まる', h2: 'f文字列の{h:02d}のような書き方で2桁ゼロ埋めできる', ref: 'def solve():\n    s = int(input())\n    h = s // 3600\n    m = (s % 3600) // 60\n    sec = s % 60\n    print(f"{h:02d}:{m:02d}:{sec:02d}")\n\nsolve()', tip: '深夜0時からの経過秒数(既出)の逆変換。//と%を組み合わせれば秒だけの値からいつでも時刻に戻せる。', ins: ['0', '3661', '86399', '59', '3600', '43200'] })
t({ lv: 34, concept: '基礎: 3進数から10進数へ変換', title: '3進表記を10進数に戻す', tags: ['基礎'], io: '3進数として書かれた文字列(0,1,2のみ)が1行', ask: 'その値を10進整数に変換して出力する', h1: '右端から桁ごとに3のべき乗を掛けて足していく', h2: 'int(s, 3) を使うと一気に変換できる', ref: 'def solve():\n    s = input().strip()\n    print(int(s, 3))\n\nsolve()', tip: '3進数へ変換(既出)の逆変換。int(s, 3)のように組み込み関数一発でも変換できるが、割り算と余りで手作業しても同じ結果になる。', ins: ['0', '1', '2', '10', '21', '222'] })
t({ lv: 35, concept: '時計: 終電までの余裕分', title: '間に合うか、何分の差か', tags: ['時計'], io: '現在時刻HH:MM、終電時刻HH:MM、移動にかかる分数mが1行に空白区切り', ask: '現在時刻からm分後に終電時刻に対して何分の余裕があるか(間に合えば正、間に合わなければ負、ちょうどなら0)を整数で出力する。日付をまたぐ場合は終電は翌日とみなす', h1: '分に直してから引き算するとよい(24*60を1日とする)', h2: '終電時刻の方が現在時刻より前なら日をまたいでいるとみなし、終電の分に24*60を足す', ref: 'def solve():\n    cur, end, m = input().split()\n    ch, cm = map(int, cur.split(\':\'))\n    eh, em = map(int, end.split(\':\'))\n    cur_min = ch*60+cm\n    end_min = eh*60+em\n    if end_min < cur_min:\n        end_min += 24*60\n    arrive = cur_min + int(m)\n    print(end_min - arrive)\n\nsolve()', tip: '終電までに帰れるか(既出)を一歩進めて、余裕が何分あるかまで求める。差が負なら間に合わない、という判定にそのままつながる。', ins: ['23:50 23:55 3', '23:50 00:10 15', '12:00 12:00 0', '23:59 00:00 2', '00:00 23:59 0', '08:00 08:30 45'] })
t({ lv: 36, concept: '整数関係: 約数の列挙', title: 'nの約数を小さい順に全部出す', tags: ['整数関係'], io: '正整数nが1行', ask: 'nの約数を小さい順にすべて空白区切りで1行に出力する', h1: '1からnまで調べてn%iが0か確かめれば約数が見つかる', h2: 'この規模ならsqrt(n)まで絞らず、そのまま全部調べても十分速い', ref: 'def solve():\n    n = int(input())\n    divs = [str(i) for i in range(1, n+1) if n % i == 0]\n    print(\' \'.join(divs))\n\nsolve()', tip: 'aはbの約数か(既出)を全てのiについて繰り返せば、約数の一覧が手に入る。素数はこの列挙結果がちょうど2個になる数。', ins: ['1', '12', '7', '36', '100', '2'] })
t({ lv: 37, concept: '整数関係: 倍数の列挙', title: '1からnまでのkの倍数を全部出す', tags: ['整数関係'], io: '整数nとkが1行に空白区切り', ask: '1以上n以下のkの倍数を小さい順にすべて空白区切りで1行に出力する(無ければ空行)', h1: 'range(k, n+1, k)を使うと効率よく倍数だけを取り出せる', h2: '無ければ空リストのままjoinすれば空行になる', ref: 'def solve():\n    n, k = map(int, input().split())\n    res = [str(i) for i in range(k, n+1, k)]\n    print(\' \'.join(res))\n\nsolve()', tip: 'aはbの倍数か(既出)をrangeで一気に判定すると、倍数の一覧が手に入る。間隔kごとに増える数列とも言える。', ins: ['10 3', '1 5', '20 4', '5 1', '7 7', '15 6'] })
t({ lv: 38, concept: '数列: 2のべき乗の累積和の列', title: '倍々ゲームの積み立て表', tags: ['数列'], io: '整数nが1行(1以上)', ask: '2^1から2^nまでを1つずつ足していったときの、各時点までの累積和をn個、空白区切りで1行に出力する', h1: '2の累乗は前の値を2倍すれば求まる', h2: '累積和は一つ前の累積和に新しい項を足していけばよい', ref: 'def solve():\n    n = int(input())\n    total = 0\n    p = 1\n    res = []\n    for i in range(n):\n        p *= 2\n        total += p\n        res.append(str(total))\n    print(\' \'.join(res))\n\nsolve()', tip: '2のべき乗を足していく(既出)の最終値だけでなく、各ステップの累積和も並べて出す。累積和は多くのアルゴリズムの基本部品。', ins: ['1', '3', '5', '2', '4', '6'] })
t({ lv: 39, concept: '数列: 奇数の和から項数を逆算', title: 'その合計は何番目までの奇数の和か', tags: ['数列'], io: '整数Sが1行', ask: 'Sが1からn番目までの奇数の和(1+3+5+...)としてちょうど作れるなら、そのnを出力する。作れなければ -1 を出力する', h1: '1からn番目までの奇数の和はn^2になることを使う(既出の性質)', h2: 'Sの平方根を整数に丸めて2乗し直し、Sと一致するか確かめる', ref: 'import math\n\ndef solve():\n    S = int(input())\n    if S < 0:\n        print(-1)\n        return\n    n = int(math.isqrt(S))\n    if n*n == S and n > 0:\n        print(n)\n    else:\n        print(-1)\n\nsolve()', tip: '奇数の和(既出)がn^2になる性質を利用した逆算。isqrtで平方根の整数部分を求め、2乗して元に戻るか確認するのが定石。', ins: ['9', '16', '10', '1', '0', '100'] })
t({ lv: 40, concept: '例外処理: raiseで例外を送出する', title: '不正な値ならエラーを投げる', tags: ['例外処理'], io: '整数nが1行', ask: 'nが正の整数でなければ ValueError を発生させて invalid と出力し、正の整数ならその2乗を出力する。try/exceptとraiseを使うこと', h1: 'if n <= 0: raise ValueError(...) のように自分で例外を投げられる', h2: '投げた例外はtryブロックの外側のexceptで受け止めて処理する', ref: 'def solve():\n    s = input()\n    try:\n        n = int(s)\n        if n <= 0:\n            raise ValueError("not positive")\n        print(n*n)\n    except ValueError:\n        print("invalid")\n\nsolve()', tip: '例外はexceptで受け止めるだけでなく、raiseで自分から投げることもできる。入力値の検証(バリデーション)でよく使うテクニック。', ins: ['5', '-3', '0', 'abc', '1', '100'] })
t({ lv: 41, concept: '数: 各桁の7セグメント本数を列挙', title: '複数桁の数字、各桁の棒の本数', tags: ['数'], io: '数字の文字列(桁の並び)が1行', ask: '各桁について7セグメント表示に必要な棒の本数を、桁の順番に空白区切りで1行に出力する', h1: '0から9までの本数表(既出の値)を辞書に用意し、文字列の各文字で引く', h2: 'for文で1文字ずつ処理すればよい', ref: 'def solve():\n    s = input().strip()\n    bars = {\'0\':6,\'1\':2,\'2\':5,\'3\':5,\'4\':4,\'5\':5,\'6\':6,\'7\':3,\'8\':7,\'9\':6}\n    res = [str(bars[ch]) for ch in s]\n    print(\' \'.join(res))\n\nsolve()', tip: '7セグメントの棒の本数(既出)を1桁ずつ調べて並べると、複数桁の数字にも対応できる。', ins: ['0', '1234', '8888', '1', '9081726354', '70'] })
t({ lv: 42, concept: '整数: 桁のすべての回転を列挙', title: '桁をぐるぐる回して全部のパターン', tags: ['整数'], io: '正整数nが1行(桁数をkとする)', ask: 'nの各桁を1つずつ左に回転させ、iを0からk-1まで動かしたときの各結果を先頭に0が来てもそのまま整数として、順にすべて空白区切りで1行に出力する', h1: '文字列としてs[i:]+s[:i]の形でi回転したものが作れる', h2: '先頭の0はint()に変換すれば自動的に消える', ref: 'def solve():\n    n = input().strip()\n    k = len(n)\n    res = []\n    for i in range(k):\n        rotated = n[i:] + n[:i]\n        res.append(str(int(rotated)))\n    print(\' \'.join(res))\n\nsolve()', tip: '桁を左に1回転(既出)を回転回数を変えながら繰り返すと、全ての回転パターンが得られる。', ins: ['105', '7', '1000', '12', '100', '9999'] })
t({ lv: 43, concept: '整数関係: 範囲内の整数をすべて列挙', title: 'aからbまでの整数を全部出す', tags: ['整数関係'], io: '整数aとbが1行に空白区切り(a<=bとは限らない)', ask: 'aからbまでの整数(小さい方から大きい方まで)を小さい順にすべて空白区切りで1行に出力する', h1: 'aとbの大小関係が分からないので、min/maxで小さい方と大きい方を決める', h2: 'range(小さい方, 大きい方+1)で一気に作れる', ref: 'def solve():\n    a, b = map(int, input().split())\n    lo, hi = min(a,b), max(a,b)\n    print(\' \'.join(str(i) for i in range(lo, hi+1)))\n\nsolve()', tip: '範囲内の整数の個数(既出)は「実際に列挙してから数える」のと同じ結果になる。個数だけでなく中身も出せるようにしておくと応用が利く。', ins: ['3 7', '5 5', '10 1', '0 -3', '1 1', '-2 2'] })
t({ lv: 44, concept: '整数: 直前と直後のkの倍数を両方出す', title: 'すぐ下とすぐ上の倍数を両方出す', tags: ['整数'], io: '整数nとkが1行に空白区切り(nはkの倍数とは限らない)', ask: 'n以下で最大のkの倍数と、n以上で最小のkの倍数を、この順に空白区切りで1行に出力する(nがkの倍数ならその値が両方になる)', h1: '一番近いkの倍数(既出)を求める過程で、下側の候補と上側の候補の両方を計算している', h2: '下側は(n//k)*k、上側は((n+k-1)//k)*k で求まる', ref: 'def solve():\n    n, k = map(int, input().split())\n    lower = (n // k) * k\n    upper = ((n + k - 1) // k) * k\n    print(lower, upper)\n\nsolve()', tip: '一番近いkの倍数(既出)を求めるときに使っていた下側・上側の候補を、そのまま両方見せるようにした。', ins: ['10 3', '9 3', '1 5', '100 7', '7 7', '0 4'] })
t({ lv: 46, concept: '数列: 等差数列でL以下の項をすべて列挙', title: 'Lまでに入る項を全部書き出す', tags: ['数列'], io: '初項a、公差d(正)、上限Lが1行に空白区切り', ask: '初項aから始めて公差dずつ増える等差数列のうち、L以下である項をすべて小さい順に空白区切りで1行に出力する(1つもなければ空行)', h1: '現在の項がL以下である間、dずつ足して集めていく', h2: 'd>0であることを確認し、L以下でなくなったら止めれば無限ループにならない', ref: 'def solve():\n    a, d, L = map(int, input().split())\n    res = []\n    x = a\n    while x <= L:\n        res.append(str(x))\n        x += d\n    print(\' \'.join(res))\n\nsolve()', tip: '等差数列がL以下に何項あるか(既出)は、この列挙結果の個数と同じ。個数だけでなく中身も出せるようにしておくと応用範囲が広い。', ins: ['1 2 10', '5 5 5', '3 1 3', '10 3 5', '1 1 1', '2 4 20'] })
t({ lv: 47, concept: '文章題: 区間ごとの速度を列挙', title: '区間ごとの時速を全部出す', tags: ['文章題'], io: '1行目に区間数n、2〜n+1行目に各区間の距離(km)と時間(時間)が空白区切りで1行ずつ', ask: '各区間の速度(距離÷時間、小数点以下切り捨て)をn個、区間の順に1行ずつ出力する', h1: '速度は距離÷時間で求まる(既出の平均速度と同じ考え方を区間ごとに)', h2: '浮動小数点の丸め誤差を避けるため、切り捨て前に小さな値を足しておく', ref: 'import math\n\ndef solve():\n    n = int(input())\n    for _ in range(n):\n        d, t = map(float, input().split())\n        speed = d / t\n        print(math.floor(speed + 1e-9))\n\nsolve()', tip: '2区間の平均速度(既出)は全体の距離÷全体の時間。区間ごとの速度を出せば、どこが速かったかも分かる。', ins: ['2\n100 2\n60 1.5', '1\n45 1', '3\n10 3\n20 4\n5 2', '1\n0 1', '2\n7 2\n9 3', '4\n1 1\n2 1\n3 1\n4 1'] })
t({ lv: 48, concept: '幾何: 直方体の対角線の長さ', title: '箱の対角線は実際何cm', tags: ['幾何'], io: '直方体の3辺の長さa,b,cが1行に空白区切り(整数)', ask: '対角線の長さ(sqrt(a^2+b^2+c^2))を小数点以下切り捨てた整数で出力する', h1: '対角線の2乗(既出)を求めてから平方根を取ればよい', h2: 'math.sqrtを使い、誤差対策に小さい値を足してからint()で切り捨てる', ref: 'import math\n\ndef solve():\n    a, b, c = map(int, input().split())\n    val = a*a + b*b + c*c\n    print(int(math.sqrt(val) + 1e-9))\n\nsolve()', tip: '直方体の対角線の2乗(既出)に平方根を取れば実際の長さが分かる。三平方の定理の3次元版。', ins: ['3 4 12', '1 1 1', '2 3 6', '5 5 5', '1 2 2', '10 10 10'] })
t({ lv: 49, concept: '基礎: n以下の2のべき乗をすべて列挙', title: '2のべき乗をn以下で全部出す', tags: ['基礎'], io: '正整数nが1行', ask: '1以上n以下の2のべき乗(1,2,4,8,…)を小さい順にすべて空白区切りで1行に出力する', h1: '2のべき乗判定(既出)ではビット演算やlogを使ったが、ここでは1から2倍ずつ増やして集めればよい', h2: 'n以下である間だけ集め、超えたら止める', ref: 'def solve():\n    n = int(input())\n    res = []\n    p = 1\n    while p <= n:\n        res.append(str(p))\n        p *= 2\n    print(\' \'.join(res))\n\nsolve()', tip: '2のべき乗判定(既出)を積み重ねてp*=2を繰り返せば、n以下の2のべき乗が全部そろう。', ins: ['1', '2', '4', '10', '100', '1000'] })
t({ lv: 51, concept: '文字列: 埋め込まれた数字をすべて抽出', title: '文の中の数字を全部拾い出す', tags: ['文字列'], io: '数字と英字が混ざった文字列が1行', ask: '文字列中に現れる数字のかたまり(連続する数字)をすべて、出てきた順に空白区切りで1行に出力する(整数として、先頭の0は消えてよい)', h1: '1文字ずつ見て、数字が続く間はためておき、数字でなくなったら確定させる', h2: 'たまった数字文字列をint()に通せば先頭の0は自然に消える', ref: 'def solve():\n    s = input()\n    res = []\n    cur = \'\'\n    for ch in s:\n        if ch.isdigit():\n            cur += ch\n        else:\n            if cur:\n                res.append(str(int(cur)))\n                cur = \'\'\n    if cur:\n        res.append(str(int(cur)))\n    print(\' \'.join(res))\n\nsolve()', tip: '文の中の一番大きな数(既出)を求める過程では全部の数字を一度拾い出している。今回はその拾い出した結果をそのまま出す。', ins: ['abc123def45', '007 up', 'no numbers here', '1a2b3', '5', '100 200 300'] })
t({ lv: 52, concept: '文字列: 交互パターンが崩れる最初の位置', title: '最初にリズムが乱れる場所はどこ', tags: ['文字列'], io: '英字の文字列が1行', ask: '大文字/小文字が交互になっている理想のパターンに対して、最初に交互でなくなる文字の位置(0始まり)を出力する。最後まで交互なら -1 を出力する', h1: '1文字目の大小を基準にして、次以降は逆になっているはずと期待しながら進める', h2: '期待と違ったらそのインデックスがすなわち答え', ref: 'def solve():\n    s = input()\n    if len(s) <= 1:\n        print(-1)\n        return\n    for i in range(1, len(s)):\n        prev_upper = s[i-1].isupper()\n        cur_upper = s[i].isupper()\n        if prev_upper == cur_upper:\n            print(i)\n            return\n    print(-1)\n\nsolve()', tip: '大小交互になっているか(既出)の判定をそのまま使い、崩れた瞬間のインデックスまで記録すると「どこが違うか」を教えられる。', ins: ['AbAbAb', 'AbbAbA', 'aAaa', 'ABC', 'z', 'AbAbb'] })
t({ lv: 53, concept: '基礎: 交互に符号をつけた値の列', title: '符号を交互につけた列を全部出す', tags: ['基礎'], io: '1行目に要素数n、2行目にn個の整数(空白区切り)', ask: '1番目はそのまま、2番目は符号反転、3番目はそのまま…と交互に符号をつけた列を、順番通り空白区切りで1行に出力する(先頭の符号はプラス)', h1: '偶数番目(0始まりで偶数index)はそのまま、奇数番目は-1を掛ける', h2: '交互符号の和(既出)は、この列を全部足したものに等しい', ref: 'def solve():\n    n = int(input())\n    a = list(map(int, input().split()))\n    res = []\n    for i, v in enumerate(a):\n        res.append(str(v if i % 2 == 0 else -v))\n    print(\' \'.join(res))\n\nsolve()', tip: '交互符号の和(既出)は、この列をすべて足し合わせた結果に一致する。値だけでなく途中の符号列も見えるようにした。', ins: ['3\n1 2 3', '1\n5', '4\n2 2 2 2', '5\n1 1 1 1 1', '2\n-3 4', '6\n1 2 3 4 5 6'] })
t({ lv: 54, concept: '文字列: 最少出現回数の文字をすべて列挙', title: '一番レアな文字を全部あげる', tags: ['文字列'], io: '英小文字の文字列が1行', ask: '出現回数が最小の文字を、その回数が同じ複数の文字にわたる場合は辞書順にすべて列挙し、空白区切りで1行に出力する', h1: 'Counterなどで各文字の出現回数を数える', h2: '最小回数を求めたら、その回数と一致する文字を辞書順に集める', ref: 'from collections import Counter\n\ndef solve():\n    s = input()\n    c = Counter(s)\n    mn = min(c.values())\n    res = sorted(ch for ch, cnt in c.items() if cnt == mn)\n    print(\' \'.join(res))\n\nsolve()', tip: '最も少ない文字(既出)は1つだけ選んでいたが、同じ回数の文字が複数あるときは全部見せてあげると公平。', ins: ['aabbbc', 'aabbcc', 'a', 'bbaaa', 'xyzxyzx', 'qwertyuiop'] })
t({ lv: 55, concept: '文字列: leet表記を元の文字に戻す', title: 'ハッカー風の文字を元に戻す', tags: ['文字列'], io: '数字と記号混じりの文字列が1行(leet表記、対応: 0->o,1->i,3->e,4->a,5->s,7->t、それ以外はそのまま)', ask: 'leet表記の文字を対応する元のアルファベットに戻して出力する(対応外の文字はそのまま)', h1: 'leet変換(既出)の対応表を逆向きに引けばよい', h2: '辞書に対応表を用意し、1文字ずつ変換する', ref: 'def solve():\n    s = input()\n    table = {\'0\':\'o\',\'1\':\'i\',\'3\':\'e\',\'4\':\'a\',\'5\':\'s\',\'7\':\'t\'}\n    res = \'\'.join(table.get(ch, ch) for ch in s)\n    print(res)\n\nsolve()', tip: 'leet文字に変換(既出)の対応表をひっくり返せば元の文字列に戻せる。暗号化と復号はいつも表裏の関係。', ins: ['h3ll0', 'l33t', 'b4n4n4', 'c0d3', '5n4k3', 'xyz'] })
t({ lv: 56, concept: '文字列: 最長の連続数字列そのもの', title: '一番長い数字のかたまりを取り出す', tags: ['文字列'], io: '英数字混在の文字列が1行', ask: '連続する数字が最も長く続く部分をそのまま文字列として出力する(同じ長さが複数あれば最初に出てくる方)', h1: '最長の連続数字列の長さ(既出)を求める過程で、区間の開始と終わりも記録しておく', h2: '現在の数字区間の長さが今までの最大を超えたときだけ更新する(同じ長さでは更新しない、つまり先に見つかった方を優先する)', ref: 'def solve():\n    s = input()\n    best = \'\'\n    cur = \'\'\n    for ch in s:\n        if ch.isdigit():\n            cur += ch\n            if len(cur) > len(best):\n                best = cur\n        else:\n            cur = \'\'\n    print(best)\n\nsolve()', tip: '最長の連続数字列の長さ(既出)を求めながら、その部分文字列自体も覚えておけば長さだけでなく中身も分かる。', ins: ['abc12345xy67', '111 22', 'no digits', '1', '22 33 44', '9'] })
t({ lv: 57, concept: '文字列: 文字を降順に並べた文字列', title: '文字を大きい順に並べ替える', tags: ['文字列'], io: '英字の文字列が1行', ask: '文字を(コード順で)大きい方から小さい方へ並べ替えた文字列を出力する', h1: '文字をソート(既出)の逆順を使えばよい', h2: 'sorted(s, reverse=True)を使うか、昇順ソートしてから反転する', ref: 'def solve():\n    s = input()\n    print(\'\'.join(sorted(s, reverse=True)))\n\nsolve()', tip: '文字をソート(既出)を逆順にするだけで、大きい順の並べ替えができる。reverse=Trueは多くの場面で使える。', ins: ['banana', 'z', 'hello', 'aaa', 'ZebrA', 'abcdef'] })
t({ lv: 58, concept: 'リスト: 1つ消して昇順にする、その位置', title: 'どれを1人抜かせば整列するか', tags: ['リスト'], io: '1行目に要素数n、2行目にn個の整数(空白区切り)', ask: 'ちょうど1つの要素を取り除けば狭義単調増加になる場合、取り除くべき要素の位置(0始まり)を出力する。既に単調増加なら -1 を出力し、どうやっても無理なら -2 を出力する(複数の位置で可能な場合は最も小さい位置)', h1: '1つ消して昇順にできるか(既出)の判定を、実際にどの位置を消すか全探索して確かめる', h2: '各位置iについて、その要素を除いたリストが狭義単調増加かをチェックする', ref: 'def is_strict_incr(a):\n    return all(a[i] < a[i+1] for i in range(len(a)-1))\n\ndef solve():\n    n = int(input())\n    a = list(map(int, input().split()))\n    if is_strict_incr(a):\n        print(-1)\n        return\n    for i in range(n):\n        b = a[:i] + a[i+1:]\n        if is_strict_incr(b):\n            print(i)\n            return\n    print(-2)\n\nsolve()', tip: '1つ消して昇順にできるか(既出)の判定に加えて、実際にどこを消すかを全探索で確かめる。答えが複数ある場合は最初に見つかった位置を選ぶ。', ins: ['5\n1 2 10 3 4', '3\n1 2 3', '1\n5', '3\n5 1 2', '4\n1 5 2 3', '3\n3 3 3'] })
t({ lv: 59, concept: '文字列: 頭と尻が同じ単語をすべて列挙', title: '始まりと終わりが同じ単語を全部あげる', tags: ['文字列'], io: '単語がスペース区切りで並んだ文が1行', ask: '最初の文字と最後の文字が同じ単語(1文字の単語も対象)を、出てきた順にスペース区切りで1行に出力する(1つもなければ空行)', h1: '頭と尻が同じ単語の数(既出)の判定条件をそのまま使い、該当する単語を集める', h2: 'word[0] == word[-1] で判定できる(1文字の単語は常にTrue)', ref: 'def solve():\n    s = input()\n    words = s.split()\n    res = [w for w in words if w[0] == w[-1]]\n    print(\' \'.join(res))\n\nsolve()', tip: '頭と尻が同じ単語の数(既出)を数えるだけでなく、実際にどの単語かも見えるようにした。', ins: ['level noon test data', 'cat dog bird', 'a', 'aha oho uhu', 'stats racecar python', 'xyz zzz abcba'] })
t({ lv: 60, concept: '頻度: 出現回数が最大の単語をすべて列挙', title: '一番人気の単語を同点も含めて全部', tags: ['頻度'], io: '単語がスペース区切りで並んだ文が1行', ask: '出現回数が最大の単語を、複数あれば辞書順にすべて空白区切りで1行に出力する', h1: 'Counter(既出)で各単語の出現回数を数える', h2: '最大回数を求めたら、その回数と一致する単語を辞書順に集める(most_commonだけでは同点を取りこぼす)', ref: 'from collections import Counter\n\ndef solve():\n    s = input()\n    words = s.split()\n    c = Counter(words)\n    mx = max(c.values())\n    res = sorted(w for w, cnt in c.items() if cnt == mx)\n    print(\' \'.join(res))\n\nsolve()', tip: '最頻単語(既出)はmost_common(1)で1つだけ取れるが、同点があると取りこぼす。最大回数を求めてから同じ回数の単語を集める方が安全。', ins: ['the cat sat on the mat', 'a a b b c', 'x', 'dog cat dog cat bird', 'one two three', 'z z z y y x'] })

export const CATALOG = TASKS;

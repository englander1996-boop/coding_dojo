import type { Problem } from '../../types'

/**
 * （梯子の水増しスキャフォールドは撤去済み。空配列。）
 * 全レベル充足は「水増しなしの別計算オリジナル問題」を各段に置く方針で達成する。
 * scripts/ladder-gen.ts が、難易度に合う多数の「別計算」問題を各レベルへ1つずつ割り当てて生成する。
 */
export const ladderProblems: Problem[] = []

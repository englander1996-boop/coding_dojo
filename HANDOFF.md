# 引き継ぎ書 — Coding Dojo

最終更新: 2026-09-04 / 場所: `Y:\coding_dojo`

このドキュメントだけで開発を引き継げるようにまとめてあります。詳細仕様は `README.md`、**問題を作るときのルールと拡張方針は `AUTHORING.md`**、**いまどこまで進んだかと並列作業のノウハウは `PROGRESS.md`** を参照。

## 作業の前提（ユーザーの常設指示 / 2026-09-04）

作業 PC をまたいでも失わないよう、ここに書き出しておく。

1. **問題作成を引き継ぐこと。** 途中で問い合わせず、バッチを出し続ける。
2. **ネタは Web 検索で新しく発掘する。** ただし**検索の前に既出の主題を棚卸しする**（`py scripts/inventory.py <lo> <hi> <out.txt>`）。取得したネタは必ずオリジナル化し、学習者に見える文面にプラットフォーム名を出さない。
3. **目標は全レベル 10 問以上。** 幅優先で進める（4問化 → 5問化 → …、`node scripts/check-thin.ts N`）。
4. **脈絡のツールを新規に作り込まない。** ユーザーは成果物（新しい問題）を見ている。衛生修正はバッチの流れの中でだけ行う。

### 1バッチの手順

```
py scripts/inventory.py <lo> <hi> out.txt   # 既出の棚卸し（PROBLEMS.md を読む）
# → 設計（値・可否で止まっている既出を「実体・過程・逆変換」に上げる）
# → concept 断片で grep して重複を潰す
# → scripts/catalog-tasks{N}.mjs を書く
# → generate.mjs に import と spread の 2 行を追加
node scripts/generate.mjs
npx tsx scripts/validate.ts && npx tsx scripts/prereq.ts && npx tsx scripts/coverage.ts
npx tsx scripts/check-thin.ts 5 && npx tsc -b && npm run build && npx tsx scripts/catalog.ts
py scripts/handoff-append.py <旧問数> <旧ケース> <旧残> <旧次> body.txt <新問数> <新ケース> <新残> <新次>
```

次バッチ（C124 / lv529〜578）の設計一覧は `scripts/idea-bank.md` の末尾にある。
近似重複の検出結果は `scripts/suspect-duplicates.md`。

---

## 0. 最優先タスク（各レベル2問以上）★進行中 ★ここから読む

**✅ 第1段階達成: 全999段に最低1問（空き段ゼロ）。**
**🔄 第2段階(進行中): 各レベルを2問以上にする。** 方針(ユーザー指示): **AtCoder/LeetCode/Kaggle の上位問題をオリジナル化 → 要素分解して低レベルに問題化**。出典はtipに明記しない(オリジナル問題として出す)。
**🆕 2026-08-27 ユーザー指示で最終目標が「各レベル10問以上」に引き上げ**（まず2問化を完遂→3問化…と幅優先で進める。検索でネタを増やす・パクリ禁止の予想問題方式・適宜レベル調整可・全問題を参照して学びの流れを意識）。

### 2問化フェーズの進め方(第1段階と同じパイプライン)
1. `node scripts/check-thin.ts` で**2問未満のレベル**を列挙(check-levels の2問版)。
2. `scripts/catalog-tasks{N}.mjs`(次は **catalog-tasks48**)に、その薄いレベルの**2問目**を追記。各レベルの帯テーマに合う「上位問題の要素」を置く。**既存とかぶると `generate.mjs` が止まる**(低レベルの素朴な計算は大体かぶる→より具体的な別計算を選ぶ)。
3. `node scripts/generate.mjs` → 重複/参照解エラーを潰す → ゲート(validate/prereq/tsc/build/catalog)。
4. `node scripts/check-thin.ts` で残数を再確認。1バッチ約49レベル。

### 進捗 (2026-08-27 更新)
- **🎉 第2段階完遂: 全999段が2問以上(2問未満 0段)。** catalog-tasks53〜72 を登録し lv393〜999 に2問目を追加(計 **2436問 / 13972ケース**)。全ゲート緑(validate 0 failures / prereq 0 / coverage 143/143 / tsc / build / catalog)。
- **✅ 3問化 完了(2026-09-03)** → **✅ 4問化 完了(2026-09-04, C94〜C113)** → **5問化 進行中(次は catalog-tasks114、lv002〜)**: catalog-tasks94〜100 登録済み(lv002〜375帯に+1問)。現在 3757問/21895ケース、4問未満 618段(次は lv376 以降)。全ゲート緑。教訓: 高レベル帯(lv327以降)は飽和が進み、C100では設計49問中18問が既出で差し替えになった→設計時grepは同義語を広めに、生成ゲートの「同一参照解」検出を最後の砦として頼る運用が確立。ins内にJSの.replace式を書かない。ゲーム系出力は first/second。10問化はこの幅優先(check-thin.ts 4→5→…)で継続。 **アイデアバンク**: scripts/idea-bank.md にWeb探索(CSES/CodeAbbey/PE)で発掘した未収録概念約60件をgrep検証済みで貯蔵(2026-09-03)。バッチ設計はまずバンクから拾う。C101登録済み(lv376〜424、ポーカー役・ボウリング・Luhn等の新ドメイン導入)→現在3806問/22189ケース、4問未満569段。 第2次探索(P99/AoC/AOJ-CGL/二分木ドメイン)で約100件に拡充+ライブラリ帯再出題方針を明記。二分木は空白ドメインと判明(復元・inorder・BST挿入・平衡判定など全部未収録)。 C102登録済み(lv425〜473、バンク駆動: BWT/De Bruijn/LFSR/エレベーターrides/銀行家/SET等30件消費、[済C102]マーク済み)→3855問/22483ケース、4問未満520段。教訓: LFSR等の状態遷移シムは初期状態に戻らない(ρ字型)入力で無限ループしうる→可逆性を入力条件で保証する。 C103登録済み(lv474〜523、バンク第2弾約35件消費: マインスイーパ/2048/ビリヤード/BST/ISBN/コラコスキ等)→3904問/22777ケース、4問未満471段。 C104登録済み(lv524〜573、宣教師/3容器/干支/和暦/ジニ係数/ミニBF/毛虫木等)→3953問/23071ケース、4問未満422段。教訓追加: ins内のJS式(.replace等)は絶対禁止(3バッチ連続で混入→リテラルで書く)。反復系refはinsの停止性を必ず机上検証(997は非ハッピー数で無限ループした)。 **設計原則追加(2026-09-03ユーザー指示): 難問は要素分解し、部品を下位レベルに先に配置する(部品の存在をgrepで確認、無ければ同バッチの下位薄レベルへ)。** C105登録済み(lv574〜623、要素分解原則を適用: 全方位木DPの差分公式・検問通過最短路数などを部品と組立の階段で配置、tipに部品既出を明記)→**4002問**/23365ケース、4問未満373段。C106登録済み(lv624〜673、部品階段を重視: カット容量・流れの値・ホール条件でフロー理論を分解、WF中継制限・サブマスク列挙・部分列判定・信号の色など複合既出問題の部品を下位に配置)→**4051問**/23659ケース、4問未満324段(次はlv674〜)。C107登録済み(lv674〜723、部品階段: 区間スケジューリングの直前区間検索・2次元累積和の表・中心展開1回分・列圧縮の行範囲列和などを下位に配置。C106のlv668がlv725「削除して稼ぐ」と実質重複と発覚→隣接禁止最大和の復元に差し替え。教訓: 概念名grepだけでなくLC系の別名も疑う)→**4100問**/23953ケース、4問未満275段(次はlv724〜)。 **C108登録済み(2026-09-04, lv700+lv724〜773、第4次Web探索駆動)**: 探索前に全4100問のconcept/title一覧をドメイン別に作って差分を取る運用に変更(ユーザー指示「検索前に既存の主題を把握」)。面接定番パターン集(Two Pointers/Binary Search/Hash/Stack/Deque/Heap/BFS/Trie)・PE51〜150 から未収録を抽出し、N以下の連続1なし整数(ビット桁DP)・増加経路の本数・連続上限つきサイコロ列・最小値×区間和の最大・括弧内反転・分割ゲーム・葉の値の木コスト・和×長さ<k・両端削りでちょうどx・重複なし区間の最大和・k回以上文字の分割統治・rev差ペア・悪いペア・ドメイン階層集計・LCM=k区間・k番目の分数・単一CPU実行順・順位票ソート・k枚連番組分け・区間挿入統合 などを配置。生成は重複/参照解エラー0で一発通過→**4150問**/24253ケース、4問未満225段(次はlv774〜)。idea-bank.md に「第4次探索」節を追加(未採用候補約80件、[済C108]マーク済み)。 **C109登録済み(lv774〜823)**: 第5次探索としてCSES全節リスト(Range Queries/Sorting&Searching/Additional)を取得して差分。Hotel Queries型のセグ木降下・List Removals型のBIT k番目削除・区間加算+代入混在・区間乗算和・給与クエリ・(最小,個数)遅延・砂山崩し・非部分列最短の構成・4つ組の和・ビル影の面積・列名変換・LFU などを配置。生成一発通過→**4199問**/24547ケース、4問未満176段(次はlv824〜)。 **C110登録済み(lv824〜873)**: 第6次探索(cp-algorithms索引・PE1〜50/151〜200)。配列コピー履歴・腐るりんご・サーバー割当・注文板・頻度スタック・最小値つきキュー・排他実行時間・アフィン遅延列・落下正方形・ハフマン最長符号・行ソート行列の中央値・スラッシュ領域(4三角形DSU)・min-plus行列累乗・漸化式の和・ガーナー法・近似分数列・ウィルソン逆算・小シュレーダー・メナージュ数・はしご全域木・平面分割 など50問(lv850含む)。参照解エラー1件(入力不足)を修正→**4249問**/24847ケース、4問未満126段(次はlv874〜)。 **C111登録済み(lv874〜923)**: 根付き森・ピサノ縮約・差のGCD・長方形の互いに素ペア(メビウス)・超回文数・並べ替え回文部分文字列(マスク)・Lyndon因子出力・接頭辞出現回数・Duval/orderly queue・下限つきフロー・共有可パス被覆・道のk番目・CHT型飛び石・素数三つ子・GF(2)連立・逆行列mod p・拡張オイラー縮約・グレイシャー分割・素数分割・切手問題・最小二分割・円周ウォーク・Lehmer復元・ケイルズ・フィボナッチニム・ターニングタートルズ・ピーク順列・モツキン水平k・リオーダン数・ファイン数・ラテン長方形・n!のb進末尾0・n!の約数個数 など50問。生成一発通過→**4299問**/25147ケース、4問未満76段(次はlv924〜)。 **C112登録済み(lv924〜973)**: 第7次探索(OEIS主要数列一覧・名前つき素数一覧・PE201〜250)。循環小数表記・最小平方非剰余・ペラン判定・包除の倍数総和・実際的数・不可触数・ブルム数・正四面体の辺の塗り分け・2×n反転同一視・最長巡回長・ヤコビ四平方・二項係数の平方因子・直角辺固定の三角形・相異なる素数分割・2D原点回帰・ラン≥2の01列・k根指定の森・一度は同点・BEST定理・ラスカーニム・ノースコット・クイーンの利き・式の反転コスト・天秤・DFA受理本数・砂時計・非表現和・相異なる回文部分列・直線の本数・最小包含円(Fraction)・耳の数・W-E数・ゴロム・ユークリッド・マリン・k-スムーズ・ウラム・フラネル・ヘロン三角形・アキレス数・超完全数・エルデシュ・ニコラ・節約/浪費数・タウ数・互いに素な約数和・公約数の個数・ファレイ次項・lcm≤nペア・波打ち数・下ウィトフ など50問。教訓: 正八面体の面=立方体の頂点(双対)で参照解が完全一致→重複ゲートが検出(四面体の辺に差し替え)。k本の木の森は「根を指定」しないと公式が成り立たない(修正済み)。Bash経由のJS文字列書き換えは
が崩れる→Editツールで直接直す。→**4349問**/25447ケース、4問未満26段(lv974〜999のみ)。 **C113登録済み(lv974〜999、26問)**: 3立方和・フィボナッチ出現点・1なし合成・巡回長相異・随伴スターリング・順序付きk組分割・三項係数・巨大n階段(行列)・区画数指定塗り分け・フィボナッチ引き算ゲーム・DAG全経路数・最小平均閉路(カープ)・AHU形の種類数・葉k個の木・部分集合の幅総和・正しい括弧部分列数・交換で括弧修正・フック長公式・SB木の道順・popcount素数・忌まわしい数・大円領域・k本集中直線・直線+円・ヴィーフェリッヒ・8パズル手順復元。→**🎉 4問化 完遂(2026-09-04): 全999段が4問以上。4375問/25603ケース。** 次は5問化(5問未満 967段、lv002から幅優先)。 **C114登録済み(5問化 第1弾: lv002〜064の50段)**: 第8次探索(初学者演習集: n+nn+nnn・先頭2文字反復・引き算で商・_アンパック・15の倍数・enumerate位置一覧・内接正方形・残りの角・L字面積・円錐表面積・複数値return・map変換・円柱体積・2本の管・列車と橋・二重割引・n個買うと1個無料・単価比較・チップ割り勘・残業代・食塩水混合・0時からの秒・分→日時分・次の正時・商余りから復元・10の位四捨五入・奇数の平方和・交代和・try-else・3k−2・最大桁の個数・区間の隙間・n桁最大倍数・等差の項数・ペース表示・箱に入る立方体・4のべき判定・長さkの単語・偶数番目単語大文字・文字削除・最頻数字・chr復元・最初の数・正の累積個数・昇順or降順・文字含む単語・k個ずつ分割・要素差・上位k平均・偶奇一致ペア)→**4425問**/25903ケース、5問未満917段(次はlv065〜)。 **C115登録済み(5問化 第2弾: lv065〜126の50段)**: 桁(2番目に大きい桁・偶奇桁の差・反転倍数・重複桁除去・桁ごとmax)・行列(0最多の行・行の累積和・市松の和・しきい値超えの行・値の位置・左右対称な行・2次元の山)・ベクトル/幾何(内積の符号で角の種類・3辺で鋭角鈍角・辺上判定・対称移動・第1象限)・集合(相異なる値の和・ちょうど1つ・被覆判定・オーバーラップ係数)・評価指標(平均誤差・sMAPE・バランス正解率・マシューズ相関)・数論(k以下の約数・約分・最小素因数一覧・直前の素数・6乗数・a^bの末尾0)など50問→**4475問**/26203ケース、5問未満867段(次はlv127〜)。 **C116登録済み(5問化 第3弾: lv127〜178の50段)**: 2の補数・2べき分解・包除の倍数除外・平方でも立方でもない数・約数の隙間・桁の山型・0除く桁積・k進回文の基数数・桁置換・単調増加最大数(貪欲)・相異なる桁のn桁数・桁の出席簿・利益k以上の売買組数・フィボの奇数個数・整数演算の平方根2桁・公平な切れ目・k進桁反転・k進の数字出現数・+1で変わるビット数・ペア交換・3連続を2つに・パターン反復削除・対称成分の組数・べき等行列・0の行と列を塗る・素数のmod4分類・n番目の平方自由数・n以下のカタラン数・平方数の約数和・ミニマックス経路・素因数和が素数か・座標降下・単調最長区間・重複ありの回転配列最小・合わせてk番目・剰余の最大・共通文字(重複込み)・両端ペアの和・a[a[i]]=i・最近傍の倍数へ丸め・1回きりの値を除去・重なる区間の数・接頭辞と接尾辞の重なり・窓の和が一定か・しりとり判定・逆さ辞書順・並べ替え回文の単語数・kブロック反転・長さの偶奇交互・略語照合→**4525問**/26503ケース、5問未満817段(次はlv179〜)。 **C117登録済み(5問化 第4弾: lv179〜228の50段)**: ビット位置一覧・予算内最大ペア和・隣接1交換で整列・ちょうどk種類の最短区間・和がKの最短区間・語彙k到達位置・平均超えの単語・辞書順並び判定・長さ列の回文・最長共通部分文字列の実体・短くなるときだけ圧縮・単語列の並べ替え回文・隣接行列の構築・隣の次数の和・森にする最小削除・直径2以下か・差の種類数・近い同値ペア・和0の3つ組の実体・到達できない通路・埋め立てて最大の島・__lt__でソート・1窓口の待ち時間・行の振れ幅合計・窓の最大最小差の列・右の相異なる値の数・増加3つ組の真ん中・行ソートで列も昇順か・外周を剥いだ表・重複行の除去・180度一致マス数・三角数ジェネレータ・硬貨の内訳復元・2段ちょうどk回・辞書順最小の道順・最小経路和の表・区間選択の実体・到達位置の個数・ジャンプ経路の復元・種類数最小の両替・長さk以上の最大和・最大和増加部分列の長さ・0と1の差がdの最長・最小絶対差ペアの実体・予算内最大個数・最大部分がkの分割数・二項係数が閾値超え・等比和mod・積の約数個数・gcdを1にする最小個数→**4575問**/26803ケース、5問未満767段(次はlv229〜)。 **C118登録済み(5問化 第5弾: lv229〜278の50段)**: 判定/個数で止まっていた既出を「実体を出す」段に上げる方針。SG素数k番目・アナグラム代表語・AND非0最長・OR不要要素・立ちビット最大群・和がK区間の位置・連続整数列の実体・1削除で偶奇和一致・和10で消去・2彩色の結果・成分ごとの頂点一覧・三角形1つ・九去法・コラッツで下回る手数・区間加算後k以上・桁和分布・最大和の通り数・最長増加区間の実体・最大積区間の位置・数塔の経路値・ちょうど容量の無制限ナップ・部分和の実体・単語分割の実体・混合文字列の分解・縄結びの各コスト・刺す点の位置・隣接1交換で最小・誰とも互いに素でない数・アナグラム窓の位置・3つ組の積の総和・Z配列・最大成分の頂点・深さ一覧・最短路2通り以上・DAG経路1つ・入次数0の頂点・8方向距離表・子の数一覧・共通素因数・逆元表・フィボ0の番号・正負交互・約数和が素数・桁和最大の数・斜辺cのピタゴラス数・階乗の和mod・符号反転で釣り合い・最大ギャップの位置・共通値の列挙・K種以下の区間数→**4625問**/27103ケース、5問未満717段(次はlv279〜)。 **C119登録済み(5問化 第6弾: lv279〜328の50段)**: 実体・過程を出す方針を継続。XOR最大の部分集合・ラン長降順・深さdの文字・最長括弧の位置・繰り返し部分文字列の実体・相異なる回転の列挙・階段の足あと・ジグザグの実体・等差部分列の実体・頻度をそろえる最小削除・個数制限ナップの内訳・回文分割の実体・先手の得点・最大価値の経路・島サイズ降順・有向閉路1つ・オイラー路の始点・母頂点すべて・無向化した辺数・Havel-Hakimi構成・区間AND・LCISの実体・ユニタリ約数・区間の素数列挙・括弧列の全列挙・最大和K区間の中身・T以下最長の中身・相異なる分割の実体・最大積の分け方・落下経路の列・仕事選択の番号・最短共通超列・最少単語分割・回文部分文字列の列挙・削除する文字・深さごとの名簿・内陸湖の埋め立て・ナイトの経路・DAG最長経路の頂点列・木の中心・更新つき区間の振れ幅・スパーステーブルの表・石の衝突の過程・半減後の列・k個反転の位置・符号割り当ての実体・電話キーの全列挙・隣接行列の2乗・二重階乗の末尾0・風船の割る順序→**4675問**/27403ケース、5問未満667段(次はlv329〜)。 **C120登録済み(5問化 第7弾: lv329〜378の50段)**: 共通接頭辞の実体・斜め対角線の列・k角数・k進の乗算・n以下のフィボ列挙・自己同型数列挙・約数最多の数・バブルソートの各パス・最も近い3数の組・和0区間の実体・最大矩形の位置・後置→中置・重複なし最長の実体・辞書順最大k部分列・キャンディの配分・2分割の実体・作れる金額の列挙・最大長方形の位置・石の合体を括弧で・負辺最短経路の頂点列・全点対距離表・補グラフの辺・偶奇交互の実体・削除位置の一覧・出現順の集計表・到達範囲の塗り・約数個数の一覧・1にする手順・和がKのペア全部・k個連続消去の実体・クリティカルパス・編集操作列・LISの実体・削除する要素・3連続禁止の位置・最長回文部分列の実体・桁和Sの最大数・割当の実体・k等分割の実体・辞書順最小の完全順列・不定方程式の全解・n!の逆元・レベル順・最大独立集合の頂点・最長回文部分文字列・更新つき第2最小・マッチングのペア・確率最大の経路・平方数分解・インターリーブの取り出し方。教訓: 前向きDPの表を前から辿ると添字がずれる→復元は表の向きに合わせて後ろから辿る(lv360で修正)。→**4725問**/27703ケース、5問未満617段。 **C121登録済み(5問化 第8弾: lv379〜428の50段)**: 最小閉路の頂点列・pで割り切れない二項係数の位置・2次元矩形加算後の盤面・更新つきk番目・各列から1つ選ぶ組・山型区間の実体・バックスペース後の文字列・SCC番号(トポロジカル順)・最小素因数一覧・円順列で隣り合う・最小集合被覆の中身・最大流の各辺流量・どのLISにも乗る要素・割り切りの鎖の実体・k個で和Tの組・コピペ手順(C/P列)・塊分割の長さ列・Luhnの生成側・ハフマンの符号長・締切つき仕事の番号・XOR基底の出力・最大XORペアの実体・区間代入と区間最大・削除ありmex・区間内K以下の列挙・多角形の向き正規化・長方形の重なり座標・凸包の頂点列・線分交点(Fraction)・内/境界/外の3分類・格子経路の全列挙・線分上の格子点・φの一覧・分数の交差乗算比較・パス上の辺重み・重み付き直径の頂点列・関数グラフの閉路頂点・マンハッタン最遠ペア・関節点の一覧・橋で分けた成分・共通の友人の実体・最小費用流の各辺流量・Nimの必勝手全列挙・引き算ゲームの負け局面・2山の必勝手・フェルマーを騙す底・フェルマー法の因数分解・C(n,k)が奇数のk・離散対数表・k乗根の全解。**重要な基盤修正**: 参照解の一括評価で、Node が UTF-8 で書いた JSON payload を Python 側が cp932 で復号し、日本語の内部バイトが直後の円記号を巻き込んで JSON のエスケープを壊す事故が発生(lv4「ワニ」入力が EOFError)。generate.mjs / validate.ts / ladder.ts の payload を非ASCII全escape(ASCIIのみ)に変更して根治。既存の期待値に変化なし。 教訓: 比較キーを持ち回るとき best を (key, 実体) のタプルにしたら比較は best[0] と行う(lv419で TypeError)。Bash のヒアドキュメントは円記号を1段外すので、円記号を含む JS/正規表現の書き換えは chr(92) で組み立てるか Write ツールを使う。→**4775問**/28003ケース、5問未満567段。 **C122登録済み(5問化 第9弾: lv429〜478の50段)**: 辞書順k番目の接尾辞・BWT逆変換・各パターンの出現回数・オイラー閉路の頂点列・2-SATの充足割当・SOS変換の表・区間+1の手順・上塗り後の色の列・エレベータの各便の乗客・最小頂点被覆の実体・最小支配集合の実体・最近点対・最遠点対・最大面積の三角形・平方の差の全表現・ペル方程式の解k個・平方剰余の全列挙・部分文字列の全列挙・GCD=1の最短区間・2列の中央値・次数dのラベル付き木の数・最短ハミルトン路の順路・一致する回転量・ハミング(7,4)の符号化・末尾に足す回文の文字列・レールフェンスk段・ヴィジュネルの鍵推定・ジグザグ部分列の実体・等和分割の塊・最長等差部分列の実体・回文2つの切れ目・行列連鎖積の括弧・金額の組合せ列挙・作れない金額の列挙・整数分割の列挙・次の括弧列・集合分割の列挙・k組以下の分割数・完全順列の列挙・ちょうどk個の素数・相異なる桁の数・最大クリークの実体・全点対最短路の経路復元・負閉路の頂点列・各タスクのラウンド・ハミルトン閉路1つ・最小彩色の割当・葉むしりの過程・閉路を作る辺・2部グラフの色分け。 教訓: 設計前のgrepは「実体化したものが既にあるか」を見ること——今回は接尾辞の出現位置・第一種スターリング数・Base32復号・レールフェンス復号・回文半径の一覧・中央値の維持・プリューファー符号化・完全二部グラフの全域木数の7件が設計段階で既出と判明し差し替えた。 →**4825問**/28303ケース、5問未満517段。 **C123登録済み(5問化 第10弾: lv479〜528の50段)**: ケーニヒの最小点被覆・頂点重み最大パスの頂点列・各辺を通るペア数・部分木サイズ一覧・併合ごとの最大成分・デカルト木の親一覧・最大和長方形の四隅・凸四角形の判定・交差する線分のペア・多角形内部の点一覧・円周上の格子点・TをXORで作る部分集合・ライツアウトの押す位置・複素数の割り算・可変長整数の復号・サイクル分解から順列へ・逆順列・不満なペアの実体・1回の編集の内容・各バーの最大幅・最大正方形の位置・素朴フィボナッチの呼び出し回数・牛の配置の実体・k分割の実体・LPTの割り当て・積載量ごとの積み荷・ISBN-10の生成側・循環節の長さ一覧・a^bの全表現・螺旋の座標から番号・行列でx以下の個数・片方だけにある値・掛け算表の値一覧・ロープの取れる本数・欠けた記録の復元・パンケーキの反転位置・組立除法・選ぶk案件の番号・最大積区間の位置・ビリヤードの反射点・出現回数を異ならせる最小削除・和がkの倍数の最長区間・水の深さ一覧・目標に近いペアの実体・k回中ただ1つ違う値・交換の手順・反転する区間・h指数を支える論文・覆われない区間・自分以外のGCD。 **棚卸しの穴を修正**: これまで src/data/generated.json だけを見ていたため、手書き問題(src/data/problems/*.ts、約209問)が棚卸しに入っていなかった(lv500 が 1 問に見えていた)。以後は PROBLEMS.md を読む `py scripts/inventory.py <lo> <hi> <out.txt>` を使う。 教訓: 生成器の重複ゲートは「概念名の完全一致」で C119 の石の衝突と衝突した(lv509 を差し替え)——同じ動詞で言い換えた概念名は別レベルにも存在しうるので、設計前 grep は概念名の断片で当てる。 →**4875問**/28603ケース、5問未満467段。 **C124登録済み(5問化 第11弾: lv529〜578の49段、lv550は9問あるため除外)**: 設計メモ(idea-bank.md末尾)の49案を独立に再検証したところ**6案が衝突**していたので差し替えた——lv545全ペアのOR総和(lv854に既出)、lv542ホフスタッターのQ数列(lv956に既出)、lv560最小ジャンプ数の経路(lv217「最小ジャンプの経路を辞書順最小で」に既出)、lv537障害物を避ける経路(「障害物を避ける辞書順最小の道順」に既出)の4件は完全衝突、lv549パスカル三角形n行すべて(同レベルに第n行あり)とlv561二部グラフ成分の数(同レベルに(クリーク/星形/一本道)成分の数が既に3問ありパターンが単調)の2件は水増し寄りとして差し替え。差し替え後は lv545 ビット別の個数から総和とXORを復元(lv853の逆変換)、lv542 ホフスタッターの女性・男性数列(相互再帰)、lv560 最小ジャンプの各段の到達限界(貪欲の内部状態を出す部品)、lv537 各マスを通る経路の本数の表(前計算と後計算の積)、lv549 二項係数の値から位置を逆引き、lv561 全体を連結にする追加辺の一覧。他は4×4数独を解く・固定グリルで暗号化・演算子の挟み方を1つ・騎士の周遊(ワーンスドルフ)・隣へしか動かせないハノイの手順・コラッツ数列そのもの・桁の積の過程・積への分解の全列挙・シルベスター数列の逆数和・入れ子リストの深さごと合計・ジョンソン法の処理順・n個の並列抵抗・EDFの遅延一覧・段落の両端揃え・単語はしごの経路・迷路の道順・最短経路木の親一覧・火からの脱出経路・2チーム分けの分け方・無向閉路を1つ・数を英語で綴る・逆走最小の経路・最遠マスの座標・独自順でのソート・毛虫木の背骨・重い子の一覧(HLDの部品)・2辺連結成分の頂点一覧・最短路に使われる辺・オイラーツアーのin/out区間・領域番号つき盤面 など。**生成は重複/参照解エラー0で一発通過**。 **教訓: 設計メモのgrep済みという注記を鵜呑みにしないこと**(49案中6案が衝突。特に「既出はAと書いてあるがBも既出だった」型の見落としが3件)。設計時grepは案の主題そのものだけでなく、同レベルの他4問との「出題パターンの重複」(同じ形の数え上げが4つ並ぶ等)も見ること。 →**4924問**/28897ケース、5問未満418段。 **C125登録済み(5問化 第12弾: lv579〜629の50段、lv600は既に厚いため除外)**: 10段ずつ5分割して並行設計し、各担当が棚卸し→同義語grep→起草→**参照解のPython実行検証**まで済ませたものを連結した。生成は重複0で一発通過。 **同じ回で「品質チェックと修正」も並行実施(ユーザー指示: チェックと問題生成を同時並行でフルパワー)**。修正の内訳: (1)**参照解が数学的に誤っていた lv130**(「2つ以上の連続する正整数の和」なのに初項0の列まで数えており、三角数入力で6ケース中3ケースの期待値が誤り。正しく解くとWAになる状態)を「約数がk個以上になる最初の三角数」に差し替え。(2)**定数を出力するだけで全ケース通る問題7件を解消** — lv609(木の2彩色は常に2。しかも問題文が答えを明記)→森を1本の木にする辺の追加方法の数、lv846(両側から見える要素は常に最大値の1つ)→更新つき区間の最長連続増加長、lv906(1列ケイルズはn≥1で常に先手勝ち)→複数列のケイルズ(Grundy数のXOR合成)、lv548/lv746/lv856/lv891は入力例の偏りだったのでinsを散らして解消(lv746は「区間を1つ消して回文にできるか」が任意の入力でyesになる欠陥だったため、消せる区間長の上限kを入力に追加してyes3件no3件にした)。(3)**停止しない参照解と規則違反**(lv376/lv395/lv285)を修正。(4)**tipの数式誤り**(lv940のΣk·n^(n−k−1)は(n+1)^(n−1)にならない。正しくはΣC(n−1,k−1)·n^(n−k))と、tipの「(既出)」が未習概念を指していたものを修正。(5)**タイトル重複の整理** — 同一レベル内の重複を2件から**0件**へ、レベルをまたぐ重複を37組から19組へ、重複に関与する問題を79問から38問へ削減(うるう年判定lv5/lv6、文字数を数えるlv1/lv4、商と余りlv3の2重、約数の個数lv7×2+lv301、二部グラフ判定4箇所、最長共通部分文字列3箇所、DAG最長経路・有向閉路・木の葉の数・0-1BFS・木の最大独立集合・重み付き木の直径・双対ナップサック・BIT上のk番目 など)。 **重複ゲート自体も強化(generate.mjs)**: 従来は参照解とconcept名の完全一致しか見ておらずtitleを一切見ていなかった(コメントには「タイトルが被っていないか」と書いてあるのに実装が無かった)。**同一レベル内のタイトル完全一致で停止**、**レベルをまたぐ同名タイトルを警告表示**、**全ケースの期待値が同一の問題を警告表示**の3つを追加。AUTHORING.mdに「機械が止める3つ/警告だけの2つ/誰も検査していない3つ(参照解そのものの正しさ・手書き問題との重複・同一レベル内の出題パターンの単調さ)」を明記した。 **教訓: 期待値は参照解の実行結果から自動生成されるので、参照解が間違っていてもvalidateは必ず通る。** 問題文の条件を1つずつ参照解に照らし、境界(n=1・0・負・同点)を手で確かめること。また自分の作った問題も「6ケースの答えがばらけるか」を必ず確認すること(今回C124のlv530数独で、6つの入力例をすべて同じ完成盤から作る失敗をしている)。 →**4974問**/29200ケース、5問未満368段。 **C126登録済み(部分・20問: lv640〜649とlv661〜670)**: サイクル2で27エージェント(修正7・設計20)を並行実行したところ**セッション上限で26体が中断**し、完了していた修正1件と書き出し済みの設計断片2本だけを検証して確定した。教訓: エージェント数を増やしても同時実行は最大16体までで、総トークン量が上限に当たると全滅する。以後は1体あたりの担当を倍(20段)にして体数を12前後に抑え、カタログを丸ごと読まない作法を指示に入れた。**品質検査を仕組み化**: `npx tsx scripts/quality-check.ts`(`--all`で全件、`--strict`でhighがあれば失敗)を追加。validateでは原理的に見つからない欠陥(定数解・出典名の露出・参照解の完全一致・同名タイトル・空の期待値・素のexit())を全問題(生成+手書き)に対して検査する。初回実行で**出典名の露出32件**(入力文字列に leetcode / AtCoder / Project Euler が残っている)と**手書き問題の参照解完全一致4件**(lv1/lv2入れ替え・lv1/lv3 2数の和・lv1/lv4文字列の繰り返し・lv60内の最頻の単語)を検出。次サイクルで修正。 あわせて `autoContinueAtUsageLimit: true` をユーザー設定に入れ、上限リセット後に自動再開するようにした。 →**4994問**/29320ケース、5問未満348段。 **C128〜C130登録済み(lv691〜840の149段)**: **方式を根本的に変えて効率が7倍になった**。それまではサブエージェント20体に分散して1問あたり約5万トークン、しかも上限で全滅して成果ゼロという回が2度続いた。原因は各エージェントが毎回ゼロから棚卸し・重複grep・問題ごとのPython検証をやり直していたこと(立ち上げコストを体数ぶん払う)。**改善点は3つ**: (1)**検証を中央に集約** — generate.mjs はもともと全参照解をPython一括実行して期待値を確定するので、各エージェントの1問ずつの検証は二重払いだった。5問ごとに異常がないか見る程度に留め、正確な検算は generate に任せる。(2)**起草は安価なモデル(sonnet)に** — 問題を書く作業自体は十分な品質が出る。(3)**参照資料を親が先に用意して渡す** — 各自が inventory.py を走らせる代わりに、親が範囲ごとに実行した小さなファイルを指定し「これだけ読めばよい。PROBLEMS.md と src/data は開くな」と明示。結果、6体で149問(1問あたり約7300トークン)を全員完走。**チェックポイント方式も継続**(1問書くたびに断片ファイルへ追記)。教訓として、上限に当たるとサブエージェントは即エラーで死に自動再開の対象外なので、成果は必ずディスクに落としながら進めること。生成は重複0・定数解0で一発通過。品質検査で新たに出た出典名の露出2件(tipのLeetCode言及)も即修正して high 0 を維持。 →**5163問**/30336ケース、5問未満179段。 →**5163問**/30336ケース、5問未満179段。 **🎉 5問化 完遂(2026-09-06): 全999段が5問以上。5342問/31410ケース。** C131〜C134登録済み(lv630〜660とlv841〜999の179段)。Sonnet 7体で起草し全員完走(1問あたり約7100トークン)。**生成で捕まえた不具合3件**: (1)`print("\n".join(...))` の \n がJS文字列で本物の改行になりPython文字列が閉じない(lv872/lv884)。ソース側で \n と書く必要がある。(2)正規表現の `\d` がJSの無効エスケープとして `d` に潰れ、re.findall が何もマッチしなくなっていた(lv886)。**この2件目は生成が成功してしまい、新設した「全ケースの期待値が同一」警告だけが検出した**——出力が全部空文字になっても validate は通るので、この警告が無ければ見逃していた。ソース側で \d と書く。 **教訓: JS単一引用符の文字列にPythonを埋めるとき、Pythonに届けたいバックスラッシュは必ず2重にする。** \n(改行)はそのままでよいが、Python文字列リテラルの中の "\n" や正規表現の \d \w \s は \n \d \w \s と書く。 あわせて**タイトル重複17組を解消**(11組は解法が本当に違うので改題、6組は変数名だけの同一計算なので差し替え)、**lv499の参照解のバグを修正**(問題文は「元の長さまで0で埋める」と書いているのに埋めておらず、仕様どおり実装した学習者が6ケース全部でWAになる状態だった)、**問題文の前提を破る入力例2件**(lv914はXORが0の後手必勝局面を「先手必勝」と称して与えていた、lv171は1行目全体が共通接尾辞だった)を修正。品質検査は high 0 を維持。 次は**6問化**(6問未満 970段、不足1149問)。 →**5342問**/31410ケース、5問未満0段。 **C135〜C138登録済み(6問化 第1陣: lv002〜240の215段)**: Sonnet 8体で起草し全員完走(1問あたり約4900トークン、これまでで最良)。**生成ゲートが3種類の不具合を捕まえた**: (1)重複2件——lv212の参照解がlv700「DPテーブルの中身を見る」と完全一致、lv214の概念名がlv634「DP: 最小コスト経路の本数」と完全一致。lv212は「2段を使う回数ごとの上り方の内訳」(合計がフィボナッチ数になるのが見える)、lv214は既出5問中4問が最小経路系で飽和していたため筋を変えて「回転を戻して昇順に直す」(既出の回転回数を使って実体を組み立てる)に差し替え。(2)**バックスラッシュの潰れ144件**——1体が `sys.stdin.read().split("\n")` を多用し、\n がJS側で本物の改行になってPython文字列が閉じずSyntaxError。起草指示に注意を明記していたが守られなかったので、**今後は組み立て後に機械的に置換する工程を入れる**(ソース上の "\n" を "\n" に、正規表現の \d \s も同様)。→**5557問**/32700ケース、6問未満755段。 →**5557問**/32700ケース、5問未満755段(次はlv241〜)。
- **🆕 データ分析トラック拡充(2026-08-27 ユーザー要望)**: catalog-tasks75 で numpy(reshape/where/argsort/@/percentile/histogram)・statistics(幾何平均/multimode/quantiles)・pandas(nlargest/isna/drop_duplicates/rank/pivot/agg/str.contains)・**scipy**(spearmanr/ttest_ind/quad/linalg.det/pdist/describe) を22問追加(lv085/090/095/105/110)。**scipy 1.18.1 をこのPCに pip install 済み**(Pyodideにもscipyあり→serverOnly不要)。**matplotlib(3.11導入済)は標準出力比較の判定と相性が悪く自動採点不可**のため、np.histogramなど「グラフの中身の計算」で代替。画像比較の判定機構を作れば解禁できる(将来TODO)。ライブラリ帯は「基礎で既出の計算でもライブラリAPIの習得として再出題可」という既存方針(numpyの合計と平均など)を踏襲。
- **⚠️ 同一計算の重複対策(最重要の教訓)**: generate の重複ゲートは「ref/concept の完全一致」のみ。今セッションで既存問題との**言い換え重複を9件**踏んで差し替えた(lv407/487 線分交差, lv457/667 積が正, lv539/721 サイコロ和, lv540/722 ナイトダイヤル, lv523/771 1/3超, lv464/941 投票, lv465/920・lv466/916 スターリング/オイラリアン, lv704/989 括弧挿入, lv869/919 シュレーダー, lv973 フィボナッチ判定 ほか)。原因は grep パターンの同義語漏れ(「挿入/追加」「ストリーム/逐次」など)。**対策手順**: ①バッチ設計前に最新 PROBLEMS.md から concepts/titles を再抽出 ②候補ごとにキーワードを同義語込みで grep ③generate 後も対象帯の既存1問目と自分の追加分を目視比較。加えて、参照解が「1行系」(isqrt 等)の問題は完全一致ゲートに引っかかりやすいので避けるか一意な書き方に。
- **水増しの教訓(重要)**: generate.mjs の重複ゲートは「完全一致」しか見ない。**同じ計算の別表現は素通りする**ので、追加前に既存の concept/title を grep で横断確認すること。実際に踏んだ例: ①lv300「最長ビトニック部分列」= 既存 lv392「最長双調部分列」(同一計算) ②lv388「重複組合せ mod」= 既存 lv409「重複を許す組合せの数」(modの有無だけ) ③「偶数フィボナッチの和」「行列連鎖」「ピックの定理」「半素数」「次の素数」「ハフマン」「アナグラム出現」「島の数」等は既出。いずれも別問題に差し替え済み。
- 残りは lv442〜999 のほぼ全段(高レベルほど別アルゴリズムを要し、かぶり回避が難しい)。
- **コツ**: 素朴な基礎計算は既存とかぶる。各レベルの帯テーマ(下記§6)に合う「やや具体的な別計算」を選ぶとかぶりにくい。1バッチで数件は必ずかぶるので generate→差し替えを2〜3往復する前提で。図形数(三角数/四角数/五角数/四面体数…)は既出多数→中心つき多角数など捻る。

※ 第1段階(全段充足)の経緯・教訓は下記「現状」を参照。

### 現状
- **999 / 999 段が充足（全段完了！）**（`node scripts/check-levels.ts` で「空の段: なし」を確認できる）。
- 充足済み: 既存の手書き帯＋生成で **lv1〜999 の全段**に最低1問。
- **空き段ゼロ達成**。今後は「各レベルを厚くする(1レベル100問へ)」フェーズに移行（後述TODO）。
- 2026-06-03 セッションで lv180 + lv211〜450 の **241段（約240問）** を追加（catalog-tasks27〜34）。DP/グラフ/数論/文字列/幾何/データ構造/ゲーム理論の本物の別問題で、最上級ランク（セグ木・遅延伝播・最大流・最小費用流・SCC・2-SAT・LCA・行列累乗・Aho-Corasick・接尾辞配列・シュタイナー木・行列木定理…）へ至る「前提分解」の踏み段として配置。
- 2026-06-03 セッション続きで **lv451〜499（catalog-tasks35）** / **lv501〜549（catalog-tasks36）** / **lv551〜599（catalog-tasks37）** の計147段を追加。35はZ配列/Manacher/KMP/LCS系DP/数え上げ(分割数・カタラン・ベル・スターリング)/桁DP/ハミルトン路・閉路/SCC・関節点・橋・2部マッチング/木の直径・距離総和/XOR基底・GF(2)階数・行列式・彩色数/凸最大三角形・線分交差。36は二分探索(答えで二分)/分割統治(クイックセレクト・カラツバ・k列マージ)/単調スタック/バックトラック(Nクイーン・m彩色・k等分割・騎士周遊・単語分割)/再帰(4本塔ハノイ・アッカーマン・コラッツ・トリボナッチ)。37は貪欲(区間スケジューリング・会議室・締切ジョブ・ガソリン・矢・SPT完了時刻)/グラフ探索(BFS最短路・グリッド・0-1BFS・多始点・ダイクストラ・ミニマックス/最大ボトルネック・最短路DAG・経由点・最短路本数・トポロジカル・閉路検出)。38はMST(クラスカル・プリム・最大全域木・ボトルネック・座標MST)/木DP(最大独立集合・重み付き)/最短路応用(復元・本数・必須辺・最短路DAG・経由・多目的)/SCC縮約・木の中心/直径/幅。39はフロー(最大流Edmonds-Karp・辺素パス・二部マッチング)/ナップサック族(0-1・無制限・個数制限・双対・部分和可否/通り数)/古典DP(編集距離派生・LCS3列・回文分割/挿入・最大正方形/長方形・カエル跳びEDPC・House Robber・三角形・Kadane派生)/スライディングウィンドウ。**重要教訓**: フロー系の参照解は s==t で増加道長0になり無限ループ→`if s==t: print(0); sys.exit()` のガード必須。generate.mjs が固まったら scripts/probe*.mjs 方式(各refを個別タイムアウト実行)で犯人を特定できる。40は区間DP(風船割り・石併合・多角形分割min/max・棒切り)/株取引DP(k回・クールダウン・手数料)/ゲームDP(両端取り)/桁DP/グリッドDP(落下経路min/max・ダンジョン・最大長方形/正方形総数)/各種(復号数・部分列出現数・タイル張り・封筒入れ子・XOR部分集合・正しい括弧列)。41は累積和/いもす法/2次元累積和/しゃくとり(積<k・k種以下・重複なし最長・和ちょうどk)/ハッシュ集計(アナグラム・最頻・最長連続整数列・ペア和)/転倒数(マージ/BIT)/区間統合/単調デック・スタック(窓最大・株価スパン)/文字列(回文判定・最長共通接頭辞・展開長・単語逆順・繰り返し)。42はセグ木(一点更新×和/最大/最小/XOR/0の数)/遅延伝播(区間加算・代入×和/最小)/BIT(接頭和・区間加算・2本BIT・2次元・順序統計量kth)/Sparse Table(最大/GCD/AND/OR)/単調デック(窓min/max/極差)/単調スタック(部分配列のmin/max/range総和・次に小さい)/Union-Find応用(重み付き・余分辺・隣接行列)/オフライン(区間distinct・値の出現数・k番目)。43は高度数論(totient/トーシェント和/mobius/平方因子なし/素因数個数ω・Ω/floor和/約数和Σ⌊n/i⌋/CRT/拡張ユークリッド/逆元/カタラン・二項・撹乱・スターリング・分割・ベルのmod版/フィボ高速ダブリング/gcd和/互いに素ペア/メルテンス/区間篩/べき乗和Faulhaber/累乗塔)・畳み込み(多項式積/FWHT XOR・AND・OR)・行列累乗(線形漸化式・経路数)・文字列(Manacher計数/相異なる回文/Lyndon/Z和/接頭辞関数和/長さk部分文字列/Booth最小回転/ラン数)・グラフ(ハンガリアン法/最小費用流/DAG最小パス被覆/木パス最大辺)・木(距離k対)・CHT。44(lv901〜949)は高度な数え上げ/ゲーム理論(Wythoff・階段Nim・ミゼール・Grundy)/数論(ルジャンドル・位数・ヤコビ・カーマイケル・Kummer)/グラフ(オイラー閉路Hierholzer・非攻撃ビショップ)/区間DP/卵落とし/正規表現。45(lv900,950,951〜998)で**全999段完成**: Frobenius/各種数列(テトラ・ペル/companion・パドヴァン・ペラン)/凸包面積・凸判定・最大共線点/完全数・友愛・約数系/ファレイ/ピサノ周期/rencontres/ラー/カタラン三角形/推移閉包/ベルマンフォード/木同型(AHU)/プリューファー/括弧の挿入反転/snoob/円・直線・平面の領域数/原始根の個数 など。

### ⭐ 全999段達成（2026-06-03）
**lv001〜999 すべてに最低1問。空き段ゼロ。** 出典は明記せず実在の定番(AtCoder/LeetCode/CSES/古典)をオリジナル化。検証はすべて緑(validate 0 failures / prereq 穴0 / coverage 100% / tsc / build / catalog)。
**重要教訓(再掲)**: フロー等の参照解は s==t やn=1で増加道長0/空入力になり無限ループ・例外を起こしやすい→ガード必須。generate.mjs が固まったら一時的に `scripts/probe{N}.mjs`(各refを個別タイムアウト実行する診断スクリプト)を書いて犯人を特定し、終わったら消す。入力例(ins)の行数は参照解の input() 回数と必ず一致させる(辺数m・頂点数nと実際の行数の不一致がEOFエラーの典型)。

### 続け方（このパイプラインに乗せるだけ）
1. `node scripts/check-levels.ts` で**空き段リスト**を取得。
2. その段の難易度に合う**別計算の問題**を `scripts/catalog-tasks{N}.mjs` に追記（既存の catalog-tasks20〜45 が手本。全段充足後は既存レベルにクラスタを足す(catalog-tasks46〜)。各タスク `{lv, concept, title, tags, io, ask, h1, h2, ref, ins}`）。難しい段ほど難しいアルゴリズムを。
   - **再帰OK**: `再帰` は features.ts で「高度な機能」カテゴリ＝prereq の土台カテゴリ外。よって再帰を使っても prereq の穴にはならない（LCA・SCC・関節点・木DP などは再帰で素直に書いてよい）。深い再帰は `sys.setrecursionlimit` を付ける。
   - **重複の実例**: ①参照解(ref)が既存と完全一致 ②concept 名が完全一致、のどちらかで `generate.mjs` が停止する。「同じ計算量を別アルゴリズムで」（例: 最大流をEdmonds-KarpとDinicの2問）は ref は違っても**答えの量が同じ＝水増し**なので避ける。
   - **重複厳禁**: 参照解(ref)・概念名(concept)が既存とかぶらないこと。`generate.mjs` が自動検出して停止する。
   - **前提を下に**: その問題が使う道具(例: ダイクストラ←heapq, BFS)が、より低い段で既に学べることを意識（言語機能は `npm run prereq` が機械保証）。
3. 新ファイルを `scripts/generate.mjs` の import 一覧に追加（`C{N}` を足して `CATALOG` に展開）。
4. `node scripts/generate.mjs`（参照解をPython一括実行＝期待値自動確定＋重複検出）。
5. ゲート: `node scripts/validate.ts`（0 failures）/ `npm run prereq`（穴0件）/ `tsc -b`＆`npm run build` / `npm run catalog`。
6. `node scripts/check-levels.ts` で充足を再確認。1ターン20〜30段ペースが目安。

### 採点が一意になる問題だけにする（重要）
答えが手法・刻みで変わる問題（微分方程式の解を当てる等）は避けるか、「手法・ステップ数・丸め桁を固定して実装を当てる」形に限定。整数の答え／固定小数／指定アルゴリズムの実装＝採点が一意なものを選ぶ。

---

## 1. これは何か

AtCoder / LeetCode / Kaggle / 初心者向けサービスの「いいとこ取り」をした **Python コード練習サービス**。
ブラウザ（Pyodide）で完結し、必要時のみローカル判定サーバーで重いライブラリも実行できる。

### 現状サマリ（全て検証済み・緑）
- **1486問 / 8265テストケース** — ほぼ全て実Pythonで AC（手書き212 ＋ 生成1274、すべて別計算）
- **lv001〜lv999 の全999段に問題あり（空き段ゼロ達成）**（水増し梯子は撤去。本物の別問題で各段を埋め中。`node scripts/check-levels.ts` で確認）
- `npm run validate` は直近セッションで **0 failures**（optuna も import 可能になっていた）。環境差で optuna が無い場合は lv160（serverOnly）の 9 件が `ModuleNotFoundError: optuna` で落ちるが追加分とは無関係。検証したいなら `py -m pip install optuna`。
- **重複ゲート**: `generate.mjs` が参照解・概念名の重複を検出して停止（問題かぶり厳禁）
- **機能カバレッジ 100%（144/144）**
- **前提の穴 0件**（後述の学習順序保証）
- 型チェック / 本番ビルド OK
- **問題一覧は `PROBLEMS.md`**（`npm run catalog` で自動生成。トラック→レベル→クラスタ→問題で俯瞰できる）

---

## 2. 設計の核（ここが一番大事）

ユーザーと固めた設計原則。**変更時はこれらを壊さないこと。**

1. **2軸構成** — 縦軸＝レベル(難易度 lv001〜999) × 横軸＝トラック(領域)。
   - トラックは開始レベルが違う（基礎を幹に枝分かれ）: 基礎 lv001〜 / Kaggle lv085〜 / LeetCode lv100〜 / AtCoder lv120〜。
2. **1問1学び＋クラスタ** — 各問は新しい機能か新しい組合せを含む。同概念は2〜3問のクラスタで反復。水増し（同じ問題の言い換え）は禁止。
3. **目的＝Pythonのあらゆる機能を使えるように** — 数(99,900)はノルマでなく足場。進捗は **カバー率** で測る。
4. **『そんなコード知らない』をゼロに（前提知識の完全性）** — 難問の解答に要るテクニックは、すべてより前のレベルで学んでいる状態を機械保証（`npm run prereq` が穴0件）。
5. **理論セクション** — 帯ごとに型/アルゴリズムの概念を理屈から（`Level.theory`）。
6. **💡発見** — 各問に便利機能の豆知識（`Problem.tip`）。解いたら表示。
7. **3段階ヒント** — 答えを一気に見せない。
8. **2実行エンジン** — 既定はブラウザ(Pyodide)。`serverOnly` 問題(optuna等)はローカル判定サーバー。

---

## 3. 起動・検証コマンド

```bash
npm install
npm run dev        # フロント開発サーバー
npm run build      # 本番ビルド(dist/)  ※必ず通すこと
npm run server     # ローカル判定APIサーバー(127.0.0.1:8787) optuna等のサーバー実行用

# ---- 3つの検証ゲート（問題を足したら必ず全部緑にする）----
npm run validate   # 全参照解を実Pythonで採点（出題データの正しさ）
npm run coverage   # Python機能カバレッジ（網羅の進捗）
npm run prereq     # 前提知識の穴（学習順序の正しさ）

npm run catalog    # PROBLEMS.md（問題一覧）を再生成。問題を足したら流す

# ---- 問題ジェネレータ（大量の「別計算」問題を量産する仕組み）----
node scripts/generate.mjs   # scripts/catalog-tasks*.mjs のタスクから src/data/problems/generated.ts を生成
                            # 参照解を Python 一括実行(eval_batch.py)して期待値を自動で埋める＝validateは定義上通る
node scripts/ladder.ts      # lv001..999 の空段に「力試し」問題を1問ずつ自動配置 -> src/data/problems/ladder.ts
node scripts/check-levels.ts # 全999段に問題があるか検査
```

> **梯子(ladder)について**: `ladder.ts` は全段カバー保証のためのスキャフォールド(id が `ladder-`)。
> 中身は基礎演算の入力違いで水増し気味。各段に本物のオリジナル問題を足したら、その段の `ladder-` 問題は
> 不要になる（`getLevel` は未定義レベルもティア名で見出しを自動生成するので、LEVELS未登録の段でも表示される）。

> **生成の鉄則（水増し防止）**: 1タスク=1問。同じ計算の数値違いを①②③で量産しない。
> 複数の入力例は「その1問の隠しテストケース」にする。タスクは必ず別の計算(オリジナル)にする。
> `validate.ts` は全参照解を1プロセスで一括実行する方式（数万問でもスケール）。

> **Windows 注意**: `validate` は日本語出力比較のため UTF-8 を強制。
> `$env:PYTHONUTF8=1; npm run validate`
> Node は .ts 直接実行のため 22.18+ 推奨。`py`(Python) に numpy/pandas/scikit-learn/optuna が必要（このPCには導入済み）。

検証は TypeScript の型チェックも忘れず: `node ./node_modules/typescript/bin/tsc -b`（`npx tsc` は別物を拾うので不可）。

---

## 4. アーキテクチャ / 主要ファイル

- フロント: React + Vite + TypeScript + Tailwind + Monaco。ルーティングは HashRouter。
- 実行: `src/lib/pyodide.worker.ts`（Web Worker で Pyodide、import からパッケージ自動ロード）。`runner.ts`(制御) → `grader.ts`(全ケース採点)。
- サーバー実行: `server/judge.mjs`（Node標準のみ・依存ゼロ。`/run` `/judge` `/health`）↔ `src/lib/serverRunner.ts`。
- データ: `src/types.ts`（Problem/Level/Track 型）, `src/data/levels.ts`（レベル/トラック/前提色/theory）, `src/data/index.ts`（全問題集約）, `src/data/features.ts`（機能カタログ＋カバレッジ/前提チェックのロジック）。
- 問題: `src/data/problems/*.ts`（`_build.ts` の `build(level, prefix, specs)` で簡潔に書く）。複数レベルにまたがる追加バッチは `expand1.ts` のように1ファイルにまとめ、build の prefix を `lv003x` 等にして既存 id と衝突させない（同一レベルに別ファイル由来の問題が来ても id がユニークなら可）。
- 検証スクリプト: `scripts/validate.ts`（全参照解を Python 一括実行で採点）/ `coverage.ts` / `prereq.ts` / `catalog.ts`（一覧生成）。
- 生成スクリプト: `scripts/generate.mjs`（タスク→問題）＋ `scripts/catalog-tasks*.mjs`（タスク定義）＋ `scripts/eval_batch.py`（参照解の一括実行器）。出力 `src/data/problems/generated.ts`。
- 画面: `pages/Home`(学習マップ) / `LevelPage` / `ProblemPage`(実行エンジン切替) / `Achievements`(実績・ランク) / `SelfCheck` / `Coverage` / `Prereq`。

### ゲーム性(2026-06-03 追加)
- **ローカルプロフィール**: バックエンド無し。`src/lib/progress.ts` がプロフィール(複数・切替・エクスポート/インポート)を localStorage に保持し、進捗(解いた問題ID)・コード下書き・解いた日付を**プロフィールごとに名前空間分離**(`cd:<id>:solved` 等)。旧 `code-dojo:solved` は初回に default プロフィールへ自動移行。`useProgressVersion()` で React に変更通知。
- **ゲート(解放条件)**: `src/lib/gameplay.ts` の純粋関数群。**クリア=そのレベルの全問AC**、**解放=より下の出題済みレベルが全てクリア**(`isLevelCleared`/`isLevelUnlocked`)。lv1 は常に解放。Home はロックレベルを淡色＋🔒で非リンク化、`LevelPage`/`ProblemPage` はロックガードで弾く。
- **やる気要素**: XP(問題の想定行数→星1〜5×10)とランク称号(`RANKS`)、連続記録ストリーク、バッジ実績(`ACHIEVEMENTS`)、Home/`Achievements` の「次に挑むレベル(frontier)」CTA、全問クリア時の祝福＋次レベル解放演出。ヘッダ右の `ProfileBar` が称号/XP/ストリーク/プロフィール切替を担う。
- 追加/変更ファイル: `src/lib/progress.ts`(刷新), `src/lib/gameplay.ts`(新規), `src/components/ProfileBar.tsx`(新規), `src/pages/Achievements.tsx`(新規), `Home.tsx`/`LevelPage.tsx`/`ProblemPage.tsx`/`App.tsx`/`main.tsx`(配線)。型チェック・本番ビルドとも緑。

**重要**: 問題ファイルを新規追加したら **3か所** に登録する:
1. `src/data/index.ts` の `ALL_PROBLEMS`
2. `scripts/validate.ts` の `ALL_PROBLEMS`
3. `src/data/levels.ts` の `LEVELS`（メタ）＋必要なら `TRACK_OF`（トラック割当）

---

## 5. 問題の追加手順

1. `src/data/problems/xxx.ts` に `build(level, 'lvNNN', [ ...specs ])` で問題を書く。
   - 各 spec 必須: `title` / `concept`(クラスタ名) / `tags` / `statement` / `hints`(3つ) / `explanation` / `reference`(参照解=既知の正解) / `cases`(`sample:true`を最低1つ)
   - 任意: `starter` / `tip`(💡) / `serverOnly`(optuna等)
2. 上記「3か所」に登録。
3. **3ゲートを緑に**: `npm run validate`（AC）→ `npm run coverage`（新機能が✓）→ `npm run prereq`（穴を増やしてない）。型チェック＆ビルドも。
4. 出力はバージョン差で揺れないように: スカラー / 小数は `f"{x:.2f}"` 等で固定 / numpy配列は `.tolist()` / pandas は sorted・round して出す。

### 前提の穴が出たら
`prereq` が「応用問題が土台機能を初出させている」と言ったら、その機能を **より前のレベルで先に教えるレッスン**を `src/data/problems/lessons.ts` に追加する（例: break→lv007, setdefault→lv012, float('inf')→lv025, 2次元リスト→lv030, list.pop→lv009）。または参照解を既習の書き方に置換（例: `reversed(a)`→`a[::-1]`）。

---

## 6. 収録トラック / レベル

- **基礎**: 001 超入門 / 002-008 / 009-013 データ構造 / 011 タプル / 015 enumerate・zip / 020-021 関数・lambda / 025 便利構文 / 030 内包表記 / 040 例外 / 045 文字列メソッド / 050 ループ応用 / 060 collections / 070-080 標準ライブラリ / 200 クラス / 210 ジェネレータ・with / 500 再帰・二分探索
- **Kaggle**: 085 numpy / 090 統計 / 095 pandas / 105 pandas応用(merge/fillna/onehot/cut) / 110 scikit-learn / 160 optuna(serverOnly)
- **LeetCode**: 100 配列 / 150 頻出パターン / 170 頻出パターン2(循環検出/区間マージ/単調スタック)
- **AtCoder**: 120 全探索・累積和 / 130 数論 / 550 貪欲・バックトラッキング / 600 グラフ探索 / 650 グラフ応用 / 700 DP / 750 DP応用 / 800 高度なデータ構造 / 850 競プロ上級2(転倒数BIT/MST/bitDP) / 999 最高峰

各レベルは現状1〜28問。最終目標は1レベル=100問（クラスタを足して厚くする）。

---

## 7. 環境メモ（このPC）

- ✅ あり: Node 25 / Python 3.12（`python` コマンド。`py` ランチャーは無い → generate/validate/ladder は py→python 自動フォールバック対応済み。env var PYTHON でも指定可）
- ❌ なし: Docker / gcc・g++ / Java / PyTorch(torch) / **optuna**
- **optuna 問題は撤去済み(2026-06-12, ユーザー指示)**: `src/data/problems/optuna.ts` は空配列。lv160 は純Pythonのグリッドサーチ問題2問(generated)でカバー。features.ts から optuna 項目も削除(カバレッジ分母 143)。levels.ts の lv160 タイトルも「グリッドサーチ」に変更済み。

サーバー実行 `server/judge.mjs` は **サンドボックス無し＝ローカル開発専用**。`127.0.0.1` のみ待受、**外部公開しないこと**。

---

## 8. 今後の TODO（優先度順の候補）

1. **各レベルを100問へ** — クラスタを足して反復を厚く。足すたびに3ゲートを緑に保つ。
2. **Docker サンドボックスのサーバー実行** — C++ / Java / PyTorch / botorch を解禁（要 Docker＋各イメージ）。`server/judge.mjs` の executor を言語別に差し替えられる設計で拡張する。
3. **PyTorch 入門トラック**（torch 導入後、サーバー実行前提）。
4. **新トラック候補** — SQL / 正規表現特化 / Web など。
5. **カバレッジ粒度UP** — イディオムをさらに細かく登録し「未習ゼロ」をより厳密に追う。

---

## 9. 既知の制約・注意

- ブラウザ実行とサーバー実行で Python/ライブラリのバージョンが違う。pandas/numpy/sklearn 問題は出力を丸める等で吸収しているが、新規追加時は両環境で揺れない出力にする。
- `serverOnly`(optuna) はブラウザのセルフチェック対象外（`npm run validate` のローカル py で検証）。
- `levels.ts` のブロック編集はヘッダ差し替えで `{` 重複を作りやすい。編集後 `grep "level: N"` で重複確認。
- Home/LevelPage は LEVELS を level 昇順ソートして表示（配列順は不問）。

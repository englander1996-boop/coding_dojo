# 問題作成ガイド — Coding Dojo

`HANDOFF.md` の設計原則と、運用の中で固めたルールをまとめた「問題を足す人のための実務ドキュメント」。
新しい問題を1問でも足すなら、まずここを読むこと。

---

## 0. 一行でいうと

**「1問につき、新しい学びがちょうど1つ。残りは全部それより前で習っている。中身はオリジナル。出自(プラットフォーム名)は表に出さない。」**

---

## 1. 守るべき方針（設計の核）

`HANDOFF.md` 第2節の8原則が土台。その上で運用ルールを足したのが以下。

### 1-1. 水増し禁止（最重要）
- 各問は **新しい機能** か **新しい組合せ** を必ず含む。
- 同じ概念は **2〜3問のクラスタ**で反復してよい。ただし「数値を変えただけ／言い換えただけ」は水増し＝禁止。
- 反復の正解は「**同じ道具を違う文脈で使う**」こと（例: 二分探索を「配列の検索」と「答えの探索」で別問にする）。
- 進捗は**問題数でなくカバー率**で測る（`npm run coverage`）。数(99,900)はノルマでなく足場。

### 1-2. 1問1新要素（前提の完全性 / 「そんなの知らない」をゼロに）
- その問題を解くのに必要な要素のうち、**新規はちょうど1つ**。残りはすべて**より低いレベルで既習**であること。
- 厳密でなくてよいが常に意識する。例：
  - 「答えで二分探索(lv550)」は、二分探索の自作を **lv500** で学んだ**後**に置く。→ 新要素は「答えに二分探索を適用する発想」だけ。
- 機械保証は `npm run prereq`（**穴0件を維持**）。これは言語の土台機能(基礎構文/制御/データ構造/関数/便利構文/文字列)について「応用問題が初出させていないか」を検査する。
- アルゴリズムの前提順序は prereq では完全には見ないので、**作る人が難易度順を意識**する（土台アルゴリズム → その応用、の順でレベルを割り当てる）。

### 1-3. オリジナル必須・コピペ禁止
- **プラットフォームの問題文・例題・ストーリー（キャラ名など）の転載は禁止。**
- アルゴリズムや「Two Sum」「Kadane法」等のパターン名は共有財産なので使ってよい（出典番号や運営名は書かない）。
- 同じ題材でも、**入出力は本アプリ様式（stdin/stdout＋「入力例/出力例」）で自分で書き直す**。サンプルの数値も自前で用意する。

### 1-4. プラットフォーム名は学習者に出さない（特徴名で表示）
- 画面に出るテキスト（statement / hints / explanation / tip / concept / title / トラック名・バッジ / Level.description / Level.theory）に **AtCoder / LeetCode / Kaggle / paiza を書かない。**
- 代わりに「特徴名」で表す。出自と特徴名の対応：
  | 出自(内部記録) | 特徴名(表示) |
  | --- | --- |
  | 基礎 | 基礎 |
  | Kaggle | データ分析 |
  | LeetCode | コーディング面接 |
  | AtCoder | アルゴリズム |
- **出自そのものは捨てない**：`Track.id`（例 `'AtCoder'`）と `TRACK_OF`、各ファイルのコメント（`// ... (AtCoder系)`）に**内部記録**として残す。表示は `Track.label` / `trackLabelOfLevel()` を使う。

### 1-5. 決定的な出力（バージョン非依存）
- 環境差で揺れない出力にする。スカラー/小数は `f"{x:.2f}"` 等で固定、numpy配列は `.tolist()`、pandas は sorted・round して出す。
- 乱数・確率的処理は避ける。必要なら seed か決定的サンプラー（optuna は GridSampler）。

### 1-6. 実行エンジン
- 既定はブラウザ(Pyodide)。Pyodide に無い重いライブラリ(optuna 等)を使う問題だけ `serverOnly: true`。
- `serverOnly` はブラウザのセルフチェック対象外。`exit()` や対話入力など Pyodide で問題になる書き方は避ける。

---

## 2. 1問のスペック（`build()` の Spec）

`src/data/problems/_build.ts` の `build(level, prefix, specs)` で書く。各 spec の必須/任意：

- **必須**: `title` / `concept`(クラスタ名) / `tags` / `statement`(入力例・出力例つき) / `hints`(3つ。3つ目に答え) / `explanation`(模範解答つき) / `reference`(=既知の正解コード) / `cases`(`sample:true` を最低1つ)
- **任意**: `starter` / `tip`(💡発見) / `serverOnly`

書き方の約束：
- `hints` は3段階（気づき → 方針 → 答え）。一気に答えを見せない。
- `reference` は実Pythonで AC する正解。`explanation` 末尾の模範解答と一致させる。
- `tip` は「こんな機能があるのか！」の豆知識。**出典名(プラットフォーム)は書かない。**
- `prefix` は **id 衝突を避ける**。同じレベルに別ファイルから足すときは `lv130x` `lv130y` のように suffix を変える（id がユニークなら同一レベル混在OK）。

---

## 3. 追加の手順（チェックリスト）

1. `src/data/problems/xxx.ts` に `build(...)` で問題を書く（複数レベルの追加バッチは1ファイルにまとめてよい：`expand1.ts` `expand2.ts` の形）。
2. **新しいアルゴリズム/イディオムなら `src/data/features.ts` に Feature を追加**（後述のカバレッジ駆動）。
3. **3か所に登録**：
   - `src/data/index.ts` の `ALL_PROBLEMS`
   - `scripts/validate.ts` の `ALL_PROBLEMS`
   - `src/data/levels.ts` の `LEVELS`（新レベルなら）＋ `TRACK_OF`（出自の割当）
4. **4ゲートを緑に**：
   ```
   $env:PYTHONUTF8=1; npm run validate   # 全参照解が実Pythonで AC
   npm run coverage                       # カバー率（新機能が✓）
   npm run prereq                         # 前提の穴 0件
   node ./node_modules/typescript/bin/tsc -b && npm run build   # 型・本番ビルド
   ```
5. `npm run catalog` で `PROBLEMS.md` を再生成（一覧の最新化）。
6. 学習者向けテキストにプラットフォーム名が混ざっていないか確認（grep `AtCoder|LeetCode|Kaggle|paiza`）。

---

## 4. 量を増やすエンジン：カバレッジ駆動

**「水増しを機械的に不可能にする」ための運用。**

1. 先に `features.ts` に「教えたいイディオム/アルゴリズム」を Feature として登録する。
2. `npm run coverage` が、まだ問題の無いものを **「未出題」** として出す（カバー率が100%未満に下がる）。
3. その未出題を埋める問題だけを作る。→ 作った問題は定義上「新しい学び」＝水増しではない。
4. 埋め終わると再び100%。これを繰り返してカタログを育てる。

> カバー率が常に100%なら「既存の道具の新しい**組合せ**」を足すフェーズ。組合せも新しい学びとして正当だが、`concept` の重複が無いか・既存問題と被っていないかを `PROBLEMS.md` で必ず目視確認する。

---

## 5. 99,900問（999×100）への拡張方向性

「999レベル×100問」は到達点を示す**足場**であって、ノルマではない（`HANDOFF.md` 原則#3）。
量は次の4つの軸を回して増やす。**どの軸も「1問1新要素」を壊さない範囲で**。

### 軸A: 横に広げる（新トラックの増設）— 一番大きな伸びしろ
現状4トラック(基礎/データ分析/コーディング面接/アルゴリズム)。領域を足すごとに独立した数百問の新学びが生まれる。候補：
- **SQL**（クエリ・JOIN・集約・ウィンドウ関数）
- **正規表現**特化
- **文字列アルゴリズム**（KMP/Z/Manacher/suffix array — 一部は導入済み）
- **計算幾何**（ベクトル・交差判定・凸包）
- **数学/整数論**の深掘り（合同式・行列累乗・包除原理）
- **Web/データ処理**（CSV・JSON・日時・集計パイプライン）
- **PyTorch 入門**（torch 導入後、サーバー実行前提：`HANDOFF.md` TODO#2,3）

新トラックは `TRACKS` に `id`(出自)＋`label`(特徴名)を足し、`startLevel` で基礎からの枝分かれ位置を決める。

### 軸B: 縦に細かく（レベルの隙間を埋める）
lv001〜999 のうち、まだ概念の無いレベルに「難易度の踊り場」を作る。
- 既存の飛び（例 lv13→15→20→25→30…）の間に中間概念を置き、初心者が一段ずつ登れるようにする。
- 各トラックの最難(lv999)に向け、中位レベル(300〜600台)の層を厚くするのが今の薄い所。

### 軸C: 粒度を上げる（features を 132 → 数百 → 千へ）
カバレッジ駆動(第4節)の燃料。イディオム・標準ライブラリ・アルゴリズムの亜種を細分化して登録する。例：
- 書式 `f"{x:,}"` `f"{x:>8}"` `f"{x:b}"`、`bin/hex/oct`、`int(x, base)`
- `Decimal` / `Fraction` / 複素数、`bytes`、`frozenset`
- `dataclass` / `Enum` / `typing`、`@property`、`__slots__`
- `itertools`(product/combinations/groupby/pairwise)、`functools`(cache/cmp_to_key)
- アルゴリズム亜種：実数二分探索、ダブリング/LCA、SCC、最大流、桁DP、行列累乗 …

### 軸D: 組合せの体系化（量の本丸）
「**機能 × 文脈**」の表を作り、各セルを1問にする。組合せは機能数に対して二乗オーダーで増えるので、ここが量を生む。
- 例：`二分探索 × {ソート配列 / 答え / 実数 / 回転配列 / 二次元}`、`DP × {数列 / 文字列 / グリッド / 木 / bit}`。
- ただし各セルは「そのセル固有の新しい気づき」がある場合だけ作る（無ければ水増しなので作らない）。

### 進め方の目安（フェーズ）
1. **基礎を厚く**：lv001〜050 を反復クラスタで充実（初学者の離脱を防ぐ最優先層）。狭い概念は無理に100問にしない。
2. **粒度UP＋既存トラックの中位層**：軸C で features を増やし、軸B で 100〜600 台を埋める。
3. **新トラック**：軸A を1つずつ追加（SQL・正規表現あたりが着手しやすい）。
4. **サーバー実行の拡張**：Docker サンドボックスで C++/Java/PyTorch を解禁し、新トラックを足す（`HANDOFF.md` TODO#2,3）。

### 99,900 を水増しなしで埋める「エンジン」：前提分解
ネット上の膨大な問題を「レベル付け＋オリジナル化」して上位に置き、**その問題を解くのに必要な前提知識を一段下のレベルに新しい問題として補う**——これを繰り返すと、前提ツリーが自動的に大量の「別の学び」を生む。`npm run prereq`（穴0件）が「上位問題の前提が下位で教えられているか」を機械保証するので、この営みは水増しにならない。上位を1問足すたびに、その部品が下位の複数問題に分解されて増えていく。これが量と質を両立させる本筋。

実装面では `scripts/catalog-tasks*.mjs` に「別計算のタスク」を足し、`node scripts/generate.mjs` で生成する（1タスク=1問、参照解を Python 一括実行して期待値を自動生成）。**採点が一意に定まる問題（整数の答え・固定小数の丸め・指定アルゴリズムの実装）を選ぶ**こと。解析解を当てさせる微分方程式のような「答えが手法依存」の問題は、手法・刻み・桁を固定して「実装を当てる」形にする。

### 各フェーズ共通の「受け入れ基準（Novelty Gate）」
新しい1問を受け入れてよいのは、次のどちらかを満たすときだけ：
- (a) `features.ts` の **新しい Feature をカバーする**（coverage が証明）、または
- (b) 既存のどの問題とも違う **新しい組合せ/文脈**である（`PROBLEMS.md` の concept 一覧で重複が無いことを確認）。
どちらでもなければ、それは水増し。作らない。

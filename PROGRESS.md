# 進捗と作業ノウハウ — Coding Dojo

最終更新: 2026-09-06 / このファイルは「いまどこまで進んだか」と「どうやると速いか」だけを書く。
詳細な引き継ぎは `HANDOFF.md`、作問ルールは `AUTHORING.md` を参照。

## 現在地

**5,163問 / 30,336テストケース / 999レベル**（コミット b44d400 時点）

全ゲート緑: `validate` 0 failures / `prereq` 穴0 / `coverage` 未出題0 / `tsc` / `build` / `catalog` /
`quality-check` の重大指摘(high) 0件。

### レベルあたりの問題数の分布

| 問題数 | レベル数 |
|---|---|
| 4問 | 179段 |
| 5問 | 791段 |
| 6〜9問 | 10段 |
| 10問以上 | 19段 |

### 目標(各レベル10問以上)までの距離

| 目標 | 不足問題数 | 対象レベル |
|---|---|---|
| 5問化 | 179問 | 179段 |
| 6問化 | 1,149問 | 970段 |
| 7問化 | 2,121問 | 972段 |
| 8問化 | 3,096問 | 975段 |
| 9問化 | 4,072問 | 976段 |
| **10問化** | **5,052問** | 980段 |

いまは**5問化の最終盤**。残り179段は lv630〜690 と lv841〜999 のあたり。
`npx tsx scripts/check-thin.ts 5` で残りが出る。

---

## 1バッチの回し方（この手順が最速）

```bash
# 1. 残っている薄いレベルを確認
npx tsx scripts/check-thin.ts 5

# 2. 対象帯の既出を棚卸し（親が先に用意してエージェントに渡す）
py scripts/inventory.py <lo> <hi> <out.txt>

# 3. 起草（下の「並列で書かせるときのレシピ」参照）→ scripts/catalog-tasks{N}.mjs を作る

# 4. generate.mjs に import と spread の2行を追加して生成
node scripts/generate.mjs

# 5. ゲートを全部緑にする
npx tsx scripts/validate.ts        # 参照解が期待値を再現するか
npx tsx scripts/prereq.ts          # 学習順序の穴
npx tsx scripts/coverage.ts        # Python機能の網羅
node ./node_modules/typescript/bin/tsc -b
npm run build
npx tsx scripts/catalog.ts         # PROBLEMS.md を再生成
npx tsx scripts/quality-check.ts   # validate では見つからない欠陥

# 6. 引き継ぎ書に追記してコミット
py scripts/handoff-append.py <旧問数> <旧ケース> <旧残> <旧次> body.txt <新問数> <新ケース> <新残> <新次>
```

Windows では `validate` に `PYTHONUTF8=1` を付ける。`py` が無い環境では `python` に自動フォールバックする。

---

## 並列で書かせるときのレシピ

サブエージェントで量産するときは次の4点を守る。**これで1問あたりのトークンが7分の1になった**（実測 約50,000 → 約7,300）。

1. **検証を中央に集約する。** `generate.mjs` はもともと全参照解をPythonで一括実行して期待値を確定する。
   各エージェントに1問ずつPython検証をさせるのは二重払い。「5問ごとに1回まとめて、エラー・空出力・
   全件同一が無いか見る」程度に留め、正確な検算は generate に任せる。
2. **起草は安価なモデルで。** `agent(prompt, { model: 'sonnet' })`。問題を書く作業自体は十分な品質が出る。
   監査や重複判定など判断が要る仕事だけ上位モデルにする。
3. **参照資料は親が先に用意して渡す。** 各エージェントに `inventory.py` を走らせず、親が範囲ごとに
   実行した小さなファイルのパスを指定し「これだけ読めばよい。PROBLEMS.md と src/data は開くな」と明示する。
   `src/data/problems/generated.ts` と `src/data/generated.json` は9MBの1行ファイルなので grep もさせない。
4. **1問できるたびに断片ファイルへ追記させる。** 最後にまとめて書かせない。

**分担の目安は6体前後、1体あたり25段。** 20体に増やすと立ち上げコストを体数ぶん払ううえ、
セッション上限を一気に食い潰して全滅する（実際に2度、21体と27体が全滅して成果ゼロになった）。

---

## 使用量上限に当たったときの挙動

| 対象 | 挙動 | 自動再開 |
|---|---|---|
| メインループ（会話そのもの） | 待機して止まる | **される**（`autoContinueAtUsageLimit: true`） |
| サブエージェント（Workflow/Agent） | 即エラーで死ぬ | **されない** |

`autoContinueAtUsageLimit` は `~/.claude/settings.json`（Userスコープ）に設定する。
プロジェクト側の `.claude/settings.json` では効かない。
**設定はセッション起動時に読み込まれるので、途中で書き足しても現行セッションには反映されない。**

だから作業設計はこうする。**サブエージェントの成果は必ず逐次ディスクに落とす。**
git にコミット・push した時点が唯一の安全な区切りで、そこまで進めてから
`/exit` → `claude --continue` で再起動すれば失うものはない。

---

## 品質検査で何が見つかり、何が見つからないか

`npx tsx scripts/quality-check.ts`（`--all` で全件、`--strict` で high があれば exit 1）

**なぜ必要か**: 期待値は参照解の実行結果から自動生成されるので、**参照解そのものが間違っていても
`validate` は必ず通る**。実際 lv130 で「2つ以上の連続する正整数の和」なのに初項0の列まで数える誤りが
長く残り、6ケース中3ケースの期待値が誤っていた（正しく解いた学習者がWAになる状態だった）。

**検査する項目**: 定数解（全ケースの期待値が同一で、定数を print するだけで通ってしまう）/ 出典名の露出 /
参照解の完全一致 / 同名タイトル（同一レベル内・レベルまたぎ）/ 空の期待値 / 素の `exit()` /
ref・期待値の非ASCII / ケース不足 / ヒント不足。

**`generate.mjs` の重複ゲートも強化済み**: 従来は参照解と concept 名の完全一致しか見ておらず title を
見ていなかった。いまは同一レベル内のタイトル完全一致で停止し、レベルまたぎの同名と
「全ケースの期待値が同一」は警告表示する。

**それでも誰も検査していないもの**（作る人が保証するしかない）:
- **参照解そのものの正しさ**。問題文の条件を1つずつ参照解に照らし、境界（n=1・0・負・同点）を手で確かめる。
- **手書き問題（`src/data/problems/*.ts`）との重複**。手書きは generate を通らないのでゲートの対象外。
- **同一レベル内の出題パターンの単調さ**。1問ずつ見れば別計算でも、「〜の個数」が4問並ぶ段は
  学習者には同じ問題に見える。

---

## 作問でよく踏む地雷

- **`ins` に JavaScript の式を書かない**（`.replace()` や文字列連結）。純粋な文字列リテラルだけ。
- **参照解の出力に日本語を使わない**。Windows の cp932 で化ける。勝敗は `first`/`second`、可否は `yes`/`no`。
- **`ins` の行数と参照解の `input()` 回数を一致させる**。「1行目に n、続く n 行」なら n と実際の行数を合わせる。
- **`while` を使う参照解は全 `ins` で止まることを確かめる**。過去に「997は非ハッピー数で無限ループ」
  「LFSRのタップが可逆でなく初期状態に戻らない」で生成がタイムアウトした。
- **`ins` はちょうど6件、答えが全部同じにならないこと**。定数を出力するだけで正解になってしまう。
  過去に4×4数独の入力例6件を同じ完成盤から作ってしまった。
- **出典名を学習者に見える文面に出さない**。`ins` の中の `leetcode` という単語まで検査対象。
- **Bash のヒアドキュメントは円記号を1段外す**。円記号を含む書き換えは Write でスクリプトファイルを
  作ってから実行するか、Edit ツールを使う。
